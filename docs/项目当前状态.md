# AgentBridge 项目当前状态

> 状态日期：2026-08-30
>
> 收口候选基线：本文件所在提交
>
> 线上 Release：`08d7ac09341a`
>
> 用途：当前实现事实的唯一汇总。历史阶段证据见 [验收目录](./验收记录/验收记录导航.md)。

## 1. 当前结论

AgentBridge 已从早期浏览器扩展原型演进为可运行的中心端、多系统、多用户、多端智能体能力平台：

- 最终用户设备不安装遗留系统连接器、Chrome 扩展或本地 AgentBridge daemon；
- OpenClaw 等宿主通过 HTTPS MCP 调用中心 AgentBridge；
- `agentbridge.host.v1` 已把 OpenClaw 从协议事实来源收敛为 Host Adapter，并提供独立 Python
  Reference Host 证明同一中央能力可由第二宿主调用；
- 登录、业务字段填写和最终授权通过模型循环之外的可信卡完成；
- Telegram、微信和独立 Agent Workspace 可使用同一用户主体接续任务、同步状态、展示卡片和交付文件；
- 多个真实用户使用独立 Token、身份绑定、系统会话、浏览器 Profile、操作账本和通道路由；
- 所有正式写入都经过冻结计划、独立授权、单次提交和权威回读，结果未知时停止且不自动重试；
- 跨系统目标可以形成中央持久任务计划，按公共能力、注册转换和一个受控写入终点执行，等待可信交互或重启后从权威步骤续办；
- Chrome 扩展、旧浏览器桥、localhost daemon 和代理型 CLI 已整体退役。

当前阶段仍是受控内网 PoC/试点基线，不等同于企业级生产平台。每用户独立 OS/容器 Worker、
企业 OIDC/OBO、Vault/KMS、正式 PKI、高可用和异机灾备仍在路线图中；试点 SLO、运行事件和
一致性备份已经进入运行治理基线。

## 2. 已接入系统

| 系统 | 会话路线 | 已发布能力 | 当前边界 |
| --- | --- | ---: | --- |
| Seeyon OA | 每用户中心浏览器 Profile + HTTP Session | 51 | 17 个读取、6 个可逆写、28 个受控写；覆盖流程、证书、组织通讯录、发起、审批、知会、确认、会议和撤销 |
| 泰华日志系统 | 每用户中心 Token，会话刷新与保活 | 5 | 个人/团队日志、项目查询、日志受控创建 |
| 部门信息库（语雀） | 每用户中心浏览器 Cookie；noVNC 可信交互登录 | 4 | 公共知识库、跨库目录、组织搜索、Doc/Sheet/Table 结构化只读 |
| 照明实验室测试系统 | CAS 登录后的中心 Cookie + JWT 再签 | 34 | 26 个读取（24 个正式能力、2 个兼容入口）、6 个可逆写、2 个受控写；覆盖运行态、告警、能耗、巡测、真实支路漏电、巡检和检修，不开放通用设备控制 |

中央能力注册表共 95 个业务能力。MCP 还包含会话、可信交互、任务、文件和治理工具；
本期中央目录共有 137 个 MCP 工具。具体宿主可见目录按 Token scope、宿主等级和公开工具策略
投影，不再把某一次 OpenClaw 投影数量写成长期固定值。

系统明细统一从 [系统适配目录](./系统适配/系统适配导航.md) 进入。

## 3. 用户端与任务连续性

当前接入端：

- Telegram 私聊；
- 微信私聊；
- Agent Workspace 网页端 `https://10.10.50.213:8783`；
- 管理控制台 `https://10.10.50.213:8782`，它不是普通用户智能体客户端。

已经实现：

- 一次性网页身份配对和持久登录；
- 同一 `userSubject` 跨端任务、文本、状态和可信卡片同步；
- 卡片由多个合格端展示，但只能按服务端状态完成一次；
- 图片输入、图片预览与下载；
- 任务文件交付、到期展示和重新生成下载入口；
- 自然语言跨端接续，不要求用户记忆任务号；
- Workspace 字段卡即使在伴随端认领通知前已经填写完成，OpenClaw 也会从任务事件恢复完整交互并
  自动续办；等待与完成事件连续到达时只续办一次，不要求用户再次发送“继续”；
- Workspace 到 Agent Host 的受理前请求先写入中心持久等待区；宿主短暂不可达时按同一幂等键
  自动等待，页面刷新、SSE 断开和 AgentBridge 重启不丢请求，恢复后最多产生一个宿主 `runId`；
- 两个真实 OA 用户的身份和结果隔离验证。

OpenClaw 当前兼容基线为 `2026.7.1`，`agentbridge-interactions` 插件版本为 `0.4.70`。

宿主无关化与标准远程 MCP 接入一期已实现并完成正式只读纵切：中央服务发布共享 Schema、分级协商、宿主登记、
任务上下文、协调 Lease、恢复和运行快照；Reference Host 覆盖 L1/L2/L3；OpenClaw 插件按同一
契约工作。两个试点宿主都诚实声明 `mcpApps=false`，通过受审计私有呈现器展示中央可信卡；原生
MCP Apps AppBridge 留给后续外部宿主兼容性二期。

跨系统编排一期已经实现通用持久任务计划：OpenClaw 或 Reference Host 只负责根据安全目录提出
结构化计划，中央服务确定性校验、持久化和执行。计划可以组合多系统读取与注册转换，并以一个
现有受控 `prepare` 作为唯一写终点；登录、字段填写和授权完成后由中央续办。Workspace 显示步骤
进度，管理端只显示非敏感治理投影，宿主与前端均看不到私有 commit 或授权内容。

## 4. 当前部署

中心服务部署在 Linux `10.10.50.213:/home/guomao/agentbridge`，由
`agentbridge.service` 托管，使用版本化 wheel、内部 CA、私网 IP HTTPS 和受限服务账号。

| 端口 | 服务 |
| ---: | --- |
| 8780 | 可信认证、字段和授权卡片 |
| 8781 | 按挑战开放的 noVNC 网关 |
| 8782 | 管理控制台 |
| 8783 | Agent Workspace |
| 8790 | Streamable HTTP MCP |

Workspace 通过服务器回环地址 `127.0.0.1:18789` 使用 Windows 工作站主动建立的反向 SSH
隧道访问 OpenClaw Gateway，工作站切换有线、无线或外部网络时无需修改服务器地址。隧道启动器
使用单实例互斥并识别更新任务遗留的同目标 SSH，避免多个包装器反复争抢服务器 `18789`。
中心端默认提供 60 秒受理前持久等待，连接恢复按 `1、2、3、5、8、10...` 秒退避；受理后只
观察和核对同一 `runId`，不因连接中断重投原请求。

会话保活间隔为 10 分钟，活动租约为 7 天。后台探测只维持仍在租约内且可续期的会话，
不会把已被下游明确注销的会话伪装为有效。

运行治理二期已加入持久化 Trace、Span、Signal、Incident、SLO 和 RecoveryAction，异常检测
使用独立于会话保活的后台周期。管理端可以查看端到端阶段时间线、运行事件和恢复账本，并且只
允许重投已确定通知、归档投递和重新评估运行状态。每日一致性 SQLite 备份由独立 systemd timer
执行，备份创建时立即做完整性、必要表和跨用户关联检查。Windows OpenClaw Gateway 与 Workspace
隧道已具备任务计划自愈；Gateway 的人工重启、发布重启和异常恢复统一使用可见前台生命周期
控制器，守护器会识别 180 秒内的冷启动、重复实例、失效锁和“后台仍活着但前台窗口消失”的
情况，最终保持一个监听实例和一个用户可见窗口；中央备份已具备隔离只读恢复演练；七天影子观察
按小时采集并按日汇总。

完整运行说明见 [当前内网部署](./部署运维/当前内网部署.md)。

## 5. 写操作治理

面向智能体公开的是业务意图，例如“提交出差申请”或“审批补签申请”，不是底层 HTTP、DOM、
选择器或 commit 工具。标准链路为：

```text
用户意图
  -> prepare / 读取实时业务状态
  -> 可信字段卡（需要补充字段时）
  -> 冻结计划与独立授权卡
  -> 内部 commit（对模型隐藏）
  -> 权威回读 / 明确成功、失败或 unknown
```

跨用户、跨会话、过期、字段漂移、目标漂移、重复消费和权限不足均失败关闭。可逆动作提供明确
补偿能力；不可逆动作必须单独标记，不因存在通用写权限而自动开放。

补签接收处理已增加批量受控编排一期。用户表达“处理所有补签申请单”时，中心端一次冻结当前用户的
候选待办，每项仍独立填写和授权；当前项经 OA 权威回读成功后自动出现下一张卡，不再要求用户发送
“继续”。批次由一个父任务持久化，明确失败、拒绝、过期或结果未知时保守停止，多端重复回调不能跳项。

跨系统持久计划与补签批量编排是两种不同机制：前者组合不同系统的公共原子能力和注册转换，后者
处理同一流程类型的多条冻结事项。二者都复用 Task Hub 和可信写入，但不会互相代替。

## 6. 验证基线

截至本状态日期：

- 线上 `agentbridge.service` 为 `active`；
- 管理端和 Workspace 健康检查均为 `ok`；
- 线上 Release 为 `08d7ac09341a`，宿主无关化一期、运行治理二期、本机宿主自愈、隔离恢复演练、影子观察、OA 通讯录只读能力、补签批量受控编排一期和 Workspace 受理前可靠投递均已部署；
- 发布验收最近 30 分钟服务错误数为 0；
- MCP 源码目录共有 137 个工具，新增计划目录、准备、查看和取消四个公共入口；OA 通讯录 9 个只读工具、补签批量受控 prepare、照明读取四期、离职申请专用审批和 Agent Host 公共契约工具均保留；
- MCP 宿主调用已对幂等读取和受治理 `prepare` 增加有界短时传输恢复，内部 `commit` 仍不自动重试；Workspace SSE 客户端断流已降噪；
- 照明读取三期、四期均已完成真实 MCP 冒烟和 Workspace 自然语言只读验收；四期还覆盖连续设备上下文、边界保护和两类 CSV 文件交付；
- 四系统试点收口一期已完成双用户在线发布验收、四系统真实 Workspace 读取、照明 26 个读取入口和 7 类 CSV 交付；第一用户 OA 接收处理已有普通协同、周报知会和补签审批等多次真实提交及权威回读证据，本轮待办为 0 不构成验收缺口；
- 两个 Workspace 已按 `xinguomao` 和 `lishiyu` 明确区分，当前活动任务、待投递和身份一致性违规均为 0；
- OpenClaw Gateway 故障注入后由当前用户计划任务守护器自动恢复；重启和恢复后均保留标题明确的前台窗口，守护区分冷启动、失效启动、隐藏实例和真正重复实例；
- 本次发布后 Gateway、插件注册和 MCP Release 检查正常；无工具模型热身连续两次热路径耗时
  `104.555s` 和 `98.084s`，超过 `60s` 发布性能门槛，已作为 OpenClaw/模型调用性能告警保留，
  不影响本次批量编排功能与服务健康结论；
- 最新一致性备份已完成隔离只读恢复演练，写入探针被拒绝，源 Release 和哈希一致；
- 七天影子观察批次 `c130e09f-e05c-4461-8112-5ff76083ac69` 已于北京时间
  `2026-08-30 13:51:00` 自动完成，共形成 167 个快照；
- 发布、恢复和观察检查不读取业务列表，也不执行任何业务写入；
- 项目保留 Python、Workspace Gateway、OpenClaw 插件、MCP App、文档链接和发布治理检查。
- 宿主无关化共享 H01-H29 套件通过；OpenClaw 插件 159 项通过；全项目 Python 740 项通过、
  3 项按既有环境条件跳过；Reference Host 源码公共协议边界检查通过。任务计划覆盖目录过滤、
  Proposal 校验、持久化、确定性转换、单写终点、Interaction 自动续办、重启安全恢复、双宿主与
  Workspace/管理端非敏感投影。
- Reference Host 已通过正式 MCP 分别读取两个用户的 OA 会话状态，并以第一用户读取 1 条实时待办；
  两身份均协商到 L3，桌面、390px 窄屏和 SSE 任务时间线通过，未执行业务写入。
- 两个真实 Workspace 已在同一分钟并发读取各自 OA 待办，形成不同 Task、Operation、run 和
  Endpoint；每个任务只有一次业务调用，主体与结果隔离，活动任务最终均为 0。受理后反向隧道
  断线注入能够从中央账本恢复同一结果且不重放；无有效 Interaction 的历史等待空任务会安全过期。
- Workspace 受理前可靠投递已通过无监听故障注入：页面在离线阶段显示持久等待，恢复后原请求
  自动完成；最终 `retry_count=6`、`attempt_count=1`、`host_accepted=1`，幂等键与唯一
  `accepted_run_id` 一致。全项目 Python 为 716 项通过、3 项按既有环境条件跳过、526 个
  subtest 通过。

本期证据见[四系统试点收口一期结果](./验收记录/四系统试点收口一期结果-2026-08-22.md)、
[运行治理与可靠性二期验收记录](./验收记录/运行治理与可靠性二期验收-2026-08-23.md)和
[本机宿主自愈与恢复演练](./验收记录/本机宿主自愈与恢复演练-2026-08-23.md)，以及
[模型上下文协议短时传输恢复与本机宿主发布验收](./验收记录/模型上下文协议短时传输恢复与本机宿主发布验收-2026-08-25.md)和
[宿主无关化一期工作台整体自测](./验收记录/宿主无关化一期工作台整体自测-2026-08-30.md)，以及
[工作台受理前可靠投递验收](./验收记录/工作台受理前可靠投递验收-2026-08-30.md)。具体某一阶段
“当时验证了什么”应查阅带日期的验收快照，不从本页反推历史结论。

## 7. 下一阶段重点

1. 跨系统编排一期完成代码和自动验收后，先完成 OA 已办到泰华日志草稿的 Workspace 真实纵切；
   真实写入只在有明确测试内容和逐次授权时进行，再用照明或语雀来源复验通用性。
2. 宿主无关化一期已完成正式远程只读纵切；后续观察运行数据，并在会话自然需要登录时补 L2
   登录续办证据，有合适低风险目标时再单独授权真实写纵切，不为证明协议而制造业务流程。
3. 新 OA 流程类型出现时继续做逐事项真实增量验收；不重复证明已通过的接收处理治理链路，也不使用第二用户待办替代。
4. 持续观察四系统会话和 OpenClaw 外部通道冷启动耗时，完善历史失败投递的治理展示。
5. 保持照明系统现有读取与受控写边界，暂不进入读取五期或通用设备控制。
6. 为更多用户试点补齐独立 Worker 安全主体、企业身份和密钥托管。
7. 运行治理二期已完成七天影子观察、本机宿主自愈和隔离恢复演练；下一步评审 SLO 基线并开展
   异机恢复，限流和正式安全事件响应仍待建设。
8. 新系统接入坚持“先读、再低风险可逆写、最后受控不可逆写”的递进路线。

未完成事项以 [后续增强事项](./后续规划/后续增强事项.md) 为准。
