# 数据库能力控制台动态授权实施与验收

实现日期：2026-09-12；真实 Workspace 验收：2026-09-13。

## 已实现

按《数据库能力控制台动态授权设计》先落文档，再实现。管理控制台“用户与令牌”可查看和配置每个中央账号的七项独立数据库能力；自由只读 SQL 单独列为高级选项。空列表可撤销全部授权，保存要求填写原因并比较配置版本，避免覆盖其他管理员的更新。审计员仅能查看。

能力仍由中央账号识别，每次数据库调用读取最新授权，不映射数据库人员 ID。此次修改不涉及 OpenClaw 插件、Token 签发或撤销、宿主配置。授权变更与数据库授权审计同事务保存，并写入管理审计。

同时补上管理接口缺失 CSRF 请求头时的拒绝逻辑；此前 None 会使会话验证跳过 CSRF 比较。窄屏弹窗固定标题和操作按钮，中间内容可滚动。

## 本地验证

- 管理接口、独立数据库和内容分析：40 项测试、80 个子测试通过。
- 新增集成测试使用真实本地管理 HTTP 服务、两个中央账号预先签发的原 Token 和同一 MCP 服务实例。管理接口启用七项能力后，两账号均可调用；撤销 guomao 后执行拒绝且不进入查询，lishiyu 不受影响；恢复后原 Token 继续可用。底层 SQL 执行使用桩，此项不是生产数据库验收。
- 未登录、审计员写入、错误或缺失 CSRF、错误 Origin、未知账号、无效能力与版本均拒绝；旧 revision 返回 409；成功操作的前后状态和审计数量一致。
- 隔离 Chromium 测试验证管理员勾选保存、重开回显七项、审计员七项禁用、1280px 和 390px 布局无横向溢出、窄屏标题及保存按钮可见。截图在 output/admin-database-grants-20260912/。
- 原有控制台全视图浏览器测试首次运行失败，输出伴随本地静态资源连接中断；单独重跑通过。新增授权浏览器测试通过。
- 文档检查：7 项测试、690 个子测试通过。

## 线上配置与 Workspace 状态

2026-09-13 已通过 Chrome 扩展复用管理控制台 admin 登录态、普通窗口 xinguomao（中央账号 guomao）与另一 Workspace 的 lishiyu 登录态。两账号均从控制台启用全部七项能力，包括高级自由只读 SQL。最终 guomao 授权版本 3、lishiyu 版本 1，均为七项。

前一日 Chrome 工具初始化返回 `failed to write kernel assets` / `os error 3`，在页面发现前失败；扩展启用和 Native Messaging 注册正常，重连无效。用户重启 Codex 后本次工具恢复，可读取原页面；没有重新登录或迁移 Cookie。此前阻塞已解除，但具体缺失路径根因未确认。

### 七项实际调用

两个 Workspace 各发送一次 DB7-0913 请求，先取 database_capabilities，再逐项调用 database_execute。页面回执与服务端数据库审计的 query_id 一致，以下 14 次均为 succeeded。

| 能力 | guomao query_id | lishiyu query_id |
| --- | --- | --- |
| database.schema | f291b0bdf12542489154163efacc2a83 | 597a27ac6c1f4d2cb64cdd9d1367e32a |
| database.directory | c38422c5e6f04d989c0469a9cca98bee | 3ec72c1b428a45878b9466cf6381df1d |
| database.logs.query | 9effd7445e9e47ebae4540c8d6bfa32d | c38f7c30cb5245e2bf3662e89988a2f2 |
| database.logs.analyze | 745f4b9b2eb84e28bc011817c48b05f3 | 3a859018539146e8abcd5f79e15e277e |
| database.logs.content_analyze | 03e781a6d638419fba94d018b24a7454 | f3ca2fde0b6d4f8398131a92d868a069 |
| database.comments.analyze | c7d4eff4496643a2bad6ace50d7b03ef | ef4ffab945bb4c809e19eb58879e2888 |
| database.free.read | a8a296337d144ff0aa6340e470989d6c | 0d76366d20a644e288e6113fba53f54d |

目录按姓名检索命中一人。2026-09-06 DAILY 统计为 7 条、登记工时 63.5；自由 SQL 同日计数为 7。正文查询和内容分析返回 2/7 条并明确 has_more=true，答复标注“样本而非全量”，引用证据 log:300430014。评论以 comment_created_at 为日期口径，2026-09-09 返回 2/2 条、has_more=false。此次验证能力链路和样本来源，未将两条正文样本宣称为全量语义分析。

### 同 Token 动态撤销、隔离、恢复

1. 09:00:29 管理控制台撤销 guomao 的 database.logs.analyze，版本 1 → 2，其余六项保留。
2. 原 guomao Workspace 发送 DB-REVOKE-0913：最新目录不再包含该能力，实际执行返回 rejected / DATABASE_CAPABILITY_DENIED；未改用仍被授权的自由 SQL。
3. 原 lishiyu Workspace 发送 DB-ISOLATION-0913：同项统计 succeeded，query_id=207b53c6178641afa7b8234c7c39f359，仍为 7 条、63.5 工时。
4. 09:02:03 从控制台恢复 guomao 七项授权，版本 2 → 3。原 Workspace 发送 DB-RESTORE-0913：目录为七项、执行 succeeded，query_id=2a5410fcda214dcd8b5ffd48aa646e41，7 条、63.5 工时。
5. 管理审计可见四次 database.grants.update（两次启用、一次撤销、一次恢复）及账号、原因和成功状态；数据库授权审计包含对应前后版本。

整个验收期间沿用原 Token；控制台原有效 Token ID 前缀 guomao=8016c0e0、lishiyu=b5ec064e，令牌数量保持 1/14、1/5。OpenClaw Gateway PID 保持 27812，发布输入校验 required=false、inputs_unchanged，包括 Token 所在环境文件指纹未变。没有重启 Gateway、重签 Token 或调用业务系统写操作。

证据文件：output/admin-database-grants-20260912/workspace-audit-0913.json（只含查询元数据和授权前后状态）、live-mcp-enabled.json（原 Token 实际读取成功）。实现版本为 335e2b33e379；后续验收文档发布不改变实现和授权。
