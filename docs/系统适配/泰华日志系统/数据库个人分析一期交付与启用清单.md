# 数据库个人分析一期交付与启用清单

更新：2026-09-11。已按用户确认范围收尾，下载保存已确认通过，其余验收补项暂保留；最终状态见[一期结项说明](./数据库分析一期结项说明.md)。一期 A/B 已部署，按用户明确授权向 guomao（日志账号 xinguomao）和 lishiyu 开放本人受控只读试点。生产实测见[试点验收记录](../../验收记录/泰华日志系统/数据库受控只读生产试点验收-2026-09-09.md)，本轮收尾见[一期收尾验收](../../验收记录/泰华日志系统/数据库分析一期收尾验收-2026-09-10.md)。这不是团队分析或全员上线。

## 已实现

- 固定本人日报输入、双 API 完整性与当前身份核验、受限 PostgreSQL 只读事务、确定性汇总与前后可见性核验。
- 独立 Scope、准入白名单、数据源默认关闭、密钥加密、会话代际与身份变更、加密结果及期限清理。
- 幂等命中重新鉴权、登录一次续办、取消及期限、全局和本人并发限制、进程独占与中断恢复标记。
- 两个日期窗口的 v2 持久比较计划、仅保存引用、零基期及非等长口径、读取时重新核验两份来源。
- 每日 CSV 生成、重新签发、认证下载、公式转义、独立导出 Scope、十分钟下载引用与最多二十四小时加密文件。
- OpenClaw 私聊 CSV 附件交付、Workspace 登录下载文件卡、生成目录、管理台与 CLI 显式权限选项、操作和任务进度摘要。

实现与调用方法见[开发与启用说明](./数据库个人分析开发与启用说明.md)，原始约束见[详细设计](./数据库个人只读分析一期详细设计.md)。

## DBA 核验单与分级说明

2026-09-10 后续变更：DBA 已完成源库收权，账号仅能读取 analysis 下五个脱敏视图。新版固定查询 analysis.work_logs，已发布 007c17004db2 并恢复 strict；预检和两用户样本通过，正式接口结果见最新验收记录。以下清单保留原检查背景，不再表示 TEMP、序列权限仍未处理。

这里需要区分实际影响：TEMP 是创建临时表的权限，不等于可修改正式业务表，属于资源及最小权限加固建议；序列 USAGE 包含 nextval，可以推进序列，属于严格只读账号应收敛的状态修改能力；可读表范围涉及数据保密，尤其应排除认证与令牌表，业务表可以随分析需求逐步增加。参见 [PostgreSQL 16 权限说明](https://www.postgresql.org/docs/16/ddl-priv.html)。

当前生产使用 strict。2026-09-09 首次启用时曾使用 controlled_readonly_pilot 接受 TEMP 和序列 USAGE 提示，该临时配置已退出。以下为持续核验清单，不表示仍待执行；角色默认只读和连接来源等 DBA 配置应保留维护回执，应用预检不代替完整 DBA 审计。

对象为 sisyphus 数据库的 xinguomao 角色。无需提供后端源码，也无需把密码放进工单。请数据库管理员检查**最终有效权限**，包括 PUBLIC 和继承角色：

1. 没有超级用户、BYPASSRLS、建库、建角色权限；无可切换的高权限继承角色。
2. 没有表及列级 INSERT、UPDATE、DELETE、TRUNCATE；无数据库或 schema CREATE。
3. 没有数据库 TEMP；没有 public 序列 USAGE/UPDATE。不能仅撤销角色上的直接授权而遗漏 PUBLIC 授权。
4. 读取范围按一期查询需要收敛，当前通过 analysis.work_logs 视图保障 id、user_id、log_date、hours、type_code、project_id、created_at、updated_at。认证、令牌及密码相关表不应因“全部 public 表可读”继续暴露；同时核查 future/default privileges。
5. 核查角色默认只读设置和连接来源限制。代码强制只读事务不代替角色默认设置或数据库权限收敛。

后续复检不通过 INSERT、nextval 或 DDL 试探生产写入。DBA 变更后运行 `python -m bscli.analytics.admin --home <中心目录> check`；该命令只读元数据并以空 ID 数组执行固定查询。只有权限门禁通过后，再安排实际取数验收。

## 当前收尾状态

| 项目 | 状态 |
| --- | --- |
| 权限策略 | DBA 已收敛至 analysis 五视图；适配已发布且 strict 已恢复，预检无权限告警 |
| 隔离 PostgreSQL | 实际写入拒绝、取消、断连释放已通过 |
| 当前实现实际对账 | 本人两周、空窗口、逐行及聚合一致性、结果重读、导出撤权拒绝已通过 |
| 部署与重启 | Linux 驱动、加密存储、固定发布和重启后结果读取已通过 |
| 模型编排与附件 | 真实模型两窗口比较、受保护导出及文件内容脱敏通过；Telegram 路由恢复后，原生工具附件回执确认 1 份发送成功、0 失败 |
| Workspace 真实界面 | guomao 的同轮汇总→导出→文件卡→实际下载及桌面/窄屏检查通过；lishiyu 的比较、导出、文件卡及用户确认保存后实际 CSV 对账通过。初次长请求的误报失败已由 Workspace 持久结果恢复修复，见 2026-09-10 Workspace 恢复验收记录 |
| 普通用户 | lishiyu 实时核验为 EMPLOYEE，人员 ID 300002378；两周、空窗口、结果重读和 CSV 对账通过 |
| 跨用户隔离 | guomao 与 lishiyu 双向读取对方结果、导出对方结果、下载对方报告共六项均返回 DATA_ACCESS_DENIED；ADMIN 也不能跨中央用户读取 |
| 第二用户撤权 | 短暂移除 lishiyu 准入后，汇总、重读、导出、下载均被拒绝；finally 恢复后重读成功 |
| 运维清理 | 每分钟治理循环已实测清理过期合成记录，保留未过期记录 |

回退时关闭 source.json 的 enabled，新查询、历史结果读取和下载都会被拒绝；不修改原有日志系统的读取与写入能力。已经交付给用户的 CSV 副本无法通过中央开关收回。

## 交付入口

- [一期结项说明](./数据库分析一期结项说明.md)：范围、发布版本、证据和后续边界。
- [一期运维交接说明](./数据库分析一期运维交接说明.md)：检查、凭据、过期、停用与恢复。
- [一期手工验收方案](../../验收记录/泰华日志系统/数据库分析一期手工验收方案.md)：待执行用例和结果记录。
