# 维护治理第五阶段验收

日期：2026-09-21。基线为 M4 已部署版本 `1a4fe29e165b`，开工仓库为 `63c5a7f`。本记录区分工程实现、测试、实际部署及外部治理；M5 不因代码合入就自动全部完成。

## 工作包

| 事项 | 本轮实现 | 验收边界 |
| --- | --- | --- |
| F09 职责拆分 | 写入注册与作用域、受控写执行器、原任务读取恢复、OpenClaw 读取描述与展示分别抽出；插件 0.4.97 注册 L3 兼容基线 | 保持原实例账本、任务锁、授权消费事务及幂等；计划运行复用既有模块，通知投递继续渐进拆分 |
| F10b 发布治理 | 无生产凭据的候选 CI；绑定完整提交、分支、事件、工作流和最新尝试；部署前等待成功，在线验收后晋升 main | 开工时 main 未保护、规则集为空。远端必需检查须启用并读回，不能用本地脚本替代 |
| F11 当前事实 | 自动生成版本、分系统效果数量、104 能力及 149 工具总数；分离代码与部署/验证/真实业务状态 | 修正旧泰华 9 项为实际 5 项；历史数字保留；不将源码版本当作运行版本 |
| F12 传输例外 | 两类既有明文链路登记范围、未证实补偿、责任角色、复核日及退出条件；新模板默认 TLS | 现网 TLS 改造与保护措施仍待确认，未关闭例外 |
| F13 资源预算 | HTTP 服务连接与来源额度、空闲超时及绝对连接寿命；认证用户 SSE 额度；MCP 全局并发上限 | 隔离慢连接/断流测试；NAT、全局耗尽、MCP 慢请求和真实容量基准仍有边界，不宣称扩容验收完成 |
| F14 配置与公开内容 | 实际三份发布单元原样移至仓库外受控配置，显式选择目标和摘要，公开模板脱敏；高置信凭据扫描进入门禁 | 不改写历史；历史文档与辅助运维脚本仍需逐项脱敏；扫描不是业务数据审查 |

规则与预算数值见[维护治理与扩容边界](../部署运维/维护治理与扩容边界.md)。

外置环境清单与三份单元已在既有独立备份节点保存一次副本。压缩包 SHA256 为 `9bab8b94bcb65217e335a69f2796137ece01ee2b8d23fe16c303d2ffc5ee4521`，本地与远端一致，远端权限 600、所有者 root；不含解密密钥。该副本不是自动版本同步，后续配置变更仍须重新备份；不据此确认地理故障域。

## 实施中发现的问题

首轮注册表移动后，计划权限分支漏导入 `_CAPABILITY_SCOPES`，定向测试捕获并修复。一个跨行定义的业务函数未被最初的机械提取覆盖，已补齐导入，并增加“每个注册写入的所有函数都可调用且有权限表”的完整性检查。故障注入位置随函数绑定迁移到新注册模块，仍保留原断言。首轮测试不作为通过证据。

固定候选 `5433e4c` 的首轮全量结果为 1021 通过、11 跳过、1238 子测试通过，另有 1 失败、13 个夹具错误，未部署：批量任务夹具动态拼接了旧模块路径，插件内部两处版本常量仍是 0.4.96。后续修复注入路径和版本一致性，保留失败记录，不删减测试断言。

第二候选 `92c3700` 的 Python 全量通过，但 Node 插件 5 个断言仍写死旧版本，被发布检查拦住，同样未部署。测试期望版本随后改从 package.json 读取，保留与实际宿主上下文的完整比较，插件 204 项定向复测通过。

候选 `a5c9e3c` 本地全量通过（1035 项、11 跳过、1241 子测试，Workspace 33、OpenClaw 204），但首次独立 [GitHub CI](https://github.com/guomxin/cli-helper/actions/runs/35523902393) 缺少 Chromium，离线 DOM 测试无法启动，1034 通过、1 失败。正式验证入口随后补齐 Playwright Chromium 安装，不跳过测试。该候选未部署；同时将 GitHub 状态读取的瞬时传输失败限制为最多三次尝试，HTTP 拒绝立即失败，仍只接受 success。

## 发布与验证

待固定候选全量验证、候选 CI、发布及在线验收完成后补记；此段未补齐前，不宣称已部署。本轮只执行模拟写入边界测试和在线只读健康检查，不发起实际业务写入。

## 尚未关闭

远端必需检查启用与读回、既有下游 TLS 保护、MCP 慢连接的分身份治理及实际容量验收、历史公开材料的系统性脱敏。这些事项必须单独形成证据；M4 真实模型盲测、历史组织树根因和 M2 地理故障域也不因本轮修改而关闭。
