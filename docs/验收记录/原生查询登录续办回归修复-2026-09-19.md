# 原生查询登录续办回归修复

日期：2026-09-19。插件版本：`0.4.96`。

## 故障与原因

9 月 18 日 Workspace 查询 OA 待办，13:47:44 返回 `LOGIN_REQUIRED`。
原查询任务 `b0ab08e5-4d85-4f84-ab8c-675c17ba5635` 停在 `waiting_user`，没有关联登录交互。
13:47:52 登录另建任务 `7458fd3e-7742-4950-a3ab-b7658fc2374a`，13:48:20 登录完成，
但原查询没有续办。13:49 的成功查询属于另一个新任务，不能作为原任务恢复证据。

`d5a1ce3` 将普通只读调用按工具调用编号隔离，解决同轮后一次查询复用已完成任务、被
`task_terminal` 拦截的问题。该规则必须保留，但它暴露了登录恢复对旧结果中间件的依赖：
原生工具返回路径只捕获可信卡片，未处理不带卡片的 `LOGIN_REQUIRED`。宿主不运行旧结果中间件时，
自动登录不会启动，模型另调登录工具会形成不同任务。中央账本只允许同任务恢复，因而无法找到原查询。

## 修复范围

- 原生工具返回 `LOGIN_REQUIRED` 时直接进入既有自动登录流程，带上失败查询的原任务编号。
- 原生代理传递实际调用参数；即使宿主未运行 `before_tool_call`，仍可建立准确的读取恢复上下文。
- 登录卡关联、轮询、协调租约、中央账本续办及去重沿用现有流程；不新增模型可见续办工具。
- 返回“宿主已打开登录卡”的结果，旧中间件随后执行也不会重复发起登录。
- 独立查询继续各自创建任务；中央只恢复同用户、同任务、同系统的失败读取，不自动重放业务写入。
- 包版本、宿主版本和中央 L3 兼容登记同步为 `0.4.96`。

## 验证

新增原生工具整链回归，分别覆盖旧钩子存在和缺失：独立待办查询失败、在原任务开启登录卡、
准确参数保留、完成后只调用一次中央续办、不唤醒模型心跳，随后新查询仍创建独立任务。
中央既有回归验证准确参数持久化、重启恢复、并发去重及时间线反馈。

- 新增两条用例在旧协调器代码上均失败，在修复后均通过。
- 完整 Python 回归：`974 passed, 5 skipped, 1057 subtests passed`；跳过项不计为通过。
- OpenClaw 插件：`204 passed`；依赖检查和打包检查通过。
- 运行治理与备份故障注入：21 项通过，无真实业务写入。
- 原始日志：`output/login-resume-validation-20260919.log`、`output/login-resume-governance-20260919.log`。
- Workspace 测试：33 项通过。
- 功能提交 `ca5326bae3b2` 已部署并推送 `origin/main`；中央、Workspace 和 Gateway 健康，
  插件 `0.4.96` 已加载、无版本漂移，发布工具目录 149 项检查通过。

## 真实验收

用户在退出 OA 后，从原 Workspace 发起待办查询并完成登录，随后确认验证完成，
并补测同一请求查询照明推广中心和运维中心。以下时间均为北京时间。

- 18:54:17 原任务 `cfa6811a-764d-4281-941d-f92999cb23c9` 查询返回 `LOGIN_REQUIRED`，
  参数为 `{"limit":50}`；18:54:19 登录交互关联到同一任务，没有另建登录任务。
- 18:55:06 登录完成，18:55:07 同一任务自动恢复一次待办读取，参数仍为 `{"limit":50}`。
- 18:55:11 读取成功，返回 4 条待办；时间线序号 `5460` 持久化一次 `login_read` 结果，
  原任务变为 `succeeded`。后台记录没有第二次恢复读取。
- 18:57:13 照明市场推广中心成员查询成功，返回 7 人，任务 `03a39ee2-23fc-47e5-a06b-40c4510e0236`。
- 18:57:22 智能照明运维中心成员查询成功，返回 2 人，任务 `b131c10f-4568-4f9e-980d-32381d5b5ed7`。
  两次均使用 OA 通讯录能力及独立任务，没有 `task_terminal` 或协调租约冲突。
- 如实保留异常：18:56:38 首次组织树查询返回 `CAPABILITY_EXECUTION_FAILED`，
  错误为 OA 页面未暴露部门树；随后组织树与成员查询成功。该次失败不计为通过，
  本轮未确定其根因，也不将其归为任务锁错误。

Chrome 扩展和 computer-use 在初始化阶段报 `failed to write kernel assets`，因此页面操作与
显示结果由用户验证，助手未独立截图复核；中央操作账本和时间线已实际回读核对。
证据位于忽略目录 `output/diagnostics/login-resume-live-20260919.json`，不提交业务明细。

按用户确认范围收尾：真实登录自动续办及同轮两个部门独立查询均完成验收。
