# 改进计划开工前核验

> 日期：2026-09-20
> 固定实验提交：`45044377fc6b54e236372e7d498ce7083324f165`
> 范围：M0 本地隔离实验与仓库备份调用链核对。未部署、未访问 OA、未执行业务写入。

## 结论

F01 的资源缺失和 F02 的非提交文件混入均已在隔离构建中复现。F04 的仓库自动备份入口仍只备份中央数据库；完整恢复方案需要覆盖其他状态及密钥依赖。F10 的发布脚本允许通过开关跳过验证，未检查绑定候选的既有验证清单。M0 本地核验完成；仓库外备份、外部 CI 和 GitHub 保护规则仍待运维侧核对，不将其记为不存在或验收通过。

本记录是风险核验结果，不是修复完成报告。M1、M2 尚未实施。

## 产物实验

使用 `git --git-dir=.gitrepo --work-tree=. archive` 从固定提交导出源码，不带入日常工作区的未提交内容。构建使用既有验证环境 Python 3.12.14，参数与现行部署一致：`pip wheel --no-deps --no-build-isolation`。另建不继承系统包的全新 venv，以 `pip install --no-deps --no-index` 安装干净 wheel；在源码树外的临时目录，以 `python -I` 执行资源加载探针。

这是两个纯标准库资源入口的安装测试，未安装项目运行依赖，不代表完整服务启动验收。

| 检查 | 实测结果 | 判定 |
| --- | --- | --- |
| 干净 wheel 内两个 `seeyon_page_scripts/*.js` | 均缺失 | F01 已复现 |
| 已安装 `ContinueSubmit` 加载入口 | `FileNotFoundError` | 缺少 `continue_submit.js` |
| 已安装 `SaveDraft` 加载入口 | `FileNotFoundError` | 缺少 `launch_save_draft.js` |
| 导入来源 | 新 venv 的 `Lib/site-packages/bscli/adapters/page_scripts.py` | 未从源码目录导入 |
| 安装包版本 | `0.1.0` | 来自安装后元数据，不代表插件版本 |
| 第二份独立导出源码加入 `bscli/m0_untracked_sentinel.py` 再构建 | wheel 收录该模块；干净 wheel 不含该模块 | F02 已复现 |

哨兵文件只加入第二份一次性导出目录，未修改生产源码或固定提交。该目录没有 Git 元数据；实验直接证明当前构建器会收录不属于指定提交的包内 Python 文件。结合发布脚本排除未跟踪文件的检查及从整个工作目录构建的调用链，确认风险成立；没有运行正式发布入口来做污染实验，也未解包线上已安装制品。

| 制品 | SHA-256 |
| --- | --- |
| 干净 wheel | `5869425e12079d22bcb3f50b1b41d2bd2843f7a765e75628f4aa70c82d446378` |
| 哨兵 wheel | `382c139ccc49dd64dd1e67b5074f21a6f8a6e6f6eaf414e96a1bce716a6cdf31` |

本地证据位于工作区忽略目录 `output/m0-4504437-20260920/`：包含源码归档、两份隔离源码、两个 wheel、新 venv、`probe.py` 和 `evidence.json`。JSON 保存制品完整文件清单、哈希、安装来源及两个加载错误。此目录未提交；上述哈希仅标识本次制品，不承诺重复构建的二进制哈希相同。

## 恢复资产与覆盖清单

以下路径相对服务运行 home（另有说明的除外）。分类为 M2 建议，尚未实施恢复。覆盖情况指仓库内 `agentbridge-backup.service → diagnostics backup-create → create_runtime_backup` 链路，不代表仓库外备份情况。

| 资产 | 位置与依据 | 当前链路覆盖 | M2 恢复要求与丢失影响 |
| --- | --- | --- | --- |
| 中央账本、身份及任务关系 | `agentbridge.db`；[中央服务](../../bscli/core/central_service.py)、[管理端](../../bscli/admin/application.py) | SQLite 在线备份整库；现有校验仅覆盖部分表和隔离断言 | 必须恢复；扩展关系核对，不能把整库复制成功视为所有业务语义通过 |
| 数据源目录及版本 | `database/catalog.sqlite3`；[Sources](../../bscli/database/sources.py) | 不覆盖 | 必须恢复；与授权及凭据版本匹配 |
| 数据源授权与审计 | `database/grants.sqlite3`；[IndependentDatabase](../../bscli/database/independent.py) | 不覆盖 | 必须恢复；缺失时不得默认放权 |
| 数据源凭据密文 | `database/credentials/`；[密文存储](../../bscli/core/data_source_secrets.py) | 不覆盖 | 必须恢复或重新受控配置；密钥不匹配时拒绝启用 |
| 报表状态及密文 | `database/reports.sqlite3`、`database/reports/`；[Reports](../../bscli/database/reports.py) | 不覆盖 | 明确保留策略；允许丢弃短期载荷时，状态必须收敛为不可交付，不能残留 ready/running 假象 |
| 登录会话密文与浏览器目录 | `session-secrets/`、`profiles/`；[中央服务](../../bscli/core/central_service.py)、[会话密文](../../bscli/core/session_secrets.py) | 文件不覆盖；数据库会话记录随主库保存 | 可选择受控恢复或要求重新登录；不能只恢复 active 记录就宣称会话可用 |
| 下载及时间线附件 | `document-downloads/`、`timeline-attachments/`；[下载](../../bscli/core/document_downloads.py)、[附件](../../bscli/core/timeline_attachments.py) | 文件不覆盖；元数据在主库 | 按短期交付策略恢复或失效；不得向用户展示无法取回的有效链接 |
| 系统配置 | `systems/*.json`；[ConfigStore](../../bscli/core/config.py) | 不覆盖 | 必须有受控恢复来源，保留端点与允许来源边界 |
| 解密密钥 | Linux 由 `AGENTBRIDGE_SESSION_KEY_FILE` 指定；Windows 使用 DPAPI | 不覆盖 | 单独保护恢复依赖；Linux 需要匹配的 32 字节密钥，Windows 不能假定跨用户/跨机器可解密 |
| 运行环境及宿主配置 | systemd、TLS 材料、环境文件、OpenClaw 配置及凭据引用 | 不覆盖 | 分清可由固定制品重建与需受控恢复的机密配置；不得把真实配置收入公开备份清单 |

依据：[备份实现](../../bscli/core/runtime_backup.py)、[备份服务](../../deploy/systemd/agentbridge-backup.service)、[CLI 入口](../../bscli/cli/main.py)、[运行治理手册](../部署运维/运行治理操作手册.md)。本轮只读源代码和配置模板，未读取真实密钥、凭据或业务数据库。

## 验证流程与外部保障

仓库已有 Python、插件和治理检查，不能描述为没有测试。但[验证脚本](../../scripts/Invoke-AgentBridgeValidation.ps1)仍采用可编辑安装；[发布入口](../../scripts/Publish-AgentBridge.ps1)的 `SkipValidation` 分支未核对同一候选制品的历史结果清单。本地未见 `.github` 目录，不能据此判断外部 CI、组织规则或 GitHub 分支保护不存在。本机未发现可直接使用的 `gh` 命令；本轮未读取 GitHub 管理配置。

待运维补证：仓库外备份的对象、周期、保留位置、密钥恢复责任与最近演练证据；外部 CI 的入口、强制条件和提交绑定方式。未核实前，M2 以明确覆盖上述缺口设计，不依赖假定存在的外部保障。

## 下一步准确范围

1. M1/F01：补齐页面脚本资源声明，并将空环境、源码目录外的安装入口测试接入现有验证流程；失败须阻断制品验收。
2. M1/F02：正式制品从指定提交隔离构建，保留工作区内容；哨兵反例必须不能进入正式制品。
3. M1/F10a：保存候选 SHA、构建环境、文件清单、制品哈希、验证范围与结果；复用验证时核对相同输入，失败和跳过分别记录。
4. M2/F04：先明确资产分类和密钥策略，再实现多库/文件的一致恢复点与隔离演练。短期文件可不备份，但恢复后必须确定失效；先测实际恢复耗时，再形成恢复目标。
5. F10b：外部保障事实核对后再设计候选晋升和强制检查；本轮不更改 GitHub 保护规则。

该次交付只有核验文档，按纯文档流程检查、提交和推送；不触发部署、服务重启或业务验收。后续 M1/M2 实施另按对应代码变更范围执行验证与发布。
