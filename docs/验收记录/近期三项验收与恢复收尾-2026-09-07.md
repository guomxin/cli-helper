# 近期三项验收与恢复收尾

## 范围与状态

覆盖通用 OA 批量、组合语义任务扩展验收、Gateway 冷启动与发布恢复。
不扩大业务权限、不操作第二用户待办、不修改 OpenClaw 源码及网络代理。
以下区分隔离测试、真实调用和未覆盖项；发布及最终回归结果在本文件补记。

## 批量与组合任务

- 隔离回归首轮 59 项通过、19 个子测试通过，覆盖五条混合批次、持久化恢复、冻结目标、
  结果未知停止、过期/拒绝、跨用户拒绝、计划取消与写入竞争、原始权限复核和历史查询完整性。
- 新增真实 1000 行与 1001 行分页边界测试：前者完整读取 20 页；后者到预算停止且明确标记
  不完整，不能据此生成“完整总结”或继续派生写入。这里使用模拟下游，不是扫描真实 OA。
- 第一用户普通 Chrome Workspace 实际发送“查看我的OA待办，只读，不处理。”，宿主运行
  `9a584c38-60f0-4f1f-9b10-ed9252a494a8` 完成 `oa_workflow_pending_list` 并返回三条待办；
  同期 Telegram 投递日志有成功记录。未直接查看手机端，不将日志等同于手机目视验收。
- 三条中有两条常规周报/效能事项，另一条为离职工作交接单。未为凑多于两条而处理高影响事项。
- Chrome 扩展可以发现并接管标签页，但随后页面读取反复出现 `Debugger unattached`。
  已尝试同一登录配置的新验收页及重新认领，未另开 OA 登录会话；后续真实页面连续卡片、
  组合派生写入及自然语言取消本轮未完成，不能标为通过。
- 双用户只读会话检查中第一用户有效，第二用户李世玉的 OA 会话非 active，隔离验收入口
  按前置条件停止。未获取其密码、代为登录或调用其待办；双活会话验收待其恢复登录后补齐。
- 独立中心账本只读诊断通过：两个用户、六个活动端点、十三类隔离关联检查均无违规，
  在途任务及宿主派发均为零。第一用户投递已确认；第二用户有 28 条历史 deferred、13 条
  failed，最早未完成投递为 8 月 30 日。未擅自重放或清理，不将其当作本轮新产生的问题。

## 启动与恢复修复

本次确认的冲突是：发布包装器 300 秒退出并释放租约时，guard 使用 180 秒宽限，
可能立即认定同一个慢启动进程已失效而再次替换。并非 SSH 的业务租约问题，不能据此归因于
Node 版本；五分钟启动耗时本身仍需阶段日志进一步区分模块加载、插件及就绪过程。

- 默认启动预算统一 600 秒，保留共享生命周期心跳保护。
- `StartOnly` 可以接续唯一且前台可见的在途启动，按原创建时间计时，不重置期限。
- 增加进程、监听、就绪三阶段日志与超时阶段说明，每 30 秒输出一次。
- 增加统一发布 `-ResumeAcceptance`：跳过已完成的部署，但重跑版本一致性及验收再推送。
- 增加启动期限负值、300 秒、599 秒、600 秒及自定义期限边界测试；保留孤儿租约与心跳测试。

## 结束条件

本地及真实运维验证完成后可以交付恢复修复；批量与组合任务的隔离验证不替代未完成的真实
页面验收。浏览器扩展恢复、第二用户登录恢复后，仅补上述缺项，不要求用户重跑全部已有功能。

## 回归与运维证据

- 完整验证入口通过：Python 873 项、4 项跳过、738 个子测试；Workspace 25 项；OpenClaw
  插件 182 项；打包检查与依赖一致性通过。插件未改动，版本继续为 `0.4.86`。
- 新增发布版本错误只读注入：期望 `000000000000`，服务器实际 `a059aefdae35`，按预期拒绝，
  没有部署、重启、推送或业务写入。
- Guard 已通过既有安装入口重载为 600 秒预算，旧 Guard 退出、新 Guard 唯一运行。
  冷启动期间实际观察到 `lifecycle_in_progress`、同一 Gateway PID 和新鲜共享心跳。
- 单次真实冷启动：23:02:51 创建前台宿主，23:03:57 就绪，约 66 秒；Gateway PID 37640
  全程未变，主要等待阶段为 `waiting_for_listener`。23:04 的本机恢复检查确认一个监听、
  可见前台、插件 `0.4.86` 已加载及原 SSH 隧道 connected，没有重建隧道或重放业务。
- 运行治理隔离故障注入 21 项通过，覆盖未知写结果、停滞任务/投递、身份隔离和备份篡改。
- 本轮未复现此前超过 300 秒的冷启动；已验证冲突修复与阶段定位能力，不宣称已经找出
  所有历史慢启动的底层性能原因。恢复发布以统一入口的版本及健康门禁为准。

## 9 月 7 日交付状态

代码提交 `6178608` 完成；最后文档、恢复边界与部署契约定向复验 42 项通过、544 个子测试通过。
本机 Guard 与前台 Gateway 修复已生效。服务器仍为 `a059aefdae35`，本轮尚未部署及推送。

统一发布入口首次被执行平台自动审核拒绝。随后核对了既有 remote、干净工作树及固定部署计划，
并取消跳过验证选项，仍被拒绝，理由为需要用户对具体 GitHub 目标及远程部署的直接确认。
没有拆分命令或改用其他通道绕过拒绝。故正确状态为“修复与本地回归完成，发布受执行审核阻塞”，
不是“三项全部验收完成”。

放行后按统一入口部署、验收及推送，再以相同 HEAD 执行 `-ResumeAcceptance`，核对部署未重复、
Gateway PID 未变及验收仍完整。当前只完成版本不匹配的真实拒绝验证，尚未完成该成功恢复路径实测。

剩余项按顺序保留：发布及成功恢复路径；Chrome 扩展恢复后的批量与组合任务页面连续操作；
李世玉重新登录后的双活会话隔离及微信实时投递。历史微信积压另行核对，不因本次验收自动重放。

## 9 月 8 日发布补记

用户再次明确允许向既有 `guomxin/cli-helper` 的 `main` 推送，并部署至
`10.10.50.213:/home/guomao/agentbridge`；同一项目、同一目标的后续发布沿用此授权。
执行平台权限约束仍须遵守，该授权不扩展业务写入或用户权限。

发布 `fa4480c4d4b6` 已安装成功，中心服务 active，144 项 MCP 目录、管理端、Workspace
就绪检查和备份检查均通过，近期服务错误数为零。GitHub 远端 `main` 已核对为完整提交
`fa4480c4d4b608d109c887d421598ccec06802fa`。

首次发布在 Gateway RPC 的 CLI 诊断处超时；同一提交恢复验收时 RPC 已通过，但插件
`plugins inspect` 又在 180 秒处超时。没有因诊断超时重启 Gateway。
独立本机健康验收确认 PID 37640、单监听、前台可见、readyz 正常、插件 0.4.86 已加载、
隧道 connected。最后使用手册已有的显式降级入口完成推送：

```powershell
.\scripts\Publish-AgentBridge.ps1 -ResumeAcceptance -SkipValidation -SkipOpenClawAcceptance
```

返回 `deploymentPerformed=false`、`pushed=true`，版本一致性、中心服务、页面及备份
门禁未跳过。此结果证明已部署后的发布恢复成功，不能声称 OpenClaw CLI 全项验收通过。
后续仍需补插件 CLI 诊断及上述浏览器、双用户缺项。本节替代上一节的“尚未部署及推送”结论，
原始审核与超时经过保留以便追溯。

## 9月8日双用户与页面补验

本轮服务器基线为 `5b40c78c0a7b`，插件仍为 `0.4.86`。通过 Chrome 扩展操作已登录的
Workspace，不另开 OA 登录，不重启 Gateway，不处理第二用户待办，也不提交业务写入。

### 已通过

1. 普通窗口第一用户新页显示 `xinguomao`；无痕窗口第二用户新页显示 `lishiyu`。
   无痕旧页无法控制，用户在原窗口新开页后恢复；详见[浏览器扩展连接故障处理](../部署运维/浏览器扩展连接故障处理.md)。
2. 两轮双用户只读隔离检查通过，`guomao`/辛国茂和 `lishiyu`/李世玉的 OA 会话均为
   `active`，均具备保活资格；两个用户、六个活动端点、十三类隔离检查均未发现串号。
3. 08:37 从第二用户无痕新页发送“检查我的OA登录状态，告诉我当前用户。只检查会话，不读取或处理待办。”，
   页面返回李世玉、`lishiyu`、`active`。宿主记录仅调用 `oa_session_status`，与页面答复一致。
4. 第二用户端点页只显示自己的 Workspace、Reference Host 和微信端点；没有第一用户端点。
   页面进行中任务为零。微信端点有效不代表本轮手机已收到消息，未将此项算作微信目视投递验收。
5. 第一用户 `2026-09-01` 已办查询返回两条，按处理日期下推，单页读取、完整覆盖。

### 新发现的未通过项

| 用例 | 实际结果 | 验收结论 |
| --- | --- | --- |
| 把2026年9月1日的OA已办和已发合在一起给我看，不写入任何系统。 | 已办成功，随后已发多次返回未获得任务协调租约；最终明确告知来源缺失 | 多来源完整读取未通过；不能因第一项任务显示已完成就判定整体通过 |
| 把2026年9月1日的OA已办和已发整理成今天0.5小时的工作日志，打开核对卡，先不要提交。 | 未调用持久计划入口；已发失败后仍调用普通日志 prepare，卡片预填两条已办与“已发未成功”的备注 | 语义规划及完整来源约束未通过；未提交字段、未授权、未写入日志 |
| 取消刚才的日志任务和填写卡，不提交日志。 | 模型把普通任务编号传给计划取消，返回 task plan not found；文字承认无法确认取消 | 自然语言取消未通过，不能用“忽略卡片”替代服务端取消 |

已定位的代码路径：普通业务工具在同轮共享任务；已办操作成功后该任务已为终态，
`agentbridge_host_coordinator_lease_acquire` 对终态返回 `ignored/task_terminal` 和空租约，
插件将空租约转为通用协调冲突。日志 prepare 又是独立任务入口，未进入持久计划。
这是当前证据指向的生命周期与规划衔接问题，不是第二用户串号、OA 登录失效或网络隧道故障。
后续应补针对性回归，核对同轮多工具任务生命周期、结构化规划引导及普通任务取消分流；
不能删除终态保护、强行接管租约或自动重放业务写入来消除提示。

### 测试清理与剩余边界

- 本次独立填写任务 `097db0ad-6ba2-4bd2-87e5-23cf9f6b2b13` 已通过中心既有存储接口定向取消；
  操作前校验用户、关联卡、创建日期、能力及 pending 状态，只失效化该测试卡，不触及其他任务。
- 页面复核：任务显示“已取消”，进行中数量归零；原填写页重新加载显示“字段填写卡已失效”，不再提供提交入口。
- 两轮查询与自然取消的宿主证据在本机会话 `d5cb3295-b874-44db-891b-26e9934d8c03`，
  第二用户会话检查在 `c842052a-272b-4e83-a1dc-25b84140c93d`；本记录不保存凭据或可信卡链接。
- 第二用户账本检查有 31 条 deferred、13 条 failed，较上一轮 deferred 增加 3 条，不能全部称为旧积压。
  当前微信最近业务活动仍为 8 月 22 日；本轮不重放通知，也不宣称微信实时交付通过。
- 本轮补齐了双活会话与两种窗口新页控制验证；组合任务发现上述缺陷，真实多条批量连续审批及
  OpenClaw 插件 CLI 完整诊断仍未补齐。近期三项不能标为全部验收通过，本节没有包含这些新缺陷的代码修复。
