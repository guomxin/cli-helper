# 工作台长请求结果恢复验收

## 问题与修复

生产 dispatch `4ad48cc9-ddc1-4569-84ce-f4a5e1dd5256` 在 11:53 被标记 failed / HOST_RUN_RESULT_UNAVAILABLE，但后台分析、导出随后成功，宿主同一 run 的历史记录也存在最终答复。原因是已接收请求的连接中断后只做有限几次历史查询，窗口结束就按失败终止了跟踪；这并不证明宿主执行失败。

已接收请求现在保持 accepted，按持久化 next_attempt_at 每 10 秒观察同一会话、同一 run 的历史结果。观察后释放租约，不重发 chat.send，不重复增加发送次数；中央重启后继续观察。恢复提示只写入一次，刷新页面仍可显示。收到最终答复后按原 run 的幂等消息键交付。

恢复期限从 accepted_at 起算 30 分钟，与接收前连接期限分开。期限内没有最终证据不标记失败；期限后使用 result_unknown，明确结果未确认、禁止自动重发。前端尊重显式 safeToRetry=false，即使没有收到工具进度也不提供安全重试。已接收请求遇到内部协调异常不会落入 failed_before_accept。

## 发布前验证

- Python Workspace 与文档检查 53 项通过。
- Workspace 前端和 Gateway Node 检查 32 项通过。
- 可控时钟重现超过原恢复窗口的长请求，覆盖持久化退避、中央重启恢复、同一请求再次订阅、最终消息仅一份、仅一次发送。
- 覆盖恢复超时结果未知、内部异常保持已接收、已接收请求不能作为未发送请求取消、刷新后恢复提示、显式禁止重发。
- 生产只读核验确认原故障 run 的 prompt 与 final 均存在。以上并不声称网络连接今后永不中断。

## 正式发布与真实页面复测

中央服务版本 d86c71830b50 已发布，Release 冒烟通过；OpenClaw 输入未变，进程 25652 保持运行。

使用 Chrome 原有 lishiyu 页面发起 WS-RECOVERY-0910：重新比较两个完整周本人 DAILY 数据，并导出第二周 CSV。dispatch 为 d4af8cef-bc05-420a-bca0-80349fbc3035，run 为 8423579e-f27c-4a01-aaba-d8f97484c39f。接收后通过页面导航刷新中断浏览器订阅，页面自动恢复同一请求的处理中状态，最终显示比较答复和有效文件卡。

中央只读回查：06:47:11.785Z 创建，06:48:51.013Z 完成，约 99 秒；state=completed，attempt_count=1，retry_count=0，last_error_code=null，host_accepted 和 host_dispatch_completed 均仅一次。文件引用 f2682f49f3e6465d871fafed46071032；结果为第一周 0 条/0 小时、第二周 1 条/4.5 小时，基期为零不计算变化率。

本次真实请求没有触发宿主侧连接中断，因此真实证据证明的是浏览器刷新接续、最终状态和单次交付。宿主侧断线后延迟超过原观察窗口、重启恢复和结果未知分支由上述可控故障注入测试覆盖；不将单次真实成功夸大为所有网络故障均已现场复现。

本地 Playwright 对实际静态资源做 1100px/390px 组件渲染验证：恢复提示保持单行状态结构、说明默认折叠，无横向溢出和 JS 错误，最终答复清理恢复状态。截图位于 output/playwright/workspace-recovery-20260910/。真实业务身份始终使用已有 Chrome 扩展登录态。
