# 独立备份节点验收

日期：2026-09-20。用户提供 10.10.50.209 作为独立备份机器，延续 M2 的外部副本建设。没有把用户提供的密码写入项目文件。操作规范见[独立备份节点手册](../部署运维/独立备份节点操作手册.md)。

## 实际结果

- 209 登录及到 213 的 SSH 连通性通过；根分区约 298 GB 可用。保留既有 Python 2.7.5 和 systemd 219，不升级系统。
- 209 专用拉取私钥保留本机；213 受限公钥只接受来自 209 的固定备份读取命令，既有 SSH 授权保留。
- 209 首次 systemd 服务执行结果 success、退出码 0，复制 1 代；再次执行复用 1 代、复制 0 代，未覆盖历史。
- 实际代次 `agentbridge-20260920T033258Z-2baeefba`，源应用 Release `01a7010d6512`。压缩包 43,452,204 字节，完整包及 7 个成员的 SHA-256、大小全部通过；包内不含应用解密密钥。
- 任意命令 `id -u`、越界路径、SFTP、远程端口转发共 4 项实际拒绝检查通过。
- 209 定时器已启用，每 30 分钟检查；首次启用后下一次计划为北京时间 13:30。成功记录在目标目录的 `last-success.json`，失败通过 systemd/journal 可见，没有声称已接入主动告警。
- 213 中央服务和 OpenClaw 均未重启，中央应用 Release 不变。仅部署独立备份运维组件。

## 自动验证与限制

本地 8 项测试、5 个子测试通过：复制与重复执行、源删除不删除副本、越权命令及未完成代次拒绝、源内容变化不覆盖历史、传输中断不发布、传输损坏拒绝、过期备份判失败、路径绑定、保留策略。209 真实 Python 2.7 编译和 systemd unit 校验通过。

收尾定向检查包含复制逻辑、文档及既有部署资产，共 34 项、842 子测试通过。定时器 enabled/active，目标配置和备份目录 0700，拉取私钥 0600。只修改独立运维组件和文档，未构建或重新部署中央应用 wheel。

目标路径 `/srv/agentbridge-backups/213/agentbridge-20260920T033258Z-2baeefba/`。包摘要为 `40999ed0534b692b93f767a5e7bc512ce169d185234be031afdfbb4dd8b4342f`，与 M2 正式演练源包一致。此次完成异机复制和完整性验证，未在 209 解密或启动应用；M2 源包的隔离恢复证据见[第二阶段验收](./可恢复基线第二阶段验收-2026-09-20.md)。

用户随后独立保管应用密钥，并提供本机 `D:\Codes\session.key` 完成隔离恢复验证。使用该副本对上述同一正式恢复包执行演练，`passed`、`credentialsDecryptable`、`sourceHashMatches`、`writeRejected` 均为 true，耗时 4.087 秒，业务写入 0，`activationAllowed=false`。报告为 213 的 `data/restore-drills/restore-eafi3yzc/restore-report.json`；远端临时密钥已删除，本机副本和原运行密钥保持不变。未在仓库保存密钥内容或密钥摘要。至此 M2 的独立副本与独立密钥可用性验证闭合。

地理位置及共同故障域尚未确认。209 root 仍有删除权，没有硬件或对象存储不可变保证。恢复点频率由每日源备份决定，复制检查不缩短源数据的备份间隔。
