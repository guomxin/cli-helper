# 可信制品第一阶段验收

日期：2026-09-20。对应[改进计划](../后续规划/代码与文档系统审查及改进计划-2026-09-19.md)的 M1（F01、F02、F10a）。

## 改动与实际收益

- F01：显式打包两个 OA 页面脚本；在新虚拟环境安装真实 wheel，并从源码目录外加载 ContinueSubmit 与 SaveDraft。避免源码测试通过、安装后却找不到资源。
- F02：从固定 Git 提交导出源码到独立目录构建。实测未跟踪的哨兵模块和旧 build 文件不进入产物；已跟踪文件未提交时拒绝正式构建与部署。
- F10a：完整验证记录候选提交、文件和依赖环境、各套件状态、Python 实际跳过项与制品摘要。部署复用已验证 wheel，服务器安装前检查 SHA-256，安装后再次验证资源加载。`SkipValidation` 仅复用匹配且完整的验证记录。

代码提交：`bf1cd3f55b8e81c3db104bb8f41a4c302bff2bf5`、`a7a8afe198c12457b7ecadcbf0fa6f12c162dc3d`。本轮部署版本为 `a7a8afe198c1`，后续验收文档提交不另行部署。

## 自动验证

| 检查 | 实际结果 |
| --- | --- |
| Python 全量 | 978 passed、5 skipped、1141 subtests passed |
| Workspace JavaScript | 33/33 通过 |
| OpenClaw 插件 JavaScript | 204/204 通过 |
| 运行治理故障注入 | 21/21 通过，真实业务写入 0 |
| Python 编译与依赖、插件打包检查 | 通过 |
| 制品隔离、缺资源拒绝、验证记录与制品不匹配拒绝 | 定向测试通过，包含实际构建与安装 |

5 个 Python 跳过项分别是 2 个须显式开启的 Chromium 控制台 UI 测试，以及 3 个仅适用于 POSIX 的密钥文件测试；没有计入通过数。完整验证记录实际保存测试名和原因。

本机证据位于 `output/release-validation/full.json`，候选清单位于 `output/release-validation/candidate-zcwgdbds/artifact/artifact.json`。wheel 为 `cli_helper-0.1.0-py3-none-any.whl`，含 168 个条目，SHA-256 为：

```text
5a8d084bb8d5e7d4c0f09771c5eb8731a250b2636ede7af4ca095d2c7add1bec
```

两个资源入口均从隔离环境的 site-packages 加载成功；服务器安装后复核同样通过。证据文件保留本机，不将完整环境信息上传公开仓库。

## 部署与 OpenClaw

固定发布入口完成部署：中央服务 active，发布版本匹配，管理端与 Workspace 就绪，149 项 MCP 工具目录检查通过，近期错误数为 0，备份定时器 active、最近备份结果 success。此检查不代表新增数据库恢复能力已完成，也没有执行 OA 业务查询或写入。

发布最初按 `inputs_unchanged` 保留原 Gateway；首次及续跑均在 `plugins inspect` 的 180 秒门槛超时。按现行手册允许的中央变更降级入口，使用 `-ResumeAcceptance -SkipValidation -SkipOpenClawAcceptance` 完成推送并读回核对 `origin/main` 为上述代码提交；未重复部署，OpenClaw CLI 当时仍为待补测。

用户随后明确要求必要时重启 OpenClaw。受控生命周期入口于 10:48 启动新 Gateway，旧 PID 34252 被停止，新 PID 为 28460；10:51 验证可见窗口、readyz 和当前进程插件注册均成功，插件版本为 0.4.96。

随后执行 `Publish-AgentBridge.ps1 -ResumeAcceptance -SkipValidation`：冷、热两轮模型预热通过，待预热标记清除；10:58 完整控制面验收通过，Gateway RPC 为 ok，CLI 与 Gateway 均为 2026.7.1，插件 loaded/0.4.96，versionDriftCount 为 0。固定入口再次核对推送成功，`deploymentPerformed=false`。此前的 CLI 待补测项已关闭，但不能仅凭重启后通过推断两次超时的根因。

## 边界与后续

验证记录是本机可信流程中的复用凭据，不是签名证明；不能防御恶意重写记录或手工篡改依赖文件，也不宣称字节级可复现构建。隔离安装探针证明两个资源入口可加载，不等同完整服务或 OA 写操作验收。

M1 代码、制品验证、部署及 OpenClaw 补验已完成。仓库侧强制检查 F10b、完整恢复 M2、发布切换与回退 M3 尚未实施。本轮继续使用既有共享虚拟环境部署方式。验收记录等纯文档收尾仅检查、提交与推送，不再次部署或重启。
