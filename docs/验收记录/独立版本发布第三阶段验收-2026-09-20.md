# 独立版本发布第三阶段验收（M3）

日期：2026-09-20。结论：M3 已实施、部署并完成在线验收。操作规则见[独立版本发布与回退](../部署运维/独立版本发布与回退.md)。

## 交付结果

- 发布前安装独立环境，旧服务继续运行；不再向运行中的共享 venv 执行 pip 安装。
- 暂停备份定时器、等待在途备份结束，再停服创建 M2 四库恢复点。恢复点失败不切换程序。
- 原子替换 `current` 指针，更新单元和版本标识，验证实际进程环境、Admin/Workspace 就绪及数据库结构，然后确认中央版本。
- 保留旧环境、旧单元、旧版本标识、制品、依赖清单和持久阶段记录。失败可按已审查的兼容契约恢复旧程序；不自动覆盖数据库或重放业务操作。
- 发布锁及未完成事务检查阻止并发或盲目重跑；中断后的受控恢复入口随版本保留。
- 大清单通过私有配置文件传递，避免 Linux 单参数长度限制；SSH 输出失败不改变已持久化的阶段判断。

## 验证与发布基线

应用发布提交 `ae2afbdf3ab288baeed5d39acc68f5bac86e2622`，Release `ae2afbdf3ab2`，前一 Release `01a7010d6512`。本次相对 M2 未修改 `bscli` 或依赖声明；独立安装会解析依赖，实际版本保存在部署记录中，不宣称全部传递依赖已锁定。

`Publish-AgentBridge.ps1 -PlanOnly` 显示 `restartOpenClaw=false`、`inputs_unchanged`、变更输入为空。随后固定发布入口完成全量验证、治理检查、部署、在线验收、推送及 GitHub 提交回读。

| 验证 | 结果 |
|---|---|
| Python 全量 | 1004 通过，1183 子测试通过，11 条件跳过；耗时 1029.59 秒 |
| Windows 跳过说明 | 既有 5 项及新增 6 项 POSIX 符号链接用例；新增用例已在 Linux 补验 |
| Linux 发布事务演练 | 12 项全部通过：安装/哈希/恢复点/健康失败、结构变化、旧指针恢复、中断拒绝、已确认重试、附属备份失败及输出断开 |
| Workspace | 33 项通过 |
| OpenClaw 插件 | 204 项通过，插件仍为 0.4.96 |
| 治理故障注入 | 既有固定检查通过，真实业务写入 0 |
| 制品 | 固定提交隔离构建、资源安装检查与远端同一 wheel 验证通过 |

wheel SHA-256：`d938159676a23879b679877af0eae7702d3f27f28a909bc2029a0d1ddb170637`。本地制品记录 `output/release-validation/candidate-8826h1px/artifact/artifact.json`；远端记录 `/home/guomao/agentbridge/releases/ae2afbdf3ab2/deployment.json`。

故障演练使用临时目录、真实 SQLite/符号链接及服务命令替身，没有故意停止生产服务来注入故障；生产执行了真实正常切换，没有实际执行生产回退。两类证据不混称。

## 真实切换与在线结果

`current` 指向 `/home/guomao/agentbridge/releases/ae2afbdf3ab2`。阶段完整经过 preparing、prepared、stopping、stopped、recovery_point_created、switching、checking、confirmed。

切换前恢复点 `20260920T065709Z-8f9d86c2` 一次生成通过，创建耗时 16.239 秒；包含四库、必要配置和凭据密文，密钥解密及关系验证通过。包摘要 `83ea4997b2e04a7d2996f1f47adee972bf9d6002cb975f9bbb3a7a2235e7ee0c`。从 stopped 到 confirmed 为 **45.671 秒**，从 switching 到 confirmed 为 26.266 秒。这是阶段测量，不是零停机或长期可用性指标。

在线验收于北京时间 15:02 完成：中央服务 active、PID `501183`，Admin/Workspace ready，149 个 MCP 工具的发布检查通过，近期服务错误 0，备份定时器 active、最近备份 success。OpenClaw RPC 正常，版本 2026.7.1，插件 loaded，版本漂移 0；Gateway PID 仍是 `28460`，启动时间仍为 10:48:41，本次没有重启。业务写入及业务列表读取均为 0；本次未另行执行双身份业务验收。

## 恢复与独立副本补验

新环境生成的发布后恢复点为 `20260920T065754Z-1fecaa1a`。使用 `current/venv` 对其做隔离恢复，报告 `data/restore-drills/restore-7xflbv46/restore-report.json`：passed、sourceHashMatches、credentialsDecryptable、writeRejected 均为 true，耗时 5.487 秒，业务写入 0，activationAllowed=false。原运行库不被恢复副本覆盖。

首次通过检查脚本补验时出现无结果退出。只读复现确认：历史清单较长，`sort | head` 在 pipefail 下偶发退出 141。改为 `sed -n '1p'` 消费完整列表后，固定检查脚本实际执行通过。此收尾只修改开发机诊断脚本，不改变 wheel 或服务单元，因此未再次部署或重启应用。

修正后最新清单选择连续 8 次通过；收尾定向验证 32 项及 851 子测试通过，6 项 POSIX 用例在 Windows 跳过，Linux 完整演练结果如上。

209 已有原 M2 代次及本次切换前、发布后两代新恢复点。15:01:59 UTC+8 的手工核验触发拉取检查，结果 success，复用 3 代、复制 0 代、删除 0 代，最新为上述发布后代次；说明定时拉取已先行完成复制。M2 独立密钥验证也已补入[独立备份验收](./独立备份节点验收-2026-09-20.md)。

## 边界

仅允许本次审查过的前一版本和 `no-migration` 契约；结构变化会停止自动回退，数据语义仍需人工审查。带数据迁移的发布需另行设计恢复方案。历史版本暂不自动删除；根目录旧 venv 仍供受限备份导出解释器和首次迁移回退使用，不得随意清理。地理故障域、不可变备份、仓库强制合并检查等后续事项未因本次发布而自动完成。

M3 完成后可进入 M4 跨组件任务生命周期契约与集成验证。本次未开始 M4。
