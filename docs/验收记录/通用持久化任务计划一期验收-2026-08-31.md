# 通用持久化任务计划一期验收 2026-08-31

## 一、验收范围

本次验收验证通用持久化任务计划的一期主链路，不执行真实业务写入。验收目标是确认：

1. OpenClaw 能识别“读取业务数据后形成另一项业务成果”的组合目标；
2. 模型先读取按身份和 Scope 过滤的计划目录，再提交一份结构化计划；
3. AgentBridge 能确定性校验、持久化和执行读取与转换步骤；
4. Workspace 展示同一计划的真实步骤进度和终态；
5. 目录未声明的用户要求不会被臆造成步骤参数；
6. 整个验收不调用业务提交、私有 commit 或 resume。

验收环境：

- 中央 Release：`67d4af8f5a32`；
- OpenClaw：`2026.7.1`；
- `agentbridge-interactions`：`0.4.70`；
- Workspace：`https://10.10.50.213:8783`；
- 身份：第二试点用户，仅具备本场景所需的 OA 读取权限，不具备泰华工作日志写权限。

## 二、真实验收中发现的问题

### 2.1 模型绕过持久计划

初次页面测试中，模型直接调用 OA 已办读取，再自行组织文字；涉及日志填写时又单独调用泰华
`prepare`。业务能力本身可用，但没有形成可恢复、可审计的整体计划。

OpenClaw `0.4.68` 增加通用组合目标识别：同时出现数据来源和后续成果时，先调用
`agentbridge_task_plan_catalog`，再通过 `agentbridge_task_plan_prepare` 提交一份计划，不在宿主
中写入 OA 到泰华的专用业务流程。

### 2.2 步骤字段不可发现

原 MCP Schema 把 `steps` 暴露为 `list[dict[str, Any]]`。模型只能看到“对象数组”，看不到
`stepKey`、`capabilityName`、`transformName`、`dependsOn` 和 `bindings`，因此连续猜测多组错误字段。

OpenClaw `0.4.69` 对应的中央接口改为强类型步骤：

- `capability` 步骤必须使用 `stepKey + kind + capabilityName`；
- `transform` 步骤必须使用 `stepKey + kind + transformName`；
- Binding 明确使用 `step + pointer`；
- 未知字段由 Schema 和中央校验共同拒绝；
- 计划目录按当前身份返回合法且 Scope 安全的示例。

### 2.3 动态参数被错误扩展

步骤字段明确后，模型第一次仍把用户所说的“3 小时”放进
`work_items_to_log_draft.v1.arguments.hours`。该转换的 `inputSchema` 没有 `hours`，中央服务安全拒绝，
模型第二次修正后成功。该过程没有业务副作用，但不满足“一次形成合法计划”的体验目标。

OpenClaw `0.4.70` 在三处统一约束：

- 计划目录说明 `arguments` 只能使用对应 `inputSchema.properties` 声明的字段；
- MCP 工具说明要求未被 Schema 接受的用户要求留在 `goal` 或最终说明；
- OpenClaw 组合任务上下文禁止从用户措辞臆造参数。

## 三、自动验证

- 全项目 Python：`740 passed, 3 skipped, 546 subtests passed`；
- OpenClaw 插件：`159 passed`；
- Agent Host 兼容性与 Reference Host 套件：通过；
- MCP 静态工具目录与当前服务 Schema：一致；
- 发布健康检查：AgentBridge、管理端、Workspace 和 OpenClaw 均正常，插件版本无漂移。

## 四、Workspace 真实验收

最终提示词：

```text
计划验收 PLAN-ARGS-0831：汇总2026年7月的OA已办，整理成3小时工作日志草稿，只预览，先让我核对，不要提交。
```

页面结果：

- Workspace 创建一张持久任务计划卡；
- 计划从“执行中 1/2”推进到“已完成 2/2”；
- 第一步 `read_done` 调用 `oa.workflow.done.list`；
- 第二步 `draft_log` 调用 `work_items_to_log_draft.v1`；
- 两个步骤状态均为 `succeeded`；
- 风险投影仅包含 `oa:read`，`writeSinkCount=0`；
- 终态原因为 `no_eligible_source_items`：本次日期范围内返回的事项经转换规则过滤后没有可形成
  日志草稿的有效事项。这是确定的业务结果，不是计划执行失败；
- 用户给出的“3 小时”保留在计划目标中，没有作为转换器不支持的参数提交。

OpenClaw 权威会话日志从该用户消息起统计：

| 项目 | 次数 |
| --- | ---: |
| `agentbridge_task_plan_catalog` | 1 |
| `agentbridge_task_plan_prepare` | 1 |
| `agentbridge_task_plan_get` | 0 |
| 模型直接调用 OA 列表工具 | 0 |
| 模型直接调用泰华 `prepare` | 0 |
| 工具错误 | 0 |
| commit 调用 | 0 |

## 五、验收结论

通用持久化任务计划一期的线上 Workspace 零写入主链路通过。宿主只负责提出受约束计划，中央服务
完成校验、持久化和执行；页面展示的是权威计划进度。最终一轮没有字段猜测、参数重试、直接业务
工具拼接或业务写入。

本记录不证明真实泰华日志已提交。完整“OA 已办 -> 草稿 -> 字段核对 -> 独立授权 -> 泰华提交
-> 权威回读”仍需在具备泰华写权限的身份下另行明确授权验收。

## 六、第一用户完整链路复验

本节为后续复验，不改变前述第二用户零写入记录。使用普通 Chrome 登录态，页面用户为
`xinguomao`，中央 `userSubject=guomao`，下游主体为辛国茂。第二用户无痕窗口未用于本节写入测试。

### 6.1 已完成的页面验收

| 场景 | 页面提示词或操作 | 核对结果 |
| --- | --- | --- |
| 读取与草稿 | 汇总2026年8月的OA已办事项，整理成工作日志草稿，只预览，不提交。 | 计划两步完成，读取结果35项、转换包含29项、排除6项；没有写入 |
| 精确预填 | 汇总2026年8月OA已办中的效能数据事项，预填今天0.5小时的泰华工作日志，先让我核对，不要提交。 | 4条来源事项，日期、工时、正文均已预填 |
| 字段阶段宿主重启 | 重启 Gateway 期间提交字段 | 重启后自动出现授权卡；同一计划和哈希、原读取及转换 Operation 未改变，各尝试一次 |
| 授权拒绝 | 查看授权卡内容后点击取消操作 | 交互取消；泰华当日日志前后均为0条。后台进一步核对发现持久计划漏收终态，修复情况见6.2 |

只读计划：`27af2f22-4910-42b7-8cf0-d23ec8a49a64`。
取消计划：`3d19989c-a638-48a5-bfb4-912fbaae491c`。
脱敏后台核对文件位于 `output/task-plan-acceptance/`，不保存凭据或完整来源正文。

### 6.2 复验发现与修正

1. 原网页把已提交的字段卡描述为“任务已经完成”，实际计划仍可能等待授权。现区分“字段已提交”、
   “认证已完成”与业务终态，不再把一个交互步骤完成当成整个业务完成。
2. 原宿主卡片轮询连续五次失败后退出，短时网络恢复后可能缺少续办触发。`0.4.71` 对可重试的只读
   状态查询故障在原有效期内每30秒退避重试，恢复后读取权威状态；不会因此重试业务提交。
   不可重试错误仍停止，超出原有效期也停止。
3. 第二条字段核对链路出现宿主进程内 `MCP_UNREACHABLE`，同期独立 MCP 检查和中心服务可用。
   新增脱敏 `transportCode` 以定位底层错误；尚不能仅凭该现象断言是代理、Node 版本或业务系统故障。
4. OA 当前读取源是页面已加载集合，不是全历史自动翻页；上表35项不等于证明完整月份无遗漏。
   本次核对的是实际返回集合到草稿的确定性转换。全量分页覆盖仍是读取能力的独立边界。
5. 取消后，宿主只查询并通知终态，不调用 resume；原计划收尾却仅写在 resume 路径，导致卡片
   已取消而计划仍等待。现在交互状态读取同步计划终态，中心恢复也只读对账等待中的交互；
   拒绝对应 `canceled`，过期、失败、替代对应明确失败。该对账不会消费授权或发起写入。

### 6.3 已授权真实提交与回读

用户单独明确允许以下一条日志，其他真实写入未获授权也未执行：

- 日期：2026-08-31；工时：0.5小时；不关联项目；
- 内容：【AgentBridge联调测试】验证OA已办效能数据归纳、用户核对及泰华日志提交回读。
  本条仅为系统验收测试，不作为实际工时统计。

计划 `4d8ec814-4de6-4140-a317-26a0fd1938d4` 在部署重启后恢复同一字段交互并生成授权，
原读取和转换 Operation 不变、各尝试一次；核对冻结字段一致后，点击一次“授权执行”。
内部写入 Operation 为 `47fe15f0-f7a9-44d7-ae3d-825a7e404414`，状态 `succeeded`；
`verification.method=GET /api/work-logs/range`，`matched=true`。

Workspace 计划显示3/3步完成。独立 `TaihuaMyLogs` 检查当日只有1条；从 Workspace 再读也显示同一
日期、0.5小时和测试正文。刷新原授权页显示“授权已使用”，没有再次授权按钮。

### 6.4 不同来源通用性复验

在同一第一用户页面发送：

```text
把照明概览、部门信息库列表和今天泰华日志的读取作为一个任务计划执行，只汇总不写入。
```

计划 `3c1eaf34-e61c-4055-8143-850b0b97a9e8` 完成三步：
`smartlight.system.overview`、`yuque.public_books.list`、`taihua.work_log.my.list`，各尝试一次。
三系统读取、同一父计划、页面3/3步终态均有实证，没有修改执行器或宿主的场景代码。
这证明不同来源组合可执行；不等于已实现“告警内容结合规程生成业务日志”的新注册转换。

### 6.5 验收边界

- 已真实验证第一用户完整单写终点、字段预填和修改、独立授权、权威回读、一次性授权、字段阶段
  与授权阶段的Gateway及中心重启恢复，以及多系统只读计划。
- 未主动注销用户当前有效登录态；登录中断及结果未知硬停本轮用自动测试覆盖，未伪造真实故障。
- 第二用户维持原只读隔离与权限测试范围，没有替其提交业务。
- 全历史分页、告警加规程的语义转换不在本轮已通过结论内。

### 6.6 修复回归

- Python 全量：`746 passed, 3 skipped, 554 subtests passed`；
- OpenClaw 插件：`162 passed`；
- 字段卡与认证卡终态文案：2项通过；
- 新增计划终态测试覆盖：只查询取消卡、不调用 resume 时计划收尾；拒绝、过期、失败和替代交互
  的重启对账；待确认或已授权卡的只读对账不能触发业务写入；重复终态查询不重复事件。
- Agent Host共享兼容性套件：OpenClaw `0.4.71` 与 Reference Host `0.1.0` 均通过。

### 6.7 授权阶段重启与最终收口

计划 `9f411cb8-6f6c-4d3c-9eb4-14d3d3f432cc` 先完成字段核对，内容明确写为
“AgentBridge授权阶段重启验收：只核对并取消，不应写入。”，随后停在独立授权卡。
重新部署中心服务并重启前台Gateway后，复开同一授权页：计划哈希、全部步骤Operation ID、
授权指纹和冻结字段均不变，各步骤仍只尝试一次。最后点击“取消操作”，没有执行第二条写入。

修复后真实结果：

- 原遗漏收尾的取消计划自动转为 `canceled / interaction_declined`；
- 新取消计划在网页上显示“跨系统计划已取消”，无需发“继续”；
- 6个第一用户验收计划中，4个成功、2个取消，等待计划0个；
- `taihua.work_log.create` 只有1个成功Operation，最终独立回读当天仍为1条日志；
- 刷新Workspace后历史卡片、用户输入和最终状态均保留，页面身份仍为 `xinguomao`；
- Release `e31887f7e3e4` 已部署到 `10.10.50.213`，发布验收通过，GitHub `main`已同步。

日志传输故障的恢复策略已验证，进程内早先 `MCP_UNREACHABLE` 的底层诱因没有在修复后复现，
不把“重启后恢复”当成底层根因已彻底确定。已增加不含凭据的传输错误码用于后续定位。

本节结论：第一用户真实业务闭环及恢复、取消验收通过；6.5列出的未覆盖边界仍保留。

## 七、后续自然请求的边界复盘

本节记录同日后续发现，不改写前述已经完成的具体计划样本。

用户发送两次“总结我昨天的OA已办和已发，时长为1小时，提交昨天的日志”：第一次在宿主受理前
等待约61秒后结束，没有业务调用；第二次创建目标日期2026-08-30、工时1小时的日志一次，
`verification.matched=true`。字段与授权交互均完成，任务终态成功。

但第二次任务 `e0f68094-51d8-498a-98c1-cde1b53a5551` 没有持久计划：插件关键词引导漏掉“总结”，
模型直接串联已办、已发和日志prepare。已发工具未接收日期参数，模型后续用日期作关键词；
已办与已发均只检查第一页50条。最终正文将“查到0条、完成检索汇总”当成业务工作记录。

尚无证据证明当天一定存在漏项；已发第一页最新日期为8月19日。问题在于查询完整性、结果到正文的
语义及计划入口不可靠，而不是提交重复或授权失效。

前述单来源计划、显式计划和不同系统只读组合的通过结论保留；**普通同义表达、多来源合并到同一
写入、完整日期查询以及空结果不编造正文，仍需新增验收**。后续工作见
[组合任务语义规划与执行约束设计](../架构设计/组合任务语义规划与执行约束设计.md)和
[工作台验收方案](../后续规划/组合任务语义规划工作台验收方案.md)，当前只有设计，尚未实施。
