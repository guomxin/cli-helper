# 组合任务提升与取消修复验收

日期：2026年9月8日。

## 范围与基线

本轮针对[近期三项验收与恢复收尾](./近期三项验收与恢复收尾-2026-09-07.md)发现的组合任务问题：第一来源成功后第二来源被终态任务租约阻挡、独立填写丢失本轮来源关联、普通任务编号误传给计划取消工具。

修改前发布基线为 `d0a233b65df3`，插件 `0.4.86`。修复候选插件为 `0.4.87`，中央工具目录新增 `agentbridge_task_cancel`，共145个工具。业务能力仍为102个。

本记录区分代码、离线验证、发布和真实页面验收，不把其中任意一项代替其他项。

## 修复内容

1. 规划检查提前到宿主任务创建和协调租约之前，原已完成任务仍保持终态，不放开终态租约。
2. 独立填写也检查当前用户原始轮次的业务来源；Workspace 的轮次关联由中心核实，不靠用户特定用词触发。
3. 原子来源读取后直接进入计划，以及收到 `PLAN_REQUIRED` 后升级计划，都可以使用新的稳定修复任务，避免卡在旧终态任务。
4. 同轮已要求持久计划后，禁止退回独立来源或写入入口绕过；新轮次和其他用户不受影响。
5. 已绑定端点建立任务上下文失败时，返回 `TASK_CONTEXT_UNAVAILABLE` 并停止，不再无任务继续业务调用。
6. 新增未提交任务取消入口，以真实任务编号路由持久计划或普通填写、授权卡。普通卡已提交或批准时保守拒绝，不替代下游业务撤销。
7. 若卡片失效后后台轮询先更新任务为 `superseded`，保留该终态并返回已完成取消操作，不因后续取消写入碰到终态而再次报失败。不允许吞掉不同任务或不同交互的状态冲突。

## 自动化证据

- 针对性中央服务与写边界测试：75项通过、11个子测试通过。
- 补充取消和轮询竞争后，写边界测试23项通过、8个子测试通过。
- MCP、文档、宿主合同、登录后续读及批量处理联合复测：87项通过、551个子测试通过。
- OpenClaw 插件全量：184项通过。
- Workspace 节点测试：25项通过。
- 插件打包检查：通过，使用工作区 npm 缓存。
- 运行治理故障注入：通过，无实际业务写入。
- 固定入口最终全量验证：通过，Python 880项通过、4项跳过、748个子测试通过；Workspace 25项和插件184项通过，打包检查通过，总耗时675.61秒。

本地证据保存在 `output/composed-repair-20260908-*.log`。首次全量使用系统 Python 时出现旧 pytest 临时目录访问拒绝；没有改旧目录权限或删除历史测试目录，改为工作区内新的专用临时目录复测。新工具清单及插件兼容版本相关的失败已修正并复测。

## 页面与发布状态

- Chrome 扩展能够识别两个用户的 Workspace 页面。
- 第一用户原页连接超时，新开同浏览器标签页后恢复，确认显示 `xinguomao`。
- 第二用户现有页面直接连接成功，确认显示 `lishiyu`，未处理其待办。
- 初次切换界面模式后，原 SSH 只读检查仍返回私钥读取拒绝。后续当前会话已支持受审核的非沙箱执行，使用同一密钥只读检查成功，服务器返回 `active`。没有修改密钥权限、复制私钥或修改服务器配置。
- 已提交修复 `212b68094ceb`，统一发布入口已将中央服务部署到同名 Release。Gateway 前台单实例重启成功，插件 `0.4.87` 已装载。
- 发布未全部通过，尚未推送：首次热预热140.353秒超过原60秒阈值；保持 Gateway 运行、不重启的独立原门禁复测仍为73.722秒，未修改阈值或绕过失败继续推送。
- 首次生命周期记录：11:07:05创建前台宿主，11:14:35就绪，约450秒；新 Gateway PID 7708全程未替换，终态为 `succeeded`、`visibleForeground=true`。
- 首次热预热的 CLI 子进程于11:18:55启动，Gateway于11:20:41受理，11:21:13正常结束。约106秒发生在受理前，实际 Gateway 运行约32秒；不能将140秒全部解释为模型执行，也不能仅据此归因为Node版本或新插件错误。
- 新第一用户页面一度恢复连接，显示 `xinguomao`，刷新时显示 Gateway 连接不可用；后续页面操作再次出现扩展调试连接丢失，新页创建也超时。已请求用户手工刷新普通窗口 Workspace；没有改用另一浏览器或绕过登录状态。
- 截至首次发布检查结束，尚未发出组合业务验收请求，未提交或取消真实日志。随后11:36的页面失败与补充修复单独记录如下；新修复的完整页面验收及发布门禁后的双用户检查仍未完成。

发布日志为 `output/composed-repair-20260908-publish.log`。PowerShell 异常由命令执行器直接返回，未全部进入重定向日志；两次热预热失败数值及状态在本节明确补记。Gateway 生命周期证据在本机 `AgentBridge/logs/openclaw-lifecycle.log`，当前应保留已经热起的唯一前台进程，不再次重启来试探。

## 待完成的生产页面验收

### 0.4.87 首轮页面复测及补充修复

11:36在第一用户Workspace发送原定只读合并请求，11:37收到旧数据摘要，未重新读取来源。宿主轨迹显示本轮没有业务工具调用，却注入了早上日志任务的接续快照。根因为提示钩子对宿主组装的整段历史识别接续意图，历史取消请求与任务编号误触发选择。

候选0.4.88将接续及跨端引用识别限定到已捕获且与宿主本次请求匹配的用户输入，历史保留为引用数据。新增测试覆盖新查询不继承历史任务、真实继续请求、错轮消息、另一用户及长输入；插件185项通过。此前0.4.87的页面失败不算通过，需用同一句原始提示词重新验收。

用户在无痕窗口新开Workspace后，刷新全局浏览器清单发现新页并成功连接，确认身份为 `lishiyu`、历史为第二用户自己的记录、进行中任务为0。此项仅为页面身份核对，不代表新版本业务隔离验收已经完成；没有处理第二用户待办。

随后点击任务列表又出现 `Debugger unattached`，再次读取和完整标签页身份连接均失败，未继续重复尝试；完整第二用户页面验收仍待恢复连接。候选0.4.88固定全量回归通过：Python 880项、753个子测试通过，4项跳过；Workspace 25项、插件185项及打包检查通过，耗时725.91秒。运行治理检查通过。证据为 `output/composed-repair-20260908-validation-r88.log` 和 `output/composed-repair-20260908-governance-r88.log`。

### 中午部署与再次复验

用户明确允许具体目标部署、前台Gateway重启及main推送后，完整发布入口正常执行，全量回归及治理检查通过，中央Release更新为 `b52647ce46cc`，Gateway进程3760于12:37:32就绪、前台可见，插件0.4.88已装载。本次热预热112.575秒超过当时60秒门槛，发布在推送前停止。会话记录中热请求12:43:13至12:43:18返回READY，表明总时间包含较大的CLI受理前开销。

随后用户明确要求因本机较慢放宽预热，默认总时间门槛改为180秒，模型热请求90秒超时及READY成功条件不变，补充计时口径及测试。此调整是用户要求的运行参数变更，不将之前失败改记为成功；须按新门槛实际复测。

12:45第一用户以原始只读提示词复验，确认不再接续旧日志任务；但模型收到PLAN_REQUIRED后将内部持久计划误解成业务写入，停止于已办2条、未读取已发。该用例仍不通过。补充中央规划策略及PLAN_REQUIRED说明：内部只读计划不向业务系统写入，不写入意图应使用无写入步骤的结果投影计划，业务写入仍需单独授权。未更改用户提示词或放开提交边界。

第二用户12:17从无痕页发起会话检查，后台只调用oa_session_status，返回lishiyu、李世玉、active、保活有效，无待办读取或处理；最终页面读取又遇扩展断连，尚未把后台成功扩大为页面显示验收通过。

180秒预热门槛与只读计划说明的定向回归通过：79项、542个子测试，证据 `output/composed-repair-20260908-policy-tests.log`。新门槛真实预热、后续发布和下列页面步骤仍分别核实。

### 剩余页面步骤

发布和版本核对完成后，通过 Chrome 扩展使用普通窗口第一用户依次发送：

1. “把2026年9月1日的OA已办和已发合在一起给我看，不写入任何系统。”
2. “把2026年9月1日的OA已办和已发整理成今天0.5小时的工作日志，打开核对卡，先不要提交。”
3. “取消刚才的日志任务和填写卡，不提交日志。”

核对两来源的日期下推及完整性、计划关联、卡片内容、自然取消和卡片失效。只到填写核对卡并取消，不提交日志。

第二用户只读确认自己的身份和任务列表，不处理待办，不观察或继续第一用户任务。真实跨端投递仍需独立证据，本轮不能把端点存在当作手机已收到消息。

浏览器操作遵循[浏览器扩展连接故障处理](../部署运维/浏览器扩展连接故障处理.md)：先直接控制，再尝试原浏览器同身份新页，仍失败则及时请用户协助；恢复后继续完成验收。
