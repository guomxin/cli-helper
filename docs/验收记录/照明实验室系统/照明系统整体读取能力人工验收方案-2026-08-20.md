# 照明系统整体读取能力人工验收方案

> 最近更新：2026 年 8 月 22 日
>
> 当前范围：照明系统读取一期至四期全部正式能力

## 一、这份方案解决什么问题

本方案供不熟悉照明实验室测试系统的验收人员使用。按照章节依次在 Agent Workspace 中发送
简短请求，再到照明系统网页的指定位置核对即可。

验收范围是当前已正式开放的一期至四期读取能力：

1. 登录状态与登录后自动续办；
2. 静态资产概览和实时运行概览；
3. 控制柜、RTU、灯杆的列表与详情；
4. RTU 和单灯的当前运行状态；
5. RTU 告警列表、备注和分析；
6. 单灯告警列表和分析；
7. 单灯告警和 RTU 支路漏电语义辨别；
8. RTU 巡测记录；
9. 巡检任务、每日进度和打卡记录；
10. RTU 用电记录、排行和趋势；
11. 单灯巡测历史；
12. 真实 RTU 支路漏电报警和分析；
13. 关灯时间段电流；
14. 巡检任务、每日进度和打卡记录；
15. 巡检日志统计和检修记录；
16. CSV 报告与文件交付；
17. 连续追问、页面刷新和历史消息恢复。

本轮全程只读。不要点击设备控制、告警处置、提交工区、撤回、备注保存、编辑、删除或批量设置。

## 二、怎样使用本方案

每个验收组都包含五类信息：

| 内容 | 用途 |
| --- | --- |
| 发送请求 | 可直接在 Workspace 中发送的自然语言，不需要工具名和内部编号 |
| 预计回答 | 智能体通常会展示的业务内容；实际数量随系统变化 |
| 数字和字段来源 | 说明数字究竟来自照明系统哪个接口，还是 AgentBridge 的有界汇总 |
| 网页核对步骤 | 告诉不熟悉系统的用户从哪个菜单进入、核对哪些列 |
| 通过标准 | 说明怎样算通过，哪些差异不能误判为失败 |

### 2.1 通用核对原则

1. 同时打开 Agent Workspace 和照明系统网页。Workspace 负责提问，照明网页只负责只读核对。
2. 收到 Workspace 回复后立即到网页查询，并记下两边的查询时间。实时状态和告警会变化，不能拿
   相隔很久的快照逐行比较。
3. “最近 7 天”按上海时区的自然日计算，包含开始日和结束日；在网页中手工填写相同日期。
4. 优先核对总数、设备编号、告警 ID、任务 ID、时间和状态。智能体会把字段整理成中文，文案不必
   与网页逐字相同。
5. `总数` 是照明系统分页结果中的 `totalCount/total`；`本次返回`只是当前页条数，两者不是一回事。
6. 资产页、地图页和实时状态页的数据范围不同。只有同一页面、同一筛选条件下的数字才应相等。
7. 页面显示 `--`、空白或暂无数据时，Workspace 也应如实显示为空或说明没有结果，不能从其他设备
   推断补齐。
8. 分析类回答最多分析 500 条。若回答出现“已截断”，排行和趋势只是有界样本，不能当作全量统计。
9. 用电量页面的 `--` 表示下游没有可用数值。Workspace 必须保留为空，不能当作 0，也不能猜测
   千瓦时等单位。
10. “单灯巡测”“RTU 巡测”“巡检任务”和“巡检日志”是四种不同数据，必须进入本方案指定的不同
    页面核对。
11. “RTU 支路漏电报警”已有正式专用读取能力。不能再用普通 RTU 告警或单灯告警替代，也不能把
    关灯时段存在电流直接判定为漏电。
12. 设备替换页面带有“开始执行”“失败重发”等设备动作，不属于读取能力；人工验收不得进入执行步骤。

### 2.2 接口名称怎么看

本方案列出接口只是为了说明数据血缘，人工验收时不需要调用接口。所有正式读取都由 AgentBridge
通过当前用户的照明系统会话执行。

## 三、开始前：登录与自动续办

### 3.1 发送请求

> 检查照明系统登录状态。

### 3.2 预计回答

- 已登录时：说明登录有效、当前主体和检查时间；不会弹登录卡。
- 未登录时：出现照明系统安全登录卡；完成登录后，原来的“检查登录状态”应自动继续并返回结果，
  不需要再问一遍。

### 3.3 网页核对步骤

1. 打开照明系统首页。
2. 看页面右上角或用户菜单中的当前用户名。
3. 与 Workspace 返回的已验证主体核对。

### 3.4 通过标准

- 登录主体正确，没有混入 OA、泰华或部门信息库身份；
- 登录卡成功后自动续办原请求；
- 登录失败时返回明确原因，不静默结束；
- 刷新 Workspace 后仍保持网页账号和任务历史。

## 四、静态资产概览

### 4.1 发送请求

> 看一下照明系统概览。

### 4.2 预计回答

回答应至少包含：

- 控制柜总数；
- 灯杆的可检索总数；
- 地图或单灯运行明细口径的灯杆总数；
- 控制柜状态分组；
- 两种灯杆口径不一致时的解释。

2026 年 8 月 20 日的一次真实结果如下，仅用于理解格式，不能作为以后固定答案：

| 项目 | 当次结果 | 正确解释 |
| --- | ---: | --- |
| 控制柜 | 36 | 地图综合控制柜接口当次返回了 36 个对象 |
| 控制柜状态 `unknown` | 36 | 这批地图对象没有可用状态字段，不表示 36 台都离线 |
| 可检索灯杆 | 131 | 灯杆资产检索列表的总记录数 |
| 地图明细灯杆 | 116 | 单灯运行或地图明细列表的总记录数，范围与资产列表不同 |

### 4.3 每个数字从哪里来

| Workspace 内容 | AgentBridge 字段 | 照明系统来源 | 取数方式 |
| --- | --- | --- | --- |
| 控制柜总数 | `cabinetTotal` | `/map/getIntegratedRControlCabinetDataByCondition` | 对返回的控制柜对象逐个计数 |
| 控制柜状态分组 | `cabinetStates` | 同上 | 读取 `onlineState/state/rtuState/alarmState`；都没有时记为 `unknown` |
| 可检索灯杆 | `lampPostTotal`、`lampPostCounts.searchable` | `/lLamppost/getDataByConditionForFacilityEx` | 使用分页结果的总记录数 |
| 地图明细灯杆 | `lampPostCounts.mapDetail` | `/lLamppost/getLampDetialList` | 使用单灯运行明细分页结果的总记录数 |
| 样例控制柜 | `sampleCabinets` | 地图综合控制柜接口 | 只展示前 10 条，不代表总数只有 10 |

### 4.4 网页核对步骤

1. 点击顶部 `设施资源管理`。
2. 在设施统计区域记录控制柜和灯杆数量；若首页没有直接显示，可分别进入控制柜管理和灯杆管理，
   查看表格底部“共 N 条”。
3. 进入 `设施资源管理` → `照明设施管理` → `灯杆管理`，清空筛选条件后记录总数，对照“可检索灯杆”。
4. 再进入 `单灯节能管理` → `单灯监控` → `运行状态`，清空筛选条件后记录总数，对照“地图明细灯杆”。
5. 两个灯杆数字不同时不要判失败；先确认比较的是上述两个不同入口。

### 4.5 通过标准

- 数量能在对应页面找到来源；
- 回答明确区分两种灯杆口径；
- `unknown` 被解释为“接口没有状态字段”，而不是“设备离线”；
- 没有拿 `131 - 116` 推断缺失、故障或离线灯杆。

## 五、实时运行概览

### 5.1 发送请求

> 照明系统现在整体运行怎么样？

### 5.2 预计回答

回答应包含 RTU、单灯控制器和灯具三组当前快照。例如 2026 年 8 月 20 日的一次真实结果是：

| 组别 | 当次结果 |
| --- | --- |
| RTU | 总数 29、在线 0、离线 27、停电 1、未启用 1 |
| 单灯控制器 | 总数 118、在线 1、离线 117 |
| 灯具 | 总数 178、开灯 1、关灯 0、告警灯杆 0 |

这些数字会变化。尤其不能因为当次 `开灯 + 关灯` 不等于 `灯具总数` 就自行补算，因为下游的灯具
登记总量、实时上报开关量和控制器在线范围不是同一统计集合。

### 5.3 每个数字从哪里来

| Workspace 内容 | 下游字段 | 接口 | 网页大致位置 |
| --- | --- | --- | --- |
| RTU 总数 | `rAllCount` | `/rRtu/getRtuStateCountDataByConditionNew` | `智能监控管理` → `常用列表` 上方统计卡 |
| RTU 在线 | `rOnlineCount` | 同上 | 在线统计卡 |
| RTU 离线 | `rNoOnlineWithNoHandleCount` | 同上 | 离线统计卡 |
| RTU 停电 | `rPowerOutageCount` | 同上 | 电源停电统计卡 |
| RTU 未启用 | `rDisableCount` | 同上 | 未启用统计卡 |
| 在线有电 | `rOnlineWithEleCount` | 同上 | RTU 运行指标中的细分状态 |
| 在线无电 | `rOnlineWithOutEleCount` | 同上 | RTU 运行指标中的细分状态 |
| 未计算 | `rOnlineNoCalCount` | 同上 | 下游未完成状态计算的数量 |
| 单灯控制器总数 | `AllControlCount` | `/lLamppost/newGetTotalStatus` | `单灯节能管理` → `单灯监控` → `运行状态` |
| 控制器在线/离线 | `OnlineCount` / `OfflineCount` | 同上 | 页面上方控制器状态统计 |
| 灯具总数 | `AlonelampCount` | 同上 | 灯具统计 |
| 开灯/关灯 | `OpenLampCount` / `CloseLampCount` | 同上 | 当前开关状态统计 |
| 告警灯杆 | `alarmLampPostCount` | 同上 | 单灯告警状态统计 |
| 观察时间 | `observedAt` | AgentBridge | 请求完成时的服务器时间，不是设备最后采集时间 |

### 5.4 网页核对步骤

1. 进入 `智能监控管理` → `常用列表`。
2. 抄下页面上方 RTU 总数、在线、离线、电源停电和未启用数量。
3. 进入 `单灯节能管理` → `单灯监控` → `运行状态`。
4. 抄下单灯控制器总数、在线、离线、灯具总数、亮灯、灭灯和告警灯杆数量。
5. 记录核对时刻。若相差 1 至数条，刷新一次 Workspace 或网页后再比，先排除实时变化。

### 5.5 通过标准

- 每个数字与对应页面统计卡一致；
- 回答注明观察时间和上海时区；
- 不把观察时间说成设备采集时间；
- 不使用减法编造下游没有返回的状态数。

## 六、资产列表和详情

### 6.1 发送请求

依次发送：

> 查找照明系统里的 RTU，先列 5 条。

> 查看上面第 1 台 RTU 的详情。

> 查找照明系统里的控制柜，先列 5 条。

> 查看上面第 1 个控制柜的详情。

> 查找照明系统里的灯杆，先列 5 条。

> 查看上面第 1 个灯杆的详情。

### 6.2 预计回答与字段来源

| 资产 | 列表预计字段 | 详情增加的字段 | 照明系统来源 |
| --- | --- | --- | --- |
| RTU | ID、编号、名称、型号、类型、控制柜、分组、工区、状态 | 安装日期、出厂日期、质保、厂家、最后在线、备注、继电器和回路 | `/rRtu/getDataByCondition`；继电器来自 `/rRturelay/getDataByCondition` |
| 控制柜 | ID、编号、名称、道路、路侧、工区、地址、容量、电气类型 | 变压器类型、电表号、组织、经纬度、创建时间、备注 | `/rControlCabinet/getDataByCondition` |
| 灯杆 | ID、编号、名称、道路、路侧、控制柜、工区、灯具数 | 类型、高度、材质、RTU、变压器、地址、经纬度、创建时间、备注 | 列表 `/lLamppost/getDataByConditionForFacilityEx`；详情 `/lLamppost/getLampPostDetail` |

列表中的 `总数` 取分页接口的总记录数；“先列 5 条”只限制展示条数，不改变总数。RTU 详情中的
`继电器总数` 来自继电器接口的总数，`回路数` 是 AgentBridge 对每个继电器下的开关和回路结构计数。

### 6.3 网页核对步骤

1. RTU：`设施资源管理` → `监控设备管理` → `RTU管理`。在 RTU 编号或名称中输入 Workspace
   返回的值，核对名称、型号、类型、控制柜、分组、安装日期和出厂日期。
2. 控制柜：`设施资源管理` → `照明设施管理` → `控制箱管理`。按名称搜索，核对编号、容量、道路、
   路侧、工区和地址。
3. 灯杆：`设施资源管理` → `照明设施管理` → `灯杆管理`。在灯杆编号中搜索，核对灯杆类型、高度、
   灯源数量、道路、路侧和控制柜。
4. 详情核对只点击目标行的查看入口。不要点击编辑、删除、批量设置。
5. RTU 的继电器和回路如果在详情页分标签展示，要逐个展开核对；页面没有数据时回答也应为空。

### 6.4 通过标准

- 三种资产没有混淆；
- 列表的总数、前 5 条稳定编号与网页一致；
- “上面第 1 个”能沿用当前任务上下文，不要求提供内部 `asset_id`；
- 详情来自同一资产，空字段不从相邻设备推断；
- RTU 继电器和回路结构与网页一致。

## 七、RTU 当前运行状态

### 7.1 发送请求

> 列出前 10 台离线 RTU。

> 再看看停电的 RTU。

> 查一台 RTU 的当前状态，带上三相电压和电流。

### 7.2 预计回答

每台 RTU 可能包含：编号、名称、型号、控制柜、工区、分组、道路、状态、最后在线时间、最后巡测
时间、是否告警、是否启用、工作模式、继电器编号，以及三相电压、三相电流、温湿度和漏电电流。

### 7.3 数字和字段来源

| 内容 | 来源与口径 |
| --- | --- |
| 总数 | `/rRtu/getRtusByConditionNew` 对当前状态筛选返回的分页总数 |
| 本次返回 | 当前页实际展示的 RTU 条数，最多为请求中的 10 条 |
| 在线/离线/停电/未启用 | 下游状态筛选和记录中的 `rtuRunningState`，不是 AgentBridge 自行判断网络 |
| 三相数据 | 记录内 `coplogPhase` 的 Ua/Ub/Uc、Ia/Ib/Ic |
| 是否告警 | `isAlarm` 或告警列表是否非空 |
| 最后在线/巡测时间 | `lastOnlineTime`、`coplogTime/lastCoplogTime` |
| 截断 | 需要本地组合筛选且命中数超过单次扫描上限时才为真 |

### 7.4 网页核对步骤

1. 进入 `智能监控管理` → `常用列表`。
2. 点击“离线”状态卡，核对总数和前 10 台的编号、名称。
3. 再点击“电源停电”状态卡，核对第二条回答。
4. 清除状态筛选，按第三条返回的 RTU 编号搜索。
5. 核对状态、巡测时间、三相电压、三相电流、控制柜、分组和告警标记。

### 7.5 通过标准

- 离线和停电是两个下游状态，不互相替代；
- 总数与页面筛选总数一致，展示条数不超过 10；
- 页面为 `--` 的电气数据没有被编造；
- 无结果时明确说未命中，不切换到静态资产列表凑数。

## 八、单灯当前运行状态

### 8.1 发送请求

> 列出前 10 个离线的单灯控制器。

> 看看当前开灯的单灯，先列 10 条。

> 看看当前有告警的单灯。

### 8.2 预计回答与字段来源

外层每条记录是一根灯杆，可能包含灯杆编号、道路、路侧、工区、控制柜、RTU、控制器状态、最后
活动时间、灯具数量和是否告警。灯杆内每盏灯可能包含灯号、用途、开关状态、电压、电流、功率因数、
有功功率、调光值、能耗和漏电电流。

所有记录来自 `/lLamppost/getLampDetialList`：

- `总数` 是当前筛选条件下的分页总数；
- `控制器在线` 是灯杆下真实灯具控制器状态的归纳；
- 任一灯具存在告警时，灯杆记为有告警；
- 任一灯具开灯时灯杆状态为开灯；全部可识别灯具关灯时为关灯；都没有状态时为未知；
- 组合筛选需要有界读取后本地过滤，回答会显示 `downstreamTotal` 和是否截断。

### 8.3 网页核对步骤

1. 进入 `单灯节能管理` → `单灯监控` → `运行状态`。
2. 先选择控制器“离线”，核对总数和前 10 根灯杆。
3. 再选择“亮灯”，核对第二条结果。
4. 选择告警或异常条件，核对第三条结果。
5. 展开一根灯杆，核对每盏灯的开关、电压、电流、功率因数、有功功率和调光值。

### 8.4 通过标准

- 灯杆与灯具的层次清楚，不把 10 根灯杆说成 10 盏灯；
- 离线、开灯和告警筛选与网页一致；
- 电气值只在下游真实返回时展示；
- 组合筛选若截断，回答明确说明，不能声称全量。

## 九、RTU 告警列表和当前备注

### 9.1 发送请求

> 列出最近 5 条 RTU 告警，按发生时间排。

> 告诉我上面第 1 条告警的当前备注。

> 再列最近活动的 5 条 RTU 告警。

### 9.2 预计回答

告警列表应包含告警 ID、首次发生时间、最近活动时间、设备、设备编号、告警类型、级别、状态、内容、
工区、工区提交状态和分组。第一条和第三条可能不同：

- 普通“最近告警”按首次发生时间 `occurredAt` 全局倒序；
- 明确说“最近活动”才按 `lastActivityAt` 全局倒序；
- 最新时间存在多条并列告警时，应说明并列组，不静默选择一个作为写操作目标。

### 9.3 数字和字段来源

| 内容 | 来源与口径 |
| --- | --- |
| 告警总数和列表 | `/rHisHitchAlarm/getDataByRtuAlarm` 的 `RtuHisHitchAlarm` 分页结果 |
| 今日告警、未处理、昨日告警 | 同一接口顶层的 `todayAlarm/untreated/yesterdayAlarm` |
| 上述三个汇总 | 当前系统快照，不受当前页、关键词或日期筛选约束，不能与列表总数直接相减 |
| 首次发生时间 | `occurDate` 等下游首次时间字段 |
| 最近活动时间 | `lastDate/lastTime`；没有时才回退到首次时间 |
| 当前备注 | `/rHisHitchAlarm/getRtuAlarmRemark` 按精确告警 ID 权威读取 |

### 9.4 网页核对步骤

1. 进入 `智能监控管理` → `查询统计` → `RTU报警记录`。
2. 第一条保持默认发生时间排序，核对前 5 条告警 ID、RTU、类型和首次发生时间。
3. 查看页面是否存在相同首次时间的并列记录，核对 Workspace 没有漏掉并列说明。
4. 在第一条目标记录的“备注”列或只读详情中核对当前备注，不点击保存。
5. 将网页排序口径改为末次发生或最近活动，再核对第三条。

### 9.5 通过标准

- 两种时间语义没有混淆；
- 页面总数、前 5 条标识和时间一致；
- 状态显示业务文字，不只返回数字代码；
- 备注读取不弹字段卡或授权卡，也不修改备注；
- 当前快照汇总没有被解释为当前筛选结果。

## 十、RTU 告警分析

### 10.1 发送请求

> 分析最近 30 天的 RTU 告警。

> 分析最近 7 天当前未解除的 RTU 告警。

### 10.2 预计回答

回答通常包含：日期范围、下游总数、取回条数、实际分析条数、是否截断、状态分布、告警类型排行、
设备排行、每日趋势和最近记录。

### 10.3 每个数字从哪里来

| 数字 | 含义 |
| --- | --- |
| `downstreamTotal` | 照明告警接口在日期和下游状态筛选后的总记录数 |
| `retrievedCount` | AgentBridge 实际从分页接口取回的原始条数，最多 500 |
| `analyzedCount` | 取回后再应用告警类型等本地筛选的条数 |
| `truncated` | `downstreamTotal > retrievedCount`，表示只分析了前 500 条以内的样本 |
| 状态、类型、设备排行 | AgentBridge 对 `analyzedCount` 条记录分组计数 |
| 每日趋势 | 按所选时间字段把分析记录按日期计数 |
| `snapshot` | 接口同时返回的当前系统快照，不是最近 30 天的分组结果 |

### 10.4 网页核对步骤

1. 在 `RTU报警记录` 页面填写相同开始日和结束日。
2. 第二条再选择当前告警或未解除状态。
3. 记录页面总数并导出页面结果，按告警类型、RTU 和日期做简单透视统计。
4. 若 Workspace 显示未截断，排行计数应能与全量导出核对；若显示截断，只核对已分析样本的前几条
   和范围说明，不能要求排行等于全量。

### 10.5 通过标准

- 日期范围和状态筛选正确；
- 三个条数和截断关系解释清楚；
- 趋势、类型和设备排行能从分析样本复核；
- 不把分析结果说成照明网页原生统计卡。

## 十一、单灯告警列表和分析

### 11.1 发送请求

> 列出最近 7 天的单灯告警，先看 10 条。

> 分析最近 30 天的单灯告警。

> 异常亮灯有几条？

### 11.2 预计回答与数字来源

单灯告警来自 `/lHisHitchAlarm/getDataByCondition`，字段包含告警 ID、首次发生时间、最近活动时间、
灯杆、灯具用途、道路、路侧、控制柜、工区、告警类型、状态和漏电读数。

列表和分析会先有界读取最多 500 条，再按告警类型、道路、工区或控制柜筛选：

| 数字 | 含义 |
| --- | --- |
| 下游总数 | 相同日期和告警状态下照明接口返回的总数 |
| 取回条数 | 实际取回的原始记录，最多 500 |
| 列表总数或分析条数 | 在取回记录上再应用文字筛选后的命中数 |
| 类型、灯杆、道路、状态排行 | AgentBridge 对命中记录分组计数 |
| 每日趋势 | 按最近活动时间逐日计数 |

### 11.3 网页核对步骤

1. 进入 `单灯节能管理` → `查询统计` → `单灯报警记录`。
2. 设置相同报警日期，核对前 10 条灯杆、道路、灯具用途、告警类型、状态和时间。
3. “异常亮灯有几条”要在告警类型中选择“异常亮灯”后搜索。
4. 分析结果可用页面导出做分组核对；若 Workspace 显示截断，只比较有界样本。

### 11.4 通过标准

- 明确称为单灯告警，不笼统叫漏电；
- 总数、取回数、分析数和截断解释正确；
- “异常亮灯”只在单灯告警页核对；
- 普通单灯告警不会被改写为真实漏电告警。

## 十二、RTU 支路漏电报警与分析

读取四期已经提供真实的 RTU 支路漏电专用能力。本章取代旧版“在普通 RTU 告警中寻找漏电”
的临时办法。以后验收普通“漏电”请求，应优先在本章指定的支路漏电页面核对。

### 12.1 发送请求

依次发送：

> 最近 30 天有支路漏电报警吗？

> 哪些设备和支路最集中？

> 看看五一路东最近一天的支路漏电报警。

> 只看当前还没有解除的支路漏电报警。

`五一路东` 是现有真实环境中曾经唯一命中的 RTU 名称，只用于说明操作方式。以后该设备不存在时，
可以从第七章的 RTU 列表选择一台当前存在的设备替换，但必须在 Workspace 和网页使用同一个名称。

### 12.2 预计回答

列表回答应说明查询日期、设备条件、报警状态、下游总数、返回条数和观察时间。命中记录时，每条
至少应尽量包含：

- 告警 ID；
- RTU 编号、名称和控制柜；
- 支路名称或支路标识；
- 漏电电流及其单位；
- 首次发生时间、最近活动时间和解除时间；
- 当前/已解除状态；
- 告警内容、备注和工区提交状态。

第二条分析应基于第一条相同的 30 天范围，给出按日趋势、RTU 排行、控制柜排行、支路排行、当前
未解除数量和最近事件，同时明确 `downstreamTotal`、`analyzedCount` 与 `truncated`。没有记录时，
应明确返回“该范围内为 0 条”，排行保持为空，不能自动扩大日期或改查普通告警。

2026 年 8 月 21 日真实验收时，最近 30 天支路漏电列表和分析均为 0 条、未截断。这只是当次快照，
用于说明“真实空结果也算通过”，不能作为以后固定答案。

### 12.3 每个字段和数字从哪里来

| Workspace 内容 | AgentBridge 字段 | 照明系统来源或口径 |
| --- | --- | --- |
| 列表总数 | `downstreamTotal` | `/rHisHitchAlarm/getDataByRtuLeakageAlarm` 在相同日期、设备和状态条件下的分页总数 |
| 本次返回 | `returnedCount` | 当前页实际标准化后的记录数 |
| 告警类型 | `alarmType` | 下游真实支路漏电告警类型 `htch047`，不能换成普通单灯告警 |
| RTU 和控制柜 | `rtuCode`、`rtuName`、`cabinet` | 告警记录中的设备归属字段 |
| 支路 | `branchId`、`branch` | 告警记录中的继电器、道路或支路字段 |
| 漏电电流 | `leakageCurrent` | 下游明确返回的漏电电流；缺失时保持为空 |
| 电流单位 | `leakageUnit` | 下游返回的单位；没有单位时不得猜测为 A 或 mA |
| 阈值 | `threshold` | 仅在下游记录明确返回时展示，不由 AgentBridge 推断 |
| 首次和最近时间 | `occurredAt`、`lastActivityAt` | 下游首次发生和最近活动时间 |
| 状态 | `stateLabel` | 页面状态 131/132 标准化后的当前或已解除业务文字 |
| 当前未解除数 | 分析结果状态分组 | 仅统计本次已分析的当前状态记录 |
| 设备、支路和每日排行 | 分析结果 | AgentBridge 对最多 500 条有界记录分组，不是网页原生统计卡 |

### 12.4 网页核对步骤

1. 进入 `漏电管理` → `RTU漏电查询` → `RTU支路漏电报警记录`。
2. 第一条将开始日期设为今天往前 29 天，结束日期设为今天，报警状态选“全部”。记录页面“共 N 条”。
3. 核对前几条记录的 RTU、控制柜、支路、漏电电流、时间和状态。页面没有电流单位时，Workspace
   也只能展示原值并说明单位缺失。
4. 第二条保持相同日期和状态。页面若支持导出，可导出后按 RTU、控制柜、支路和日期做透视统计；
   页面不支持导出时，至少核对总数和排行中前几项确实来自当前列表。
5. 第三条在设备名称或编号条件中输入 Workspace 使用的 RTU，日期改为最近一天，确认页面没有
   混入其他设备。
6. 第四条将状态改为“当前”或“未解除”，核对总数和记录状态。
7. 可到 `支路漏电统计` 或 `漏电电流曲线`查看同设备的辅助趋势，但这些页面与报警记录的统计口径
   可能不同，不能要求数字逐项相等。

### 12.5 通过标准

- 普通“漏电”请求进入 RTU 支路漏电来源，不落到旧单灯告警兼容入口；
- 日期、设备和当前/已解除筛选与网页一致；
- 总数、分析数和截断关系解释正确；
- 电流值、单位和阈值只在下游真实返回时展示；
- 空结果保持为空，不扩大范围凑数；
- 没有把异常亮灯、控制器离线、普通回路电流或关灯时段电流直接称为支路漏电报警。

## 十三、RTU 巡测记录

### 13.1 发送请求

先从第七章选一台名称明确的 RTU，再依次发送：

> 查看这台 RTU 最近 24 小时的巡测记录。

> 只看其中的异常记录。

> 查看这台 RTU 最近 10 天的巡测记录。

### 13.2 预计回答

- RTU 名称命中一台时返回记录；命中多台时先返回候选，不混合查询。
- 默认查询最近 24 小时。
- 每条记录可能包含接收时间、RTU 自身时间、三相电压、三相电流、电流比、功率因数、温湿度、
  继电器电流、回路电流、漏电电流、开关继电器、状态和告警内容。
- 最近 10 天超过单次 7 天上限，应返回范围错误并要求缩短，不执行无界扫描。

### 13.3 数字和字段来源

| 内容 | 来源与口径 |
| --- | --- |
| RTU 候选 | `/rRtu/getRtusByConditionNew` 按名称或编号搜索 |
| 巡测总数和记录 | `/rHisCoplogPhase/getDataByCondition` |
| 总数 | 当前 RTU、时间范围和异常筛选下的分页总数 |
| 接收时间 | `addDateTime/copDate`，是平台收到记录的时间 |
| RTU 时间 | `rtuDateTime/rtuTime`，是设备自身时间 |
| 三相电压、电流 | Ua/Ub/Uc 和 Ia/Ib/Ic 原始字段 |
| 漏电电流 | `LeakCurrents/relayLeakCurrents`，没有返回时保持为空 |
| 异常状态 | 下游 `isSucceeded/state`，不由 AgentBridge 用阈值重算 |

### 13.4 网页核对步骤

1. 进入 `智能监控管理` → `查询统计` → `RTU巡测记录`。
2. 输入 Workspace 锁定的 RTU 编号或名称。
3. 设置相同开始和结束时间；第二条再勾选“仅显示异常”。
4. 对照总数、巡测时间、RTU 时间、Ua/Ub/Uc、IA/IB/IC、状态、告警内容、温度和湿度。
5. 用“显示列”打开漏电、回路或功率因数列。页面没有有效值时 Workspace 也应为空。
6. 第三条不用在网页执行 10 天扫描；AgentBridge 拒绝超过 7 天是预期保护。

### 13.5 通过标准

- 只查询指定 RTU；
- 24 小时和异常筛选正确；
- 平台接收时间与 RTU 自身时间没有混为一个字段；
- 10 天请求被明确拒绝；
- 巡测记录没有被称为巡检任务。

## 十四、RTU 用电记录、排行和趋势

### 14.1 发送请求

在同一个任务中依次发送：

> 看看最近 7 天的 RTU 用电量。

> 哪几台用电最多？

> 看看五一路东最近 30 天的用电趋势。

> 查一下最近两年的 RTU 用电明细。

第四条是边界测试，不是要求系统真的扫描两年明细。

### 14.2 预计回答

第一条应给出日期范围、设备总数、每台 RTU 的编号、名称、逐日用电原值、单位、缺失点数量和数据
质量提示。第二条沿用第一条的 7 天范围：只有存在数值型用电数据时才给出总用电量和设备排行；
数据均为 `--` 时，应明确说当前无法形成排行，不能把 `--` 当成 0。

第三条应先按设备名称消歧。唯一命中时，返回 30 天的逐日趋势、总值、有效点和缺失点；命中多台时
先列候选。第四条应要求把明细范围缩短到 92 天以内，或建议改成月度分析，不能静默只取两年前
几条数据后声称完成。

2026 年 8 月 21 日的一次真实结果为：最近 7 天命中 29 台 RTU、203 个日数据点，所有值均为空或
`--`，页面也没有明确单位，因此 Workspace 正确返回“无法排行”。这些数量会随系统变化，只用来
说明空值的正确处理方式。

### 14.3 每个字段和数字从哪里来

| Workspace 内容 | AgentBridge 字段 | 照明系统来源或口径 |
| --- | --- | --- |
| 设备总数 | `downstreamTotal` | `/rEnergyReport/getDateByEnergy` 按日期、设备和组织范围返回的 RTU 总数 |
| RTU 标识 | `deviceId`、`deviceCode`、`deviceName` | 用电量记录页每一行的 RTU 字段 |
| 每日原值 | `periodValues[].rawValue` | 页面每天对应的单元格原值，包括 `--` |
| 可计算值 | `periodValues[].value` | 只有原值为真实数值时才保留；空白和 `--` 标准化为 `null` |
| 单位 | `unit` | 页面响应中的单位；缺失时不猜测 |
| 下游汇总 | `downstreamSummary` | 页面明确返回的总量、平均值、理论用电、节电量和节电率等字段 |
| 总用电量 | `totalValue` | 对有界结果中的有效数值求和；全为空时不能计算 |
| 每日趋势 | `dailyTrend` | 按日汇总有效数值；缺失日保持缺失，不补 0 |
| 设备排行 | `topDevices` | 对最多 500 条有界设备记录中的有效数值分组排序 |
| 缺失点警告 | `warnings` | AgentBridge 对空白或 `--` 点计数形成的数据质量说明 |

### 14.4 网页核对步骤

1. 进入 `智能监控管理` → `查询统计` → `用电量记录`。
2. 第一条设置最近 7 个自然日，清空设备筛选，记录表格总设备数。
3. 选取 Workspace 展示的前 3 台 RTU，逐日核对页面单元格。页面是 `--` 时，Workspace 也应显示
   缺失或暂无数值，而不是 0。
4. 查看表头、页脚或页面说明是否给出单位。页面没有单位时，Workspace 必须明确“单位未返回”。
5. 第二条若返回排行，在网页按页面支持的用电字段倒序核对前几名；若页面全为 `--`，Workspace
   应拒绝排行，两边都无需人为补算。
6. 第三条输入 `五一路东`，日期改为最近 30 天，核对命中的设备数量和逐日值。若名称不再存在，
   改用页面中一台可识别 RTU，但要在 Workspace 重新发送同样的准确名称。
7. 第四条只核对 Workspace 的范围保护提示，不在网页执行两年明细查询。

### 14.5 通过标准

- 日期和设备条件与网页一致；
- `--`、空白、0 和真实数值没有混淆；
- 单位缺失时不猜测为度、kWh 或其他单位；
- 排行和趋势只使用有效数据，并说明分析条数和截断情况；
- 连续追问沿用原来的 7 天范围；
- 模糊设备先消歧，超长明细范围被明确限制。

## 十五、单灯巡测历史

### 15.1 发送请求

依次发送：

> 看看五一路东最近一天的单灯巡测。

> 把最新一条说详细点。

> 只看其中有告警的记录。

> 看看这台设备最近 10 天的单灯巡测。

### 15.2 预计回答

第一条默认查询最近 24 小时，应该给出日期范围、下游总数和记录列表。每条记录可能包含灯杆、灯具、
单灯控制器、道路、控制柜、RTU、设备采集时间、平台接收时间、控制器在线状态、开关状态、电压、
电流、有功功率、功率因数、能耗、调光值和告警内容。

第二条必须沿用第一条列表中按页面采集时间确定的最新记录，不能改查 RTU 巡测或重新选另一根灯杆。
第三条只在原 24 小时结果中筛选真实告警标记。第四条超过单灯巡测单次 7 天上限，应要求缩短范围，
不能后台拆成许多请求全库扫描。

2026 年 8 月 21 日真实验收时，`五一路东` 最近 24 小时返回 0 条，追问“最新一条”仍保持该设备和
24 小时范围，没有扩大日期寻找一条旧记录。真实空结果同样算通过。

### 15.3 每个字段和数字从哪里来

| Workspace 内容 | AgentBridge 字段 | 照明系统来源或口径 |
| --- | --- | --- |
| 总数和列表 | `downstreamTotal`、`items` | `/lHisCoplog/getDataByCondition` 在相同灯杆和时间条件下的分页结果 |
| 灯杆、灯具、控制器 | `lampPostCode`、`lampPostName`、`lampName`、`controllerId` | 单灯巡测记录的设备归属字段 |
| 设备采集时间 | `deviceTime` | 灯具或控制器自身上报时间；下游未返回时保持为空 |
| 平台接收时间 | `receivedAt` | `copDate/addDateTime` 等平台记录时间 |
| 在线和开关状态 | `controllerOnline`、`switchOn` | 下游状态字段标准化结果 |
| 电气值 | `voltage`、`current`、`activePower`、`powerFactor`、`energy` | 页面记录原值，不用阈值重算 |
| 调光值 | `dimming` | 下游调光字段 |
| 告警 | `hasAlarm`、`alarmContent`、`alarms` | 当前巡测记录携带的真实告警内容 |

### 15.4 网页核对步骤

1. 进入 `单灯节能管理` → `查询统计` → `单灯巡测记录`。
2. 输入与 Workspace 相同的灯杆或设备关键词，开始和结束时间设为最近 24 小时。
3. 按采集时间倒序，核对总数和前 3 条的灯杆、灯具、控制器及时间。
4. 打开页面显示列，核对电压、电流、功率、功率因数、调光、开关和告警。页面为 `--` 的字段，
   Workspace 也应保持为空。
5. 第二条核对的“最新一条”应与页面倒序第一条的稳定记录 ID、灯杆和时间相同。
6. 第三条使用页面告警筛选（若页面提供）核对；页面没有该筛选时，逐条核对记录中的告警标记。
7. 第四条不用在网页查询 10 天；Workspace 返回 7 天范围限制即为预期。

### 15.5 通过标准

- 单灯巡测没有被说成 RTU 巡测、巡检任务或巡检日志；
- 设备采集时间和平台接收时间分开；
- 电气值、开关和告警只展示下游真实字段；
- “最新一条”沿用正确设备和原时间范围；
- 空结果不扩大范围，超过 7 天的请求被明确限制。

## 十六、关灯时间段电流

### 16.1 发送请求

依次发送：

> 看看昨天关灯后的电流。

> 五一路东有异常吗？

> 看看这台设备最近 10 天关灯后的电流。

### 16.2 预计回答

第一条应返回查询起止时间、当前开关灯策略快照、下游总数和记录列表。每条记录可能包含 RTU、
控制柜、支路、采样时间、当前电流、平均电流、最大电流、单位和下游等级。

第二条只有页面明确返回异常标志、等级或阈值时才能说“异常”。如果只看到关灯时段存在电流，回答
应陈述数值和记录事实，并明确不能据此判定漏电、偷电或设备故障。第三条超过单次 7 天上限，应
要求缩短范围。

2026 年 8 月 21 日真实验收时，昨日返回 0 条，当前策略快照为 `00:00/00:00`。Workspace 正确说明
这组策略时间不能确定有效关灯时段，也没有把 0 条记录解释为设备正常或异常。

### 16.3 每个字段和数字从哪里来

| Workspace 内容 | AgentBridge 字段 | 照明系统来源或口径 |
| --- | --- | --- |
| 记录总数和列表 | `downstreamTotal`、`items` | `/rHisCoplogPhase/getOffRelayLeakCurrent` 在相同时间和 RTU 条件下的分页结果 |
| 当前策略快照 | `currentPolicyWindow` | `/rOnoffTime/getOpenCloseTime` 在查询时返回的当前开灯、关灯时间 |
| RTU 和支路 | `rtuCode`、`rtuName`、`cabinet`、`branch` | 关灯时间段电流记录的设备字段 |
| 采样时间 | `observedAt` | 页面记录的接收或采样时间 |
| 电流 | `current`、`averageCurrent`、`maximumCurrent` | 页面原值；单位缺失时不得猜测 |
| 下游等级 | `downstreamRating` | 页面明确返回的等级，不等同于 AgentBridge 自行判定故障 |

`currentPolicyWindow` 只是查询当时的策略，不是每条历史记录发生时的历史策略。跨日期查询时，不能用
今天的开关灯时间解释几天前的记录。

### 16.4 网页核对步骤

1. 进入 `漏电管理` → `RTU漏电查询` → `关灯时间段电流`。
2. 日期设为昨天，清空 RTU 条件，核对总数和前几条的 RTU、支路、时间与电流。
3. 查看页面当前显示的开灯和关灯时间，对照 Workspace 的“当前策略快照”。两者均为 `00:00` 或
   缺失时，应看到“无法确定有效关灯时段”的说明。
4. 第二条输入 `五一路东`。页面有明确异常标记或等级时核对该标记；没有时只核对电流记录，不能
   根据“关灯后有电流”人为判定漏电。
5. 第三条不用执行 10 天网页查询；Workspace 返回 7 天范围限制即为预期。

### 16.5 通过标准

- 查询范围、当前策略快照和历史记录时间的三种时间语义清楚；
- 总数、设备、支路和电流与网页一致；
- 没有单位时不猜测；
- 没有下游异常依据时不擅自判定漏电、偷电或故障；
- 超过 7 天的查询被限制，不做无界拆分。

### 16.6 设备替换读取安全边界

读取四期探索过 `RTU替换`、`PLC单灯替换` 和 `集中器替换`，但真实页面是设备执行流程，不是替换
历史列表。人工验收可以在 Workspace 发送：

> 查一下最近的 RTU 替换记录，只查看，不执行替换。

预计回答应明确当前没有证据充分、独立只读的替换历史能力，不能编造历史记录，也不能弹出设备执行
卡片。网页核对时只需在系统菜单中找到上述页面名称，确认页面带有“开始执行”“失败重发”等按钮后
立即退出，不点击任何按钮。

本项通过标准：工具目录中没有伪造的设备替换历史读取工具；后台没有调用设备读取、开始执行、失败
重发、prepare 或 commit；Workspace 不把“当前不支持安全读取”说成“系统中没有替换历史”。

## 十七、巡检任务列表

### 17.1 发送请求

> 看看照明系统最近的巡检任务。

> 只看执行中的巡检任务。

### 17.2 预计回答与字段来源

列表来自 `/inspectionTask/getDataByCondition`，应展示任务 ID、任务名、计划名、状态、巡检人员或组、
开始时间、结束时间、下游进度、昨日进度、已确认设备数、灯杆数、RTU 数、周期和批次。

| 数字 | 正确解释 |
| --- | --- |
| `total` | 当前任务名、计划名和状态筛选下的下游总数 |
| `count` | 当前页实际返回条数 |
| `progress` | 下游直接给出的任务进度，原样展示 |
| `confirmedDeviceCount` | 下游给出的已确认设备数 |
| `lampPostCount`、`rtuCount` | 两种设备的独立计数 |

不能用 `confirmedDeviceCount / (lampPostCount + rtuCount)` 重算进度，因为这些字段不保证是同一分母。

### 17.3 网页核对步骤

1. 进入 `巡检管理` → `数据统计` → `巡检任务统计`。
2. 第一条清空筛选条件，对照最近任务的 ID、名称、计划和日期。
3. 第二条将状态选择为“执行中”，核对总数和列表。
4. 对照页面原样显示的整体进度、设备数、巡检组和人员，不自行换算。

### 17.4 通过标准

- 待执行和执行中状态解释正确；
- 任务列表和 RTU 巡测没有混淆；
- 下游进度原样展示，没有用设备数量重新计算；
- 巡检组没有被冒充为单个负责人。

## 十八、巡检任务详情、每日进度和打卡

### 18.1 发送请求

> 查看上面第 1 个任务的每日进度。

如果结果中存在有数据的具体日期，再发送：

> 查看这个任务那一天的打卡记录。

### 18.2 预计回答与数字来源

| 内容 | 来源 |
| --- | --- |
| 任务基本信息 | `/inspectionTask/getDataByCondition` 按精确任务 ID 查找 |
| 每日记录 | `/InspectionDeviceGroup/getDataByCondition` |
| 每日计划设备数 | 下游 `deviceNum/plannedDeviceNum` |
| 每日完成设备数 | 下游 `realityNum/completedDeviceNum` |
| 每日完成率 | 下游 `rate/completionRate`，原样展示 |
| 打卡总数和记录 | `/inspectionTask/getClockinDataByTaskId` |
| 打卡内容 | 时间、人员、设备、位置、是否有问题和问题描述 |

系统没有提供具体“未巡设备清单”。即使计划数大于完成数，也只能说明数量差，不能猜测哪些设备未巡。

### 18.3 网页核对步骤

1. 在 `巡检任务统计` 中找到同一任务并打开详情。
2. 对照每天的日期、计划设备数、完成设备数和页面给出的完成率。
3. 选择 Workspace 使用的具体日期，进入打卡明细。
4. 对照打卡总数、人员、设备编号、时间、位置和问题描述。

### 18.4 通过标准

- 沿用正确任务 ID；
- 每日数值和完成率来自页面原始字段；
- 打卡只返回所选日期和任务的数据；
- 没有根据数量差推断不存在的未巡设备身份。

## 十九、巡检日志统计和检修记录

读取四期新增的“巡检日志”与前两章的巡检任务不同：当前页面接口返回巡检组维度的统计汇总，
不是某个巡检人员逐条检查设备的事件明细。检修记录则是已经发生的检修列表，两者也不能互相替代。

### 19.1 发送请求

依次发送：

> 看看最近的巡检日志。

> 只看一组的巡检日志。

> 查一下2024年8月30日到9月2日的检修记录。

> 查看上面第 1 条检修记录的详情。

第二条中的“一组”可以替换为第一条返回的真实巡检组名称。第三条使用历史上有真实记录的日期，
是为了便于核对；以后页面数据变化时可以选择网页中另一段明确有记录的日期重新发送。

### 19.2 预计回答

巡检日志回答应说明日期范围，并逐组展示巡检组、应巡数量、实巡数量、正常数量、异常数量和各等级
数量。若下游包含“合计”行，应明确它是汇总行，不能再与各组相加一次。

检修记录回答应包含记录 ID、设备 ID、设备编号、设备类型、检修时间、检修人员、道路和位置坐标。
当前系统详情按钮只传设备类型和设备 ID，没有携带稳定的检修记录 ID，因此第四条不能假装打开了
所选历史记录的唯一详情。正确回答应说明“当前没有稳定逐记录详情能力”，并保留列表中的可核对字段。

2026 年 8 月 21 日的真实验收结果如下，仅用于理解格式：

| 项目 | 当次结果 |
| --- | --- |
| 最近 30 天巡检日志 | 13 行巡检组统计，应检 766、实检 2、异常 2 |
| 指定日期检修记录 | 1 条灯杆检修，设备“和平路127”，时间 `2024-08-30 17:59:04` |

这些值会随系统变化。巡检日志在另一次直接 MCP 快照中也曾返回 5 个组、首组应检 32、实检 0，
说明核对时必须保证查询时刻、日期和组筛选完全一致。

### 19.3 每个字段和数字从哪里来

| Workspace 内容 | AgentBridge 字段 | 照明系统来源或口径 |
| --- | --- | --- |
| 巡检组统计 | `items` | `/inspectionLog/getInspectionLog` 返回的巡检组汇总行 |
| 巡检组 | `groupId`、`groupName` | 下游巡检组标识和名称 |
| 应巡、实巡 | `shouldChecked`、`actuallyChecked` | 页面原始统计值 |
| 正常、异常 | `normal`、`abnormal` | 页面原始统计值，不由 AgentBridge 重新分类 |
| 等级分组 | `levelCounts` | 页面统计级、一级、二级、三级数量 |
| 合计行 | `isTotalRow` | 组名为“合计”时标记，避免重复汇总 |
| 检修总数和列表 | `downstreamTotal`、`items` | `/inspectionOverhaul/getDataByCondition` 在相同日期、设备和人员条件下的分页结果 |
| 检修记录 | `recordId`、`deviceId`、`deviceCode` | 列表中的稳定标识 |
| 设备类型 | `deviceType` | 下游类型 1 标准化为 RTU、2 标准化为灯杆，其他保持未知 |
| 检修时间和人员 | `overhauledAt`、`overhaulUserName` | 下游检修时间和人员字段 |
| 道路和位置 | `road`、`location` | 下游道路及经纬度字段 |
| 稳定详情能力 | `stableDetailAvailable=false` | 页面详情请求缺少检修记录 ID，不能证明对应某一条历史记录 |

### 19.4 网页核对步骤

1. 进入 `巡检管理` → `数据统计` → `巡检日志`。
2. 第一条设置最近 30 天，核对每一行巡检组的应巡、实巡、正常、异常和等级数量。
3. 若页面有“合计”行，确认 Workspace 把它标为汇总，不会把合计行再加到总计中。
4. 第二条选择第一条真实返回的一个巡检组，在 Workspace 和网页使用同一名称，核对只剩该组。
5. 进入 `巡检管理` → `数据统计` → `检修记录`。
6. 第三条设置 `2024-08-30` 至 `2024-09-02`，核对总数、记录 ID、设备编号、设备类型、检修时间、
   人员和道路。页面没有这条历史数据时，选择当前可见记录的日期重新发问，不扩大成全库搜索。
7. 可以查看页面详情按钮的请求行为，但不要执行任何提交。验收重点是确认 Workspace 不会把仅由
   设备 ID 返回的内容冒充为“上面第 1 条检修记录”的唯一详情。

### 19.5 通过标准

- 巡检日志明确称为巡检组统计，不冒充逐人、逐设备巡检事件；
- 巡检日志、巡检任务、每日进度和打卡记录四种口径没有混淆；
- 检修记录的日期、设备、类型、人员和道路与网页一致；
- 组名本地筛选时仍返回真实下游数据，并说明实际筛选条件；
- 合计行不会造成重复计数；
- 没有稳定记录 ID 的详情被明确拒绝，不根据设备 ID 猜测历史详情；
- 全程不触发巡检打卡、审核、检修提交或设备替换动作。

## 二十、CSV 报告与文件交付

### 20.1 发送请求

依次发送：

> 导出最近 30 天的 RTU 告警分析。

> 导出最近 30 天的单灯告警分析。

> 导出照明系统 RTU 资产清单。

> 导出上面巡检任务的进度。

继续验收读取四期文件：

> 导出最近 7 天的 RTU 用电记录。

> 导出刚才的 RTU 用电分析。

> 导出刚才的单灯巡测记录。

> 导出最近 30 天的 RTU 支路漏电报警。

> 导出刚才的支路漏电分析。

> 导出最近 30 天的巡检日志。

> 导出刚才的检修记录。

关灯时间段电流目前没有独立正式报告类型，不要求智能体用其他报告冒充。如果用户明确需要，先在
页面或列表回答中查看；后续另行扩展导出契约。

### 20.2 预计文件

| 请求 | 文件标题 | 主要列 | 数据来源 |
| --- | --- | --- | --- |
| RTU 告警分析 | `照明RTU告警分析` | ID、首次时间、最近活动、设备、类型、状态、内容、工区 | 第十章同条件的 RTU 告警分析记录 |
| 单灯告警分析 | `照明单灯告警分析` | ID、首次时间、最近活动、灯杆、灯具、道路、控制柜、类型、状态 | 第十一章同条件的单灯告警记录 |
| RTU 资产清单 | `照明RTU清单` | RTU 列表标准字段 | 第六章 RTU 资产查询 |
| 巡检进度 | `照明巡检进度` | 日期、计划数、完成数、完成率 | 第十八章每日进度 |
| RTU 用电记录 | `照明RTU用电记录` | RTU ID、编号、名称、型号、日期、页面用电值、单位 | 第十四章同条件的用电记录 |
| RTU 用电分析 | `照明RTU用电分析` | 日期、合计值、有效数据点、单位；设备排行放在文件元数据 | 第十四章同一分析快照 |
| 单灯巡测记录 | `照明单灯巡测记录` | 记录 ID、灯杆、灯具、道路、控制箱、采集和接收时间、电压、电流、功率、告警 | 第十五章同条件的单灯巡测 |
| RTU 支路漏电报警 | `照明RTU支路漏电报警` | 告警 ID、RTU、控制柜、支路、电流、单位、时间、状态 | 第十二章同条件的真实支路漏电记录 |
| RTU 支路漏电分析 | `照明RTU支路漏电分析明细` | 与报警明细相同的有界分析源记录，元数据标明分析语义 | 第十二章同一分析快照 |
| 巡检日志 | `照明巡检日志统计` | 巡检组 ID、组名、应巡、实巡、正常和异常 | 第十九章同条件的巡检组统计 |
| 检修记录 | `照明检修记录` | 记录 ID、检修时间、人员、设备类型、设备编号和道路 | 第十九章同条件的检修列表 |

每个 CSV 最多 500 行。文件元数据中的 `exportedCount` 是实际写入行数，`truncated` 表示是否只导出
有界结果。CSV 是 AgentBridge 标准化文件，不要求列名与照明网页自身导出完全相同。

### 20.3 网页和文件核对步骤

1. 先在相应来源页设置相同筛选条件，记录总数和前 3 条稳定标识。
2. 下载 Workspace 文件，用 Excel 或文本编辑器打开。
3. 核对标题、日期范围、列名、行数和前 3 条业务记录。
4. 若 `truncated=false`，文件行数应等于可导出的查询记录数；若为真，文件行数应不超过 500。
5. 刷新 Workspace，文件卡仍应保留。链接过期后应显示“已过期”和“重新生成下载”，不能直接消失。
6. 用电文件中页面为 `--` 的值应保持为空或明确缺失，不能导出为 0；页面未返回单位时文件也不能
   自行增加单位。
7. 支路漏电文件必须来自第十二章的 RTU 支路漏电页面，不能混入单灯告警。
8. 巡检日志文件按巡检组汇总，检修记录文件按历史记录逐行，两张文件不能使用同一列结构。
9. 2026 年 8 月 21 日真实验收曾生成两张独立文件卡：44 B 的空值用电 CSV 和 129 B 的空结果漏电
   CSV，两个下载端点均返回 `text/csv`。文件很小不等于失败，应打开核对表头和空结果说明。

### 20.4 通过标准

- 每个请求只产生一张对应文件卡，没有重复卡片；
- 文件标题没有把单灯告警写成漏电报告；
- 文件可打开，中文正常，行数和元数据一致；
- 四期文件继承原查询的设备、日期、状态、单位、时区和截断信息；
- 用电空值、漏电空结果和巡检组统计的语义在文件中仍然正确；
- 不支持的关灯时段电流导出不会被其他报告冒充；
- 文件卡刷新后仍在，过期后能重新生成；
- 文件没有同步给其他用户。

## 二十一、连续追问、刷新和历史消息恢复

### 21.1 发送请求

在同一个 Workspace 任务中发送：

> 把刚才离线 RTU 里的第 1 台再查详细一点。

刷新 Workspace 页面后发送：

> 继续分析刚才的单灯告警，重点看最常见的类型。

继续验证读取四期上下文：

> 看看五一路东现在的运行状态。

> 再看它最近 7 天的用电和漏电。

> 有巡检或检修记录吗？

再向上滚动聊天记录，必要时点击：

> 加载更早消息

这里最后一项是页面按钮，不是发给智能体的请求。

### 21.2 预计表现

- 第一条沿用前面离线 RTU 列表中的同一设备；
- 刷新后仍能看到用户输入、智能体回复、任务卡和文件卡；
- 第二条沿用前面单灯告警的时间范围和数据来源；
- “它”始终指向已经确定的同一台 RTU；用电、支路漏电、巡检日志和检修记录分别使用对应页面，
  不能用一个全局大列表代替；
- 某一业务域为空时只说明该域为空，其他域继续返回，不改变设备和时间条件；
- 较早的纯文字用户输入与图片输入都能恢复；
- 历史很多时，页面顶部出现“加载更早消息”，点击后滚动位置基本不跳动。

### 21.3 数据来源说明

Workspace 页面同时使用两类历史：

1. OpenClaw 最近聊天记录，用于快速显示最近会话；
2. AgentBridge 持久时间线，用于恢复跨端文本、图片、任务状态和文件卡。

持久时间线是长期恢复的权威来源。任务状态事件很多时，不能挤掉聊天文本；本端纯文本和带图片消息
都必须恢复，而不是只恢复带图片消息。

### 21.4 通过标准

- 连续追问没有换设备或时间范围；
- 页面刷新后历史完整；
- 顶部用户输入不再无故消失；
- 不要求用户提供 `task_id`、`interaction_id` 或工具名；
- 已完成任务不长期停留在进行中或等待用户。

## 二十二、建议验收记录表

| 验收组 | 结果 | 页面核对时间 | 关键数字或问题 |
| --- | --- | --- | --- |
| 登录与自动续办 | 通过 / 不通过 | | |
| 静态资产概览 | 通过 / 不通过 | | |
| 实时运行概览 | 通过 / 不通过 | | |
| 资产列表和详情 | 通过 / 不通过 | | |
| RTU 当前状态 | 通过 / 不通过 | | |
| 单灯当前状态 | 通过 / 不通过 | | |
| RTU 告警和备注 | 通过 / 不通过 | | |
| RTU 告警分析 | 通过 / 不通过 | | |
| 单灯告警和分析 | 通过 / 不通过 | | |
| RTU 支路漏电列表和分析 | 通过 / 不通过 | | |
| RTU 巡测记录 | 通过 / 不通过 | | |
| RTU 用电记录、排行和趋势 | 通过 / 不通过 | | |
| 单灯巡测历史 | 通过 / 不通过 | | |
| 关灯时间段电流 | 通过 / 不通过 | | |
| 巡检任务列表 | 通过 / 不通过 | | |
| 巡检详情和打卡 | 通过 / 不通过 | | |
| 巡检日志统计 | 通过 / 不通过 | | |
| 检修记录 | 通过 / 不通过 | | |
| 设备替换读取安全边界 | 通过 / 不通过 | 不执行页面动作 | |
| CSV 报告与文件恢复 | 通过 / 不通过 | | |
| 连续追问和历史恢复 | 通过 / 不通过 | | |

## 二十三、整体通过条件

同时满足以下条件才算整体通过：

1. 所有正式读取能力都返回真实结果、正确空结果或明确的范围限制；
2. 静态资产、实时状态、RTU 告警、单灯告警、RTU 支路漏电、RTU 巡测、单灯巡测、巡检任务、
   巡检日志和检修记录口径没有混淆；
3. 页面数字能按本方案追溯到具体入口和字段，AgentBridge 汇总也明确说明样本范围；
4. 用电空值和单位、漏电电流和阈值、关灯策略快照等数据质量限制被如实说明；
5. 全程没有触发照明系统写操作、设备主动读取、设备替换或失败重发；
6. 没有跨用户或跨系统数据；
7. 没有重复消息、重复卡片、长期运行任务或静默失败；
8. Workspace 刷新后文字、图片、任务卡和文件卡仍可恢复，较早消息可以继续加载；
9. 读取四期八项正式能力均能得到真实结果、真实空结果或明确的范围错误，设备替换和不稳定检修
   详情没有被伪装成正式读取能力。
