# 数据库受控只读生产试点验收

2026-09-09，按用户明确授权使用现有账号试点。分析代码版本 `5c221fc7e31e` 已通过固定发布流程部署至中心生产服务，健康、治理、MCP 目录、OpenClaw runtime 检查及 GitHub 回读通过。

## 最终范围

数据源 enabled=true，privilege_policy=controlled_readonly_pilot，仅 admitted_subjects=["guomao"]。中央身份辛国茂已绑定日志系统用户 ID 300002517；其现有 Token 增加 taihua:analytics:read、taihua:analytics:export。其他身份未扩权。数据库凭据仍加密保存，生产源库权限未修改。

TEMP、序列 USAGE 经本次明确授权作为试点风险提示；数据库账号并未完成最小权限收敛。表/列写权限、序列 UPDATE、特权角色仍拒绝。只读事务、固定参数 SQL、本人范围和接口/数据库前后核对保留。查询只覆盖指定 work_log 列及必要元数据，认证表与未来默认授权仍需 DBA 后续收敛。

## 验收证据

| 检查 | 结果 |
| --- | --- |
| 分析与导出自动测试 | 51 项通过 |
| MCP、身份、任务计划回归 | 80 项通过 |
| PostgreSQL 16.1 独立容器 | 表写入拒绝、只读事务序列推进拒绝、严格策略拒绝、试点固定查询成功；取消约 0.51 秒，断连错误脱敏，连接全部关闭；容器已清理 |
| 生产元数据检查 | 通过，风险提示 temp、sequence_usage |
| 本人 2026-08-31 至 2026-09-06 | 3 条、3.5 小时；个人接口、本人过滤团队接口、数据库逐行及聚合匹配 |
| 本人 2026-09-02 单日空窗口 | 0 条、0 小时，完整性通过 |
| 历史结果重读 | 当前身份、会话及可见性重新验证后成功 |
| CSV 认证下载 | 导出与下载成功，1480 字节；临时撤销导出 Scope 后下载拒绝，随后恢复原授权 |
| 两周持久比较 | 基期 2026-08-24 至 2026-08-30：4 条、12 小时；本期 3 条、3.5 小时，差额 -8.5 小时；三步均 succeeded，受保护结果重新鉴权读取成功 |

比较计划 ID：`e184253e-60d6-449f-bf91-fbe1f56f80d5`。原始执行结果保存在本地 output/analytics-pilot-*.json，隔离数据库证据位于 output/analytics-isolated-pg-result.json，不包含数据库密码。

真实取数发现团队日志只返回 username、不返回 userId。实现已补充登录 principal 的 ID/username 与 member-options 精确映射；无 ID 行必须精确匹配该账号名，有 ID 时必须一致，拒绝相互冲突字段。完整快照包含账号映射，数据库仍使用可信 user_id 与 API 日志 ID 双重过滤。没有按姓名推断身份。

## 验收边界

比较成功由宿主协议客户端完成，使用正式宿主注册、任务和协调租约检查。OpenClaw CLI 的一次模型自动编排调用超时，未作为模型端到端验收通过。普通用户、全员分析与团队数据库直读均未开放；本次结论仅为辛国茂个人数据库分析试点可用。

回退配置备份保留在中心 analytics/source.before-controlled-pilot.json，原 Token Scope 备份在 analytics/pilot-token-scopes-before.json。关闭 enabled 可停止新查询、结果读取与下载；不影响原日志能力。
