# 数据库分析一期收尾验收

本轮继续个人试点的使用端和运维验收，源库权限及准入范围保持不变。

## 已核实

- xinguomao 对应用户 ID 300002517，角色 ADMIN、EMPLOYEE。它是本人试点样本，不是非管理员验收样本。
- 生产服务重启后，次日通过认证 MCP 重读昨天的分析结果成功，仍返回 3 条、3.5 小时，并重新核验当前会话及来源可见性。
- 使用两条不含业务数据的专用加密合成记录检查自动清理：过期记录的索引及密文文件由后台治理循环自动删除，未过期记录保留；不通过手动清理冒充定时任务成功。
- 生产开关实测：关闭 enabled 后，新汇总、历史结果读取及下载均返回 SOURCE_UNAVAILABLE；finally 路径恢复 enabled=true，未修改其他准入字段。
- 编排回归：70 项通过；工具目录与服务描述检查：5 项通过。已同步生成 OpenClaw 工具目录。

## 编排问题与修正

原独立测试会话未绑定身份；改用本人已绑定 Telegram 会话后工具可用，模型调用了计划目录及计划 prepare，但编造了 taihua_personal_summary_csv_attachment.v1 转换，中央校验按预期拒绝，尚未读取业务数据。

根因是通用策略要求“数据读取后生成附件”进入持久计划，而一期单窗口 CSV 已实现为受保护结果的原子调用链。中央策略、计划目录和模型工具描述现已明确：

- 单窗口：summary(group_by=day) → report_export(result_id) → report_download(report_id)，使用现有附件交付。
- 两窗口比较：两个 summary + taihua_personal_compare.v1 三步 v2 计划，再 result_get。
- 不支持的转换仍拒绝；不把导出塞入比较计划，不放宽任意 SQL、源数据范围或写入校验。

## 原生附件处理修正

第一次修正后的真实模型调用完成了 summary、export、download，但模型声称已发送附件时，中央并无 task.artifact.delivery 回执，工具结果仍带文件字节。因此该次不能计为附件交付通过。

根因是原生工具结果路径只捕获交互卡，CSV 处理依赖旧 OpenClaw 中间件；当前宿主可绕过该中间件。已在可信原生结果处理入口接入附件交付，并向模型返回真实 hostDelivery 回执、移除 base64 文件内容。交付失败与后续中间件重复处理均有覆盖，插件测试 121 项通过。

代码修正 5df24ed、6ff03d2 均已通过固定入口发布、服务健康检查、Gateway 重启及冷/热预热，并推送 origin/main。

## 真实模型与 Workspace 复验

- 原生模型已完成两个 summary + compare 三步计划，随后读取受保护比较结果：2026-08-24 至 08-30 为 4 条、12 小时；08-31 至 09-06 为 3 条、3.5 小时。减少 1 条（25%）、8.5 小时（70.83%）。
- 原生 CSV 结果现在不含 content_base64，并返回真实 hostDelivery。命令行绑定会话两次返回 ROUTE_MISSING、attemptCount=0；模型如实报告未发送，不能计为 Telegram 附件交付成功。
- Chrome 重启后，全浏览器发现入口报请求头策略加载失败；直接选择 Chrome 扩展成功，已核实并复用 xinguomao Workspace 页面，未切换账号或修改浏览器权限。
- 10:04 点击有效 CSV 文件卡后，浏览器已在 Downloads 保存 1480 字节 CSV；解析为表头 + 7 天数据，合计 3 条、3.5 小时。文件 SHA-256：0ad8cdae6a3c8e7891a1b6334135c2f401f5c275fbe8779966a441560258165e。扩展的 download 事件等待超时，但落盘文件证明下载成功。
- 新 Workspace 请求完成汇总，但导出复用了已终结任务，导致协调租约拒绝。修正为受保护结果读取、导出、下载各自创建独立任务，原始汇总和计划门禁不变；122 项插件测试通过，包含同轮汇总终结后继续三种结果操作。
- 961bc7b 发布后，10:12 从真实 Workspace 新发起请求，汇总、导出、下载三个任务全部 succeeded。网页端回执确认 1 个认证下载入口可用；实际点击保存的新 CSV 仍为 1480 字节、7 天、3 条、3.5 小时，数据库读取时间为 02:12:58 UTC，证明本轮重新取数。报告 ID：476f359b8fe948ab9af2e00c7283b85a；SHA-256：a505953e246f08ba7a643ab2b22c5a9fba1a2df5f775c67d1665c7b92ce91843。
- 已检查桌面和 390×844 窄屏，结果文本、输入区可用；恢复原视口。过期 CSV 卡片显示已过期并移除下载按钮。模型 Markdown 表格当前沿用普通文本显示，此次未改动通用聊天渲染。
- 跨端通知另发现受保护 CSV 被通用附件分发误认：相对认证 URL 抓取报 TYPEERROR 后回退发链接。已将此类伴随端 artifact 通知设为 no_op，保留 Workspace 文件卡及专用认证下载工具；普通证书附件分发保持原有行为。不能将先前 fallback_link_sent 计为 CSV 原生附件发送成功。
- Telegram 路由恢复后，原生模型调用专用报告下载工具成功发送同一份本人报告。中央 02:18:53 UTC 回执确认 attachmentSentCount=1、failedCount=0、attemptCount=1、errorCode=null、state=attachment_sent；任务 60212eca-f5f4-4ade-9cee-38c86f93d7ad。此前 ROUTE_MISSING 失败已与本次成功区分。
- 最终回归：插件 122 项、任务与分析 B 期 73 项、文档 7 项通过。生产个人试点范围不变。

## 第二用户准入与实际对账

上午后续经用户明确授权，完成 lishiyu 的 Token 更换、可信宿主配置和 Gateway 重启；保留原有 OA 权限，补齐 `taihua:read`、`taihua:analytics:read`、`taihua:analytics:export`。丢失及被替换的两个 Token 已撤销，签发、身份绑定修正及准入变更均有中央审计。泰华按 fullname 核验，预期主体由初填的账号 lishiyu 修正为“李世玉”，本人在可信页面完成登录。

生产准入现为 `guomao`、`lishiyu`，不是全员启用。源库账号、权限及 controlled_readonly_pilot 策略保持不变。中央实时身份接口确认 lishiyu 的人员 ID 为 300002378，唯一角色为 EMPLOYEE，满足非管理员样本条件。

| 样本 | API / 数据库条数 | 登记工时 | 验收结果 |
| --- | --- | --- | --- |
| lishiyu，2026-08-24 至 08-30 | 0 / 0 | 0 | 完整空窗口，非缺权限造成的假空 |
| lishiyu，2026-08-31 至 09-06 | 1 / 1 | 4.5 | 日志 ID 集合、逐条日期/类型/工时及聚合一致 |
| lishiyu，2026-09-05 至 09-06 | 0 / 0 | 0 | 零分母返回 null，不伪造平均值 |
| guomao，2026-08-31 至 09-06 | 3 / 3 | 3.5 | 同一轮第一用户对照样本 |

非管理员可完成本人 API 与团队接口仅本人筛选的完整性核验。两用户的 summary、result_get、report_export、report_download 均成功。认证 MCP 下载已分别保存 CSV 并逐日核对：lishiyu 为 7 天、1 条、4.5 小时，guomao 为 7 天、3 条、3.5 小时。

- lishiyu 结果：`005255d8239d49dd9f9524116878da38`；CSV 报告：`38123f493d634246b125c7688080489b`；CSV SHA-256：`adfc2afa6bf8f4099a7eb02c1e676f257b33a8dfad8093748828c627d2bdaa30`。
- guomao 结果：`51615bcfc6fe43ecbe0cc56fa3e6de16`；CSV 报告：`47b0dd225ba74a47a9983ff5a8180168`；CSV SHA-256：`bdb27dcb50ad333710c6a6b4d05be624122d2cbb4bd7ace8839c97c2bd9c0471`。

## 双用户隔离与撤权

两名真实用户分别以自己的有效 Token 访问对方的 result_id 和 report_id，双向 result_get、report_export、report_download 共六项全部返回 `DATA_ACCESS_DENIED`，未返回业务数值或文件。guomao 的下游 ADMIN 角色不能突破中央结果归属。

另短暂从数据源准入中移除 lishiyu，验证新汇总、已有结果重读、导出和下载四项均返回 `DATA_ACCESS_DENIED`。同一执行流程的 finally 恢复 `admitted_subjects=["guomao","lishiyu"]`，并以原有效 Token 成功重读比较结果，证明恢复完成；未改变 guomao 的准入或任一 Token Scope。

## 非管理员真实模型与工作台

- 李世玉自己的 Workspace 已完成三步 v2 比较计划 `50503eb2-06af-4cdc-83f1-9413b30cef91`，状态 succeeded；受保护比较结果为 `9be653cc20b347b0b0adc7d9551c1eca`。
- 比较值为 0 → 4.5 小时、0 → 1 条；变化率为 null，说明为“基期为零”。中央 result_get 与页面答复一致。
- 原生导出及下载任务完成，报告 `7d2b73e225964d27b1be469d2384ba6d` 的文件卡显示“网页端：1 个下载入口可用”。
- 初次长请求曾显示“结果连接中断”，中央 dispatch 记为 failed，但对应比较、结果读取、导出、下载任务继续完成，之后原答复也出现在页面。另一次只重读已有比较结果的请求完成并正常显示；没有重放原分析/导出链。不能将初次流式连接记录改写为从未中断。
- 认证 MCP 文件已落盘并对账；首次浏览器点击仅确认中央下载操作 succeeded，尚未发现 Downloads 新文件。浏览器内部下载页被 URL 策略拒绝，跨用户网页导航另返回 ERR_BLOCKED_BY_CLIENT；这些浏览器拦截不能作为应用返回越权拒绝的证据，网页下载验收与上述 MCP 隔离验收分开记录。
- 13:00 使用已有按天结果重新签发报告 `22f7b1f542b24ecea21c51ab5c04248a`，导出、下载任务及正常页面答复均完成，没有重新汇总。再次点击有效文件卡，等待下载事件 12 秒仍超时，Downloads 仍只有第一用户早先的两个文件。已请求用户确认浏览器保存窗口；未把过期引用或未落盘状态写成下载通过。
- 13:03 用户确认浏览器弹出了文件保存对话框，并完成保存。实际文件 `C:\Users\xingm\Downloads\taihua-personal-daily.csv` 为 1480 字节，解析为 7 天、1 条、4.5 小时，数据库读取时间为 `2026-09-10T03:50:19.241178+00:00`，SHA-256 为 `adfc2afa6bf8f4099a7eb02c1e676f257b33a8dfad8093748828c627d2bdaa30`，与同一按天结果的 MCP 下载完全一致。普通用户网页文件卡→保存对话框→实际文件对账通过；下载事件超时不能单独判定服务端下载失败。跨用户网页导航的浏览器拦截仍不计入应用隔离证据。

本轮文档与分析 A/B 回归共 58 项通过。受限现场证据保存在工作区 `output/analytics-two-user-20260910/`；凭据、日志正文、解密证据行不进入 Git。

## 剩余事项与边界

- 长请求的流式连接提前结束、随后结果可恢复的现象保留为宿主交付稳定性改进项，不能称为所有请求均无中断。
- DBA 后续处理认证表/默认授权读取范围、TEMP 与序列权限、角色默认只读和连接来源，满足条件后再恢复 strict；本轮未执行生产 GRANT/REVOKE、DDL 或写入探测。

当前两用户本人分析试点的数值正确性、真实模型比较、MCP 隔离、撤权和网页文件实际交付收尾已完成。DBA 加固及通用宿主流式交付稳定性继续单独跟进；团队/项目分析、正文周报和全员上线不属于本轮交付。


## DBA 收权后的视图适配

DBA 通知 xinguomao 已仅保留 analysis 下 users、departments、projects、work_logs、work_log_comments 五个脱敏视图，密码不变。只读现场检查确认 TEMP=false、public USAGE=false、analysis USAGE=true。旧服务预检出现 DATA_CONTRACT_CHANGED，原因是 SQL 和字段契约仍固定依赖 public.work_log。

新版将明细、聚合和字段契约指向 analysis.work_logs，保留八个字段的严格类型校验，不读取 content。源表主键改用 pg_catalog OID 关联核验，避免 regclass 名称解析要求 public USAGE；写权限检查包含可更新视图，序列检查覆盖非系统 schema。API 双路可见性、本人 ID 与日志 ID 参数限制、逐行和聚合对账均保留。

发布前通过 59 项相关回归。候选执行器在独立进程、同一生产凭据下通过 strict 空 ID 预检，告警为空；两份已验收样本经新视图重查，lishiyu 为 1 条/4.5 小时，guomao 为 3 条/3.5 小时。此步骤未替换运行中的服务，也未修改源库权限；正式接口复测结果另记。

正式发布完成：中央版本 `007c17004db2`，Release 冒烟通过，OpenClaw 保留原进程 25652。安装版本再次通过 strict 空 ID 预检后，仅将 source.json 的 privilege_policy 改为 strict，账号、密码、enabled 和两用户准入保持不变；配置备份为 source.before-strict-20260910.json，中央审计事件为 028a344f-597e-488f-9556-9a87048a1d92。

正式 MCP 接口共 17 项检查通过：两用户新汇总、结果读取、CSV 导出/下载，双向六项跨用户拒绝，以及 lishiyu 基期、空窗口和宿主身份检查。新汇总 provenance 均为 strict、privilege_warnings=[]，数值仍为 lishiyu 1 条/4.5 小时、guomao 3 条/3.5 小时。证据在本地 output/analytics-strict-views-20260910/。本轮未重新操作浏览器下载对话框；正式下载通过 MCP 获得文件字节。

此前 TEMP、序列 USAGE 和直接读取 public 业务表的遗留已由 DBA 处理，当前应用已适配；早前段落保留为历史验收事实。宿主长请求的流式交付稳定性仍是独立待办。五视图是数据库授权范围，不代表应用已开放团队、项目、正文或评论分析。
