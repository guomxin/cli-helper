# 数据库三场景流程与 Workspace 验收

日期：2026-09-16。状态：实施与验收进行中。

## 范围

复用现有只读数据库能力，强化事项覆盖、连续追踪、相似案例流程；不新增能力或授权。设计见[设计说明](../../系统适配/泰华日志系统/数据库事项复核追踪与案例检索设计.md)。

## 已有后端样本证据

2026-09-15 已完成授权执行器样本读取：周范围 13 篇日报、5 人、104 小时；追踪读取 30 篇上下文；案例扩展命中 9 篇。标准查询与自由 SQL 的样本 ID 一致，21 篇引用源正文重新核对。94 个拆分项为记录级事项，同一智能体复核，不是独立人工标准。原始正文保留在本地忽略目录，未提交仓库。

这些结果不代表 Workspace 端到端通过，段落链接当时也未逐个在浏览器打开。

## 本轮执行表

| 项目 | 状态 | 通过要求 |
| --- | --- | --- |
| 查询、证据分页、预算与权限回归 | 通过：全量 974 项、1041 子测试；5 项按原条件跳过 | 相关测试通过，原权限边界不变 |
| 函数拒绝恢复 | 通过：4 项传输测试及 8 子测试；修正 SQL 仍受账号授权约束 | 不原样重试；保持筛选语义，不扩大授权 |
| xinguomao 事项复核 | 基本流程通过：13 篇、13 来源链接、逐来源明细 | 周范围与计数正确、清单及引用支持结论 |
| xinguomao 连续追踪 | 通过本次样本：6 篇候选、10 个段落引用，建议与实施分开 | 分开不同问题、建议与实施，保留证据缺口 |
| xinguomao 案例检索（同范围运行两次） | 待执行 | 说明检索范围与限制，结果不夸大 |
| lishiyu 标准查询三场景 | 本次样本通过：三轮均完成，无自由 SQL 授权 | 无自由 SQL 授权也能完成标准能力范围内分析 |
| 段落来源页面 | 通过：原文300430174及300430533第5段实际读取、锚点存在且可见 | 点击结果来源，正文和段落可核对 |
| 发布与线上检查 | 通过：5972eebe5d7a 已部署并推送 origin/main；服务、宿主、预热通过 | PlanOnly、全量验证、部署及线上检查 |

## 浏览器恢复

前次工具初始化报 kernel assets 路径错误，未完成页面验收。用户重启 Codex 后，本轮恢复 Chrome 连接，分别读取 xinguomao 与 lishiyu 的现有 Workspace 页面身份。未修改浏览器安全设置或重新登录。

## 实施中发现并修复的问题

中央规划提示增至 6664 个 UTF-16 单元，超过宿主 6000 上限；部署前停止发布，等义精简三段已有规则后为 5773，并新增完整提示长度回归。宿主仅在 gateway_start 加载中央规则，因此本次用 ForceRestartOpenClaw 刷新缓存，PlanOnly 原因为 forced；不是路径或换行导致的重启。无关工作区改动保留，发布从同一提交的独立干净副本运行。

## 发布证据

运行提交 5972eebe5d7ae851de57be29773fcb381434bae9；Publish-AgentBridge 于 2026-09-16 完成全量验证、部署、线上检查和 GitHub 推送。线上服务 active，admin/workspace ready，近期错误数 0，OpenClaw 插件 0.4.95 loaded，RPC 正常。发布脚本未配置 identityIsolation 探针，本轮双账号功能另以真实页面及实时授权目录验证，不能混称该探针已通过。

实时授权核对：guomao 包含 database.free.read；lishiyu 没有该项，保留标准目录、日志查询与正文分析，未改授权。

## 页面执行记录（持续更新）

- 10:29—10:33，两个账号同题完成 9月7日至13日 DAILY 复核，均分页读完13篇并列出逐来源事项。xinguomao 首查精确同名部门为0，随后明确说明采用带 #236023 的目录候选；后续测试固定该显示名称。lishiyu 无自由 SQL 权限，仍完成标准查询路径。明细按来源归纳，并非94项人工金标准逐项评分，语义无遗漏尚不能据此保证。
- 10:35，打开回答提供的日志300430174原文链接，作者、日期、部门及正文五项与回答对应。自动化点击 target=_blank 未观察到新标签，随后用该已返回URL在同一Chrome会话新标签读取成功；只据此确认链接可访问，不把点击弹窗行为记为通过。

- 10:34—10:38，双账号问题追踪完成。当前同部门同日期全范围为32篇（昨日后端样本30篇，不是冻结快照）；直接候选6篇。guomao补充扩展词检索，未声称读完全部32篇；lishiyu实际续页读完32篇上下文。两者均区分计划重下发、节能率恢复、灯态误判、道路调整、登录故障；明确迁移服务器仅为建议、验收资料提交不代表验收通过。guomao提供10个带段落锚点来源。
- 10:38，实际读取300430533的第5段，段落锚点存在且进入可视区域，原文支持登录问题持续、服务器迁移尚为建议。

## 案例首轮发现与修正

- lishiyu 首轮案例检索完成：两轮关键词共17条、去重16篇，归并事件后保留10类相关案例，剔除相邻APP工单等问题；保留处理动作与明确恢复的区别。新增日照港案例的实际来源页300430178已读取，原文明确写解决登录问题并增加定时检查。
- guomao 首轮主候选17篇，宽词补查36篇。页面出现恢复原请求状态，最终返回结果，未重复发送。一次自由SQL函数被拒绝（02:40:42 UTC，DATABASE_FUNCTION_DENIED），后续查询成功，失败步骤仍保留供展开检查。
- guomao 首轮案例质量未通过：表格写仅已处理，汇总却称明确快速恢复；相邻APP问题混入主表。已加强 experience 的跨段状态一致性、无时间证据不称快速、相邻问题单列、缺后续不推断当前未解决。相关内容/证据/传输20项及43子测试通过，另扩展四个分析模式预算回归4项及20子测试通过；页面复测待执行。
