# 数据库分析一期问题修复验收

日期：2026-09-11，12:49—13:01（北京时间）。针对[工作台补测](./数据库分析一期工作台补测-2026-09-11.md)发现的四项问题修复并发布，代码提交 `3933517`，生产版本 `39335170a79b`。复用 Chrome 的 A=xinguomao、B=lishiyu 独立会话，结合中央只读回执复核；没有改变数据、账号、密码或权限。

## 修复与工作台复验

| 问题 | 修改及实际结果 |
| --- | --- |
| 周报被当成日报 | 在规划策略和工具说明中要求澄清。A 输入“用数据库统计我2026年8月31日至9月6日的周报”，实际询问周报类型还是一周日报汇总，没有直接查询；明确“一周的日报”后返回 3 条、3.5 小时，七天完整 |
| 数据库请求转团队接口 | 明确指定数据库时不自动改用普通接口。B 输入“用数据库分析查看刘大扬2026年9月6日的日报”，实际解释本人范围并拒绝，没有调用团队接口 |
| 失败卡一直等待确认 | 筛选条件无法核验改为 QUERY_FILTER_UNVERIFIED 业务失败。A 明确选择普通接口查询同一人员和日期，真实触发该拒绝，卡片显示“失败”，没有等待确认，也未返回日志内容 |
| 拒绝及过期提示混淆 | B 读取 A 的新结果，返回 DATA_ACCESS_DENIED，说明无权执行该操作或访问指定结果／报告；下载旧报告返回 REPORT_DOWNLOAD_EXPIRED，说明来源仍有效时重新导出，无需重新汇总 |

周报和数据库路由修复属于模型规划约束，以上是本轮真实措辞的验证，不代表所有自然语言表达都已穷举。团队普通接口仍不能确认日期筛选生效，因此保持拒绝；本次修复的是错误分类及任务状态，没有绕过筛选保护。历史任务卡不回写。

## 回执及检查

- 周报澄清 dispatch：`430f3896-412f-4891-8677-4b1df5f37598`；数据库他人查询拒绝 dispatch：`9cdc4dcc-ac30-4237-8040-b47062381635`。两者均完成，未产生对应业务查询。
- 明确日报后的汇总 operation：`e127dee8-5947-4a99-8743-fffd69590db7`；结果 `e157f1ac7d6c4b9e8c6b4cce44dc9bbc`。
- B 跨用户读取拒绝：`758a87e9-4eca-44a4-b221-05ecbfaddbf5`。
- 普通团队查询筛选拒绝：`5627b164-2089-4cc6-b589-cf49bf14f964`。
- 旧报告入口过期：`fad2ed96-89bc-4403-b26a-f2e41b9f4042`。
- 分析、导出、适配器和工具目录回归：74 项通过；另定向运行日期／部门筛选拒绝及中央任务终态 2 项通过（与全套有重叠，不累加）。导出测试同时验证来源仍可读取、重新导出和新下载成功。
- 发布 smoke 成功，151 项工具、必需工具完整；生产数据库预检 passed、strict、warnings=[]。

脱敏回执保存在工作站 `output/workspace-repairs-20260911.json`。本次只完成所列产品问题的技术复验；手工方案的未触发、管理员配置、浏览器保存等剩余子项仍按原记录，最终验收由用户签收。

## 另发现的连接问题

12:50—12:53 及 12:59—13:00 部分请求在宿主接收前超时，没有进入业务系统。远端旧 SSH 会话仍占用 127.0.0.1:18789，本地重连反复报告 remote_forward_conflict。核对来源地址、密钥指纹和转发监听进程后，仅结束本工作台的残留会话，现有守护程序自动重连，随后业务复验完成。

这是两次连接恢复，不能称为根因修复；旧会话为何未及时退出及自动回收机制仍待处理。它与浏览器下载 ERR_BLOCKED_BY_CLIENT 是不同现象，不能据此断言 Chrome 扩展阻止了下载。

## 连接稳定性复验及用户确认（13:44 后）

用户已确认文件下载保存测试通过，其余验收补项暂保留。无需再将文件落盘列为未验证。

当前新 SSH 会话实测生效 ClientAliveInterval=5、ClientAliveCountMax=2。13:43:56 暂停经命令行核对的本机隧道进程 9512，模拟客户端不响应；13:44:08 服务端记录 Timeout, client not responding，旧监听自动释放。恢复进程后，现有守护程序自动建立新进程 15660，13:44:30 状态 connected，远端 /readyz 返回 ready=true。没有手工结束远端 SSH 会话，没有业务写入。

已修复安装脚本的 Windows 换行兼容问题，并将验证从文件内容扩展到 sshd -T 的实际匹配配置；生产重新安装验证成功。安装输出明确提示已有会话需重建，配置说明明确 Match User root 影响所有 root SSH 会话，并非仅某个密钥。reload 不会把新保活参数补到已经建立的旧连接上。

原残留会话已经退出，当前连接的自动回收及重连已通过故障注入验证。此前两次具体断连触发原因无法由现存日志完全还原，本次不声称穷举所有网络故障；已验证的半开连接不再依赖人工清理端口。

## 最终收尾确认

2026-09-11，用户在确认文件下载保存已测试、其余验收补项暂保留后，认可连接恢复验证结果并要求更新收尾文档。一期按该确认范围收尾。历史失败、未执行和未触发记录保留；不将暂保留项写成通过，不扩展两用户本人分析边界。现行结论见[一期结项说明](../../系统适配/泰华日志系统/数据库分析一期结项说明.md)。
