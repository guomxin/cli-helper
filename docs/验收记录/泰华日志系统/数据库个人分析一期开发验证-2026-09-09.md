# 泰华日志数据库个人分析一期开发验证

日期：2026-09-09。范围：一期 A 本地实现及生产只读启用门槛检查，不是生产业务发布验收。

## 实现交付

本人 DAILY 汇总、每日分布和历史结果读取已接入中央能力与 MCP。新增独立 Scope，受保护结果仅在重新授权后解密交付。Operation/任务历史保存结果引用，持久计划和导出未开放。详见[开发与启用说明](../../系统适配/泰华日志系统/数据库个人分析开发与启用说明.md)。

## 真实数据库检查

17:30—17:40 使用用户已授权的生产只读账号，通过隐藏标准输入向临时进程提供密码；没有保存密码、创建连接配置、修改数据库权限或授予用户 Scope。

首次元数据查询暴露 PostgreSQL 求值顺序问题：权限函数可能在 relkind 过滤之前求值，返回 SQLSTATE 42809（非序列对象被传给序列权限函数）。将序列及表权限检查改为 CASE 保护后重新执行，通过真实数据库语法与求值验证。

17:33:08 检查结果如下，17:39:57 使用最终默认驱动连接器再次复验一致：

| 检查 | 结果 |
| --- | --- |
| 显式只读事务 | true |
| REPEATABLE READ | true |
| 工作日志主键 | 符合 |
| 特权角色标志 | false |
| 表/列写入权限 | false |
| TEMP | true |
| public 序列 USAGE | true |
| 新执行器启用门槛 | 按预期拒绝，DATASOURCE_POLICY_REJECTED |

另外在独立只读事务中验证了同一实现的必需列元数据、空 ID 参数的行投影及聚合 SQL：字段类型匹配，业务行数 0、日志计数 0、登记工时 0。未读取其他用户记录，也未扫描日志正文。所有连接显式 rollback/close。证据位于本地 `output/analytics_live_gate.json`；临时验证脚本不含密码。

事务默认只读值受客户端启动选项强制设置，本次结果不能替代对角色原始默认设置的管理员审计。

## 自动化覆盖

新增分析测试覆盖输入边界、ID 精度、完整性与分页、空窗口、Decimal、只读固定 SQL 参数、数据变化、异常脱敏、跨用户历史读取、会话版本、过期、幂等重读、中央 MCP Scope、登录单次续办、撤销 Scope 后不续办、任务取消和连接释放。数据库故障/取消测试使用可控驱动替身；真实 PostgreSQL 仅完成上述无业务行读取检查。

完整 pytest 复验：945 passed、4 skipped、826 subtests passed，耗时 511.68 秒。最终分析、治理与文档定向回归：57 passed，其中分析专用测试 34 项；OpenClaw 插件测试：194 passed。编译、依赖检查和 Workspace JavaScript 语法检查通过；运行日志保留在本地 output 目录。

## 未完成的生产门槛

当前账号仍因 TEMP 和序列 USAGE 被阻断；认证表范围需要数据库所有者继续审查。未开放普通用户、未完成本次实现的真实非空本人窗口与普通用户对账、未验证真实 PostgreSQL 执行中取消、未验证 Linux 部署。不能把模拟测试或上一次三条日志的历史对账当成这些项目通过。

本轮未部署中心服务、重启 Gateway 或启用数据源。完成权限收敛与真实试点验收后，再按现有发布流程启用一期 A。周期比较、CSV、团队直读仍属后续阶段。
