# 四系统试点收口一期结果

> 验收日期：2026 年 8 月 22 日
>
> 结论：有条件通过
>
> 条件项：第一用户当前 OA 待办为 0，本轮未取得可授权的真实接收处理样本；该项保留为后续补测，不影响四系统读取、多用户隔离和运行基线收口。

## 一、验收范围与安全边界

本轮围绕 Seeyon OA、泰华日志系统、语雀部门信息库和照明实验室测试系统进行试点收口，覆盖：

- 本地全量自动化和发布治理脚本；
- 线上 AgentBridge、管理端、Workspace、MCP 和 OpenClaw 健康；
- 两个真实用户的身份、Endpoint、系统会话和 Workspace 隔离；
- 四系统代表性真实读取；
- 照明系统一期至四期全部读取能力和 CSV 文件交付；
- 四系统保活周期和当前待投递状态。

本轮没有执行 OA、泰华、语雀或照明系统的业务写入，没有读取第二用户 OA 待办，也没有触发照明设备控制。

## 二、版本与运行基线

| 项目 | 结果 |
| --- | --- |
| 本地收口候选 | 本文档所在提交；仅包含验收工具、测试和文档改进 |
| 线上 AgentBridge Release | `53b0c9b7b59a` |
| 线上服务 | `agentbridge.service` 为 `active`，进程号 `1120901` |
| MCP 工具目录 | 114 个，必需工具齐全 |
| OpenClaw | CLI 与 Gateway 均为 `2026.7.1`，深度 RPC 正常 |
| OpenClaw 插件 | `agentbridge-interactions` `0.4.53`，已加载，无版本漂移 |
| 管理端与 Workspace | 健康检查均为 `ok` |
| 当前活动任务 | 0 |
| 当前待投递或延迟投递 | 0 |
| 身份隔离违规 | 0 |

线上 Release 与本地收口候选不同是预期状态：本轮修改不涉及 AgentBridge 运行代码，只增强发布验收脚本、照明冒烟工具、自动化测试和文档，因此没有重启或重新部署线上服务。

## 三、自动化与发布验收

### 3.1 本地自动化

- Python 全量测试：627 个通过、3 个跳过、419 个子测试通过；
- Python 依赖：`pip check` 无损坏依赖；
- Workspace Gateway：21 个测试通过；
- OpenClaw 插件：142 个测试通过；
- OpenClaw 插件打包检查：通过；
- 部署资产和照明冒烟新增检查：16 个测试通过；
- `agentbridge-mcp-smoke.mjs` 语法检查：通过。

### 3.2 发布验收脚本改进

首次执行时，OpenClaw 深度 RPC 在 45 秒边界内未返回。单独复核后确认 Gateway RPC 正常，慢点发生在 OpenClaw CLI 启动期间的外部通道网络探测，日志中可见 Telegram 连接超时，不是 AgentBridge 业务调用失败。

本轮完成两项治理修正：

1. 深度检查默认时限从 45 秒调整为 90 秒；
2. 插件检查去除会启动长驻运行态活动的 `--runtime`，改为一次性加载状态检查。

修正后双用户在线发布验收完整通过。发布和部署脚本保持一致，避免日后再次把外部通道启动延迟误判为 Gateway 故障。

## 四、四系统会话与代表性读取

| 系统 | 身份 | 会话 | 真实读取结果 | 判定 |
| --- | --- | --- | --- | --- |
| Seeyon OA | 辛国茂 / `xinguomao` | `active`，可保活 | Workspace 读取待办，结果为 0 条 | 通过，真实空结果 |
| Seeyon OA | 李世玉 / `lishiyu` | 重新登录后 `active`，可保活 | 只检查会话，未读取待办 | 通过，符合第二用户边界 |
| 泰华日志 | 辛国茂 | `active`，可保活 | Workspace 读取今日日志，结果为 0 条 | 通过，真实空结果 |
| 语雀部门信息库 | 辛国茂 | `active`，可保活 | 返回 4 个可访问知识库 | 通过 |
| 照明实验室 | 辛国茂 | `active`，可保活 | 返回控制箱、灯杆和运行概览 | 通过 |

语雀本轮返回的知识库为：空间指南、会议记录汇总、团队建设和共享文档。Workspace 一次自然语言四系统只读检查完成后，明确说明没有查看详情、没有执行写操作。

## 五、双用户 Workspace 与隔离

浏览器中同时存在两个已登录 Workspace，验收前先读取页面头部身份并固定映射：

| Workspace | `userSubject` | 下游 OA 主体 | 验收内容 |
| --- | --- | --- | --- |
| 第一用户 | `xinguomao` | 辛国茂 | 四系统代表性只读检查 |
| 第二用户 | `lishiyu` | 李世玉 | 仅检查 OA 会话状态 |

结果：

- 两个 Workspace 均完成自己的请求，导航处活动任务均回到 0；
- 第一用户结果中未出现第二用户主体或业务内容；
- 第二用户未读取 OA 待办、未触发任何业务写入；
- 四个活动 Endpoint 与预期一致：第一用户 Workspace + Telegram，第二用户 Workspace + 微信；
- 当前无待投递、延迟投递或身份一致性违规。

管理数据中仍保留第二用户微信历史失败投递 13 条，但均不是当前 outstanding 项，也没有阻塞本轮任务。它们属于历史通道故障证据，应在后续治理中按时间和终态展示，不能与当前失败混算。

## 六、照明系统整体读取验收

### 6.1 能力覆盖

通过真实 MCP 会话执行了照明系统 24 个正式读取能力和 2 个兼容入口，覆盖：

- 系统概览、控制箱、RTU、灯杆、单灯运行状态；
- 综合告警、单灯告警、告警分析、告警备注；
- 能耗记录和能耗分析；
- 灯杆巡测、RTU 巡测、真实支路漏电；
- 巡检任务、执行中巡检、巡检日志和任务详情；
- 检修记录和资产详情；
- 四期 CSV 报告与文件下载。

真实结果包含成功数据和真实空结果。灯杆巡测、RTU 支路漏电、非工作时段当前告警等查询返回 0 时，系统保留为明确空结果，没有伪造成异常或示例数据。

### 6.2 关键数据

- 系统概览：控制箱 36 个、可搜索灯杆 132 基、地图明细灯杆 117 基；
- RTU：29 台，其中离线 27 台、断电 1 台、停用 1 台；
- 单灯控制器：119 台，当前离线 119 台；
- 灯杆：117 基，灯源 179 盏；
- 最近 30 天单灯告警：12 条；
- 综合告警总量：5710 条，最新发生时间存在 2 条并列候选；
- 巡检任务：112 条，当前执行中 3 条；
- 检修记录：指定历史日期范围返回 1 条。

### 6.3 页面核对

使用用户已经登录的 Chrome 页面进行只读核对。照明系统“运行状态”页面显示：

- 灯杆总数 117 基；
- 单灯控制器总数 119 台；
- 离线单灯控制器 119 台；
- 页面列表共 117 条记录。

这些数字与 MCP 的地图明细和单灯运行结果一致。页面同时显示登录用户 `yanshi`，未执行批量巡测、导出、控制或其他写操作。

### 6.4 文件交付

四期 7 类 CSV 均成功生成并下载：

| 报告 | 数据行 | 结果 |
| --- | ---: | --- |
| 能耗记录 | 203 | 成功 |
| 能耗分析 | 0 | 成功，只有表头，符合当前无有效趋势数据 |
| 灯杆巡测记录 | 0 | 成功，真实空结果 |
| RTU 支路漏电告警 | 0 | 成功，真实空结果 |
| RTU 支路漏电分析 | 0 | 成功，真实空结果 |
| 巡检日志 | 13 | 成功 |
| 检修记录 | 1 | 成功 |

文件均为带 UTF-8 BOM 的 `text/csv`，无截断。验收辅助脚本原先把“当前页 5 条”误写成“总计 5 条”，本轮已修正为优先使用下游总数；复测后巡检日志正确显示当前页 5 条、总计 13 条。

## 七、保活与稳定性

只读检查线上最近约两小时的 keepalive 日志，共取得 12 个周期：

- 前 11 个周期：活动会话 4、可保活 4、成功保活 4；
- 第二用户 OA 登录恢复后的最近周期：活动会话 5、可保活 5、成功保活 5；
- 所有周期的明确过期、连续 deferred、租约外和 cycle failed 均为 0。

该结果证明当前周期正常，不表示下游永不注销。下游明确失效时，AgentBridge 仍应进入过期并在业务请求中生成可信登录卡。

## 八、未完成项与下一阶段入口

### 8.1 本轮条件项

第一用户当前 OA 待办为 0，因此无法选择真实接收处理样本。本轮没有为了追求“全通过”而制造流程，也没有使用第二用户待办替代。

后续出现合适待办时，按“读取详情 -> 专用能力识别 -> 精确授权 -> 单次提交 -> 权威回读 -> 跨端终态”补测，并把结果追加为新的带日期验收记录。

### 8.2 后续建议

1. 等待第一用户低风险真实待办，补齐 OA 接收处理样本；
2. 对历史微信失败投递做按时间、终态和是否仍需处理的治理展示；
3. 继续观察 OpenClaw 外部通道网络对 CLI 冷启动耗时的影响，但不改用户网络或代理；
4. 保持照明系统现有能力边界，暂不进入读取五期或通用设备控制；
5. 下一系统接入仍采用“先读、再低风险可逆写、最后受控不可逆写”的顺序。

## 九、最终判定

四系统中心会话、真实读取、双用户 Workspace 隔离、OpenClaw/MCP 发布链路、照明一期至四期读取和文件交付均达到试点一期收口标准；没有发现跨用户串用、未授权写入、当前卡死任务或当前待投递积压。

因此本期判定为“有条件通过”。唯一条件项是缺少第一用户 OA 真实接收处理样本，待真实业务条件出现后单独补测。
