# 照明系统二期 Workspace 真实验收报告

## 一、验收结论

- 验收日期：2026-08-12
- 验收入口：Agent Workspace 网页端
- 验收身份：第一用户辛国茂
- 目标系统：照明实验室测试系统
- 安全边界：只读查询、分析和 CSV 导出；业务写入 0 次
- 结论：**有条件通过**

照明二期的资产详情、告警分析、巡检进度、漏电分析和四类 CSV 报告均取得真实数据并完成网页端闭环。页面刷新恢复、任务详情、文件下载、文件自然过期后重新生成以及双用户隔离也通过。

验收发现两项需要修复的自然语言接续路由缺陷：同一句中的“任务，并继续读取”可能被误判为跨任务接续；用户在候选项后说“选择第 2 项并导出”时，`导出` 尚未被识别为允许的跟进动作。两项都不影响底层照明能力，但会影响 Workspace 的连续对话体验，因此本轮不判定为无条件通过。

## 二、运行基线

| 项目 | 验收值 |
| --- | --- |
| AgentBridge 发布版本 | `772fbdf5a201` |
| AgentBridge 服务 | `active`，近 30 分钟错误 0 条 |
| Admin / Workspace | 健康检查均为 `ok` |
| OpenClaw | CLI 与 Gateway 均为 `2026.7.1`，深度 RPC 正常 |
| AgentBridge OpenClaw 插件 | `0.4.40`，状态 `loaded`，版本漂移 0 |
| 身份与端点隔离 | 均为 `succeeded` |
| 最终发布验收 | `succeeded` |

照明系统的身份链路在本轮显示为：Workspace 账号 `xinguomao` -> AgentBridge `userSubject=guomao` -> 照明系统登录账号 `yanshi` -> 已验证下游主体“无为”。这四个字段属于不同层级，不应在界面上统称为“账号”。

## 三、用例结果

### W0 基线与会话

- 会话处于有效状态，没有重复弹出登录卡。
- 系统概览返回 36 个控制柜、131 个可检索灯杆和 116 条地图详情。
- 因会话未过期，登录后自动续办用例 W0-3 本轮不适用。
- 首次概览运行时，主模型收到一次上游 `server_error`，OpenClaw 自动回退后成功完成；Workspace 短暂展示了中间失败文案，属于展示状态问题。

### W1 资产详情与分析

| 用例 | 结果 | 关键证据 |
| --- | --- | --- |
| W1-1 RTU 前 5 项 | 通过 | 返回真实 RTU 标识、名称和状态。 |
| W1-2 首项详情 | 通过 | 正确承接“上面第 1 个”，返回继电器和回路信息。 |
| W1-3 控制柜与灯杆详情 | 通过 | 操作台账记录 2 次 `asset.search` 和 2 次 `asset.detail`。 |
| W1-4 30 天 RTU 告警 | 通过 | 2026-07-14 至 2026-08-12，共分析 4 条且未截断；类型包括电源缺相、接触器失效和反馈状态不一致。 |
| W1-5 巡检每日进度 | 有条件通过 | 独立明确请求返回“牛海亮新版计划”、4 号小组、执行中及每日计划/完成数；原始连续语句触发路由误判。 |
| W1-6 30 天漏电分析 | 通过 | 共分析 11 条且未截断；2026-08-11 为 10 条，2026-08-12 为 1 条，并返回灯杆、道路、类型和状态排行。 |

巡检数据中 2024-10-15 返回“计划 1、完成 2、完成率 200%”。智能体没有篡改下游数据，但后续应把这类结果明确标记为数据异常。

### W2 四类 CSV 报告

| 用例 | 生成文件 | 大小 | 结果 |
| --- | --- | --- | --- |
| W2-1 RTU 告警分析 | `照明RTU告警分析_20260812_224009.csv` | 876 B | 单一文件卡，浏览器实际下载成功。 |
| W2-2 漏电分析 | `照明漏电分析_20260812_225322.csv` | 1.6 KB | 单一文件卡。 |
| W2-3 RTU 资产清单 | `照明RTU清单_20260812_225418.csv` | 2.7 KB | 单一文件卡。 |
| W2-4 巡检每日进度 | `照明巡检进度_20260812_230002.csv` | 237 B | 携带完整任务条件的独立请求通过。 |

同名“牛海亮新版计划”存在两条记录时，系统能正确要求用户在“待执行/牛海亮测试巡检小组”和“执行中/4 号小组”之间选择。选择后的跟进语句包含“导出”时未进入业务能力，是本轮主要缺陷之一。

四类 CSV 的 `Content-Type`、UTF-8 BOM、非空内容、表头与数据行契约已由同一发布版本此前的真实 MCP + HTTP 验证覆盖；本轮进一步验证了 Workspace 中四张唯一文件卡及一次真实浏览器下载。

### W3 页面状态与恢复

- 四张文件卡在刷新前后均各 1 张，顺序不变。
- 刷新后 W0、W1、W2 的消息、任务和产物仍然存在。
- “查看进度”能打开对应任务，产物和事件时间线完整。
- 任务运行时输入框与发送按钮被禁用，任务完成后恢复，避免同一 Workspace 并发重复执行。
- 验收结束时 Workspace 进行中任务数为 0。

### W4 自然过期与重新生成

- W2-1 文件在自然超过 30 分钟有效期后仍保留在原位置，并显示“下载链接已过期”“已过期”和“重新生成下载”。
- 点击重新生成后，同一张卡更新为 `照明RTU告警分析_20260812_231232.csv`，大小仍为 876 B，有效期更新至 23:42。
- 卡片总数、任务总数均未增加；刷新后新文件名出现 1 次，旧文件名不再出现。
- 任务 `e8fbc876-7e76-46ab-ab75-338e36ffb083` 的创建时间仍为 22:40，更新时间变为 23:12，时间线新增“文件下载已重新生成”，证明没有创建第二个任务。
- UI 没有直接展示内部 `artifactId`，因此本轮没有逐值记录；同一任务、单一卡片和原位更新时间线支持“原产物重发”的行为结论。

### W5 双用户隔离与运行治理

- 第一用户工具目录：原始工具 88 个，授权后 56 个，其中照明工具 13 个。
- 第二用户工具目录：授权工具 30 个，照明工具 0 个。
- 第二用户直接请求照明会话状态得到预期的 `MCP_TOOL_ERROR`，没有复用第一用户会话。
- 管理端检查 565 条多端任务：照明任务均属于 `guomao / Agent Workspace: xinguomao`，第二用户 `lishiyu` 的照明任务为 0。
- 管理端进行中任务 0、待投递 0、隔离异常 0。
- 最终发布脚本确认服务、Workspace、管理端、OpenClaw、身份隔离和端点隔离全部通过。

## 四、问题与修复优先级

### P1 连续语句被误判为跨任务接续

`TASK_CONTINUATION_HINT_PATTERN` 会把“查看巡检任务，并继续读取第 1 个任务每日进度”中的“任务...继续”识别为继续历史任务，导致模型列出旧任务而不是执行当前请求。

建议把“继续”是否指向历史任务的判定与当前句内明确对象、最近助手提出的接续选项结合，避免只靠宽泛正则。

### P1 候选项后的“导出”不能继续执行

`TASK_FOLLOW_UP_HINT_PATTERN` 没有覆盖“导出/生成报告”，所以“选择刚才第 2 项并导出”被解析为不允许新业务动作的任务接续。

建议把报告导出类意图加入受控跟进动作，并保持候选项、任务 ID 和用户身份绑定，避免把任意历史任务转成导出。

### P2 模型回退过程泄露中间失败状态

主模型发生一次上游 `server_error` 后，OpenClaw 已回退到备用模型并成功返回，但 Workspace 曾显示 `The agent run failed before producing a reply.`。建议只在所有回退均失败后持久化终态失败；回退中的候选错误可进入诊断日志。

### P2 登录状态字段语义不清

AgentBridge 用户、Workspace 账号、目标系统账号和下游主体是四个概念。建议登录状态同时显示带标签的 `userSubject`、目标系统账号和已验证主体，避免把 `guomao` 或 `yanshi` 单独称为当前“账号”。

### P2 下游数据异常提示

完成率超过 100% 时应保留原值，同时显示“下游数据异常，建议核对”，不能静默纠正或当作普通进度。

## 五、最终判断

照明二期底层能力和 Workspace 文件闭环已经具备真实使用条件；本轮没有发现业务调用重复、文件卡重复、长期运行任务、身份串用或服务端错误。修复两项 P1 路由问题并做针对性回归后，可以升级为无条件通过。

后续回归最小集合：

1. 原样执行“查看巡检任务，并继续读取第 1 个任务每日进度”。
2. 在同名任务候选列表后执行“选择第 2 项并导出每日进度 CSV”。
3. 模拟主模型失败、备用模型成功，确认 Workspace 不展示中间失败终态。
4. 检查登录状态的三层身份标签和巡检异常数据提示。
