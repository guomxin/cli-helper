# 照明实验室测试系统二期 B 验收报告

## 一、验收结论

照明系统二期 B 的 CSV 报告导出能力通过验收。中心服务、MCP 工具目录、OpenClaw
插件和 Workspace 文件卡已接通；四类报告均通过第一用户真实只读调用和文件下载校验。

本次验收没有执行设备控制、告警处置、巡检打卡或其他下游业务写入。

## 二、发布信息

- 中心服务发布：`9cd5969c2bc3`
- OpenClaw 插件：`0.4.40`
- MCP 原始工具数：88
- 第一用户可见工具数：56
- 新增工具：`smartlight_report_export`
- 服务运行状态：active
- 发布验收时最近 30 分钟服务错误数：0

## 三、真实报告验收

验收身份为辛国茂，时间为 2026-08-12。每次调用均重新读取照明系统当前数据，随后
从 AgentBridge 的短时下载地址读取文件并校验 MIME、UTF-8 BOM 和有效期。

| 报告类型 | 真实条件 | 数据行 | CSV 行数 | 文件大小 | 截断 |
| --- | --- | ---: | ---: | ---: | --- |
| 告警分析 | 最近 30 天 | 4 | 5 | 876 B | 否 |
| 漏电分析 | 最近 30 天 | 11 | 12 | 1602 B | 否 |
| 资产清单 | RTU | 29 | 30 | 2809 B | 否 |
| 巡检进度 | 任务 `fbd3be5143c5425b956791a9aabb0c7d` | 8 | 9 | 246 B | 否 |

四份文件均满足：

- 响应类型为 `text/csv`；
- 文件名为中文业务名和生成时间；
- 文件包含 UTF-8 BOM，可由中文 Windows/Excel 直接识别；
- CSV 行数等于数据行数加一行表头；
- 生成后剩余有效期为 1800 秒；
- 下载地址和文件内容未写入验收文档。

## 四、自动化验证

- Python 全量测试：573 项通过，3 项按环境条件跳过；
- OpenClaw 插件测试：130 项通过；
- Workspace 专项测试：28 项通过；
- 插件打包检查：通过，包版本 `0.4.40`；
- Python 编译检查和 Git diff 检查：通过；
- 发布后管理端、Workspace、MCP、Gateway RPC 和插件加载检查：通过。

新增固定 smoke check `SmartlightReport`。该检查会真实调用报告工具、下载 CSV，并验证
文件契约和 30 分钟时效；发布必备工具清单也已加入 `smartlight_report_export`。

## 五、仍由自动化覆盖的边界

历史文件卡过期后的“原位重新生成”通过中心服务和 Workspace 自动化测试验证：旧卡保留
为已过期状态，重新生成使用原查询条件读取当前数据，复用原 `artifactId` 并替换下载源。
本次未在生产环境人为修改数据库或等待 30 分钟制造过期记录。

CSV 公式注入防护、500 行上限、跨用户拒绝和重新生成身份绑定均由自动化测试覆盖。
