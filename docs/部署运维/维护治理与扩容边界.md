# 维护治理与扩容边界

## 代码边界

业务写入名称、字段 Schema、函数绑定和权限表集中在 `bscli/core/write_catalog.py`；中央服务不再通过自身全局符号查找业务函数。新增业务先注册绑定，再补模拟测试。

`controlled_write_executor.py` 承担可信字段准备与单次授权提交；它与中央服务共享同一组账本、锁和事务，不创建新的数据库提交边界。`login_recovery.py` 只恢复账本中原用户、原任务、原系统的读取与精确参数。OpenClaw 的 `read-continuation.js` 负责读取描述及展示，原任务事实仍由中央账本决定。计划执行继续由已有 `task_plan_runtime.py` 管理；通知领取、投递和确认仍集中在中央服务，后续可在同一回归保护下继续拆分，不以拆小文件为完成标准。

保护契约：取消不得穿过授权消费边界，结果未知不得重放，普通读取各有任务，登录恢复保留原任务，重复通知不得重复业务执行。M4 的真实 Node—Python—SQLite 测试与受控写边界测试共同保护这些行为。

## 当前事实和发布检查

执行 `python scripts/current_facts.py --write` 更新当前状态的生成区，`--check` 检测漂移。版本来自插件 package.json，数量来自实际构造的注册表与 MCP；不推断已经部署或已经通过真实业务验收。历史验收数字仍属于各自日期。

发布准备先核对实际运行 Release 与 `deploy/release-policy.json` 的 compatibleFrom，并审查数据兼容性；不能仅沿用上一轮声明。该声明和实际 SQLite schema 比较共同决定能否切换与程序回退，不允许为通过发布而使用任意版本通配。

正式发布门禁：本地固定候选全量验证成功，且相同提交推送到 `candidate/<完整提交号>` 后的 GitHub `candidate-validation` 成功 → 同一已验证 wheel 部署 → 在线验收 → 推送 main。两套独立验证可并行准备，正式入口仍逐项确认两个结果后才允许部署。CI 不接收生产凭据，也不负责部署。重复运行只接受该提交、候选分支、push 事件、指定工作流的最新执行；失败、取消、跳过或无法读取结果都阻断部署。`-SkipValidation` 仍必须匹配本地完整回执，不能跳过候选 CI。

仓库管理员还须为 main 启用必需状态检查 `candidate-validation`，要求分支最新、禁止强推与删除、对管理员同样执行；不要设置绕过主体。先让候选 CI 实际运行并确认发布顺序，再启用规则。CI 文件和本地门禁不能替代远端分支保护；规则生效要单独读回核验。2026-09-21 开工时 main 未保护；后续已启用经典分支保护并保存后读回确认：必需 candidate-validation（GitHub Actions 来源）、要求分支最新、禁止管理员绕过、禁止强推和删除。历史材料盘点与规则证据见[补验记录](../验收记录/主分支保护与历史材料盘点-2026-09-21.md)。文档提交也先通过候选检查再推送 main，但不部署。配置含义参见 [GitHub 必需检查说明](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)；本地部署门禁仅接受 success，比平台允许的 skipped/neutral 更严格。

## GitHub 检查分级

必需检查名保持 `candidate-validation`。`scripts/ci_validation_plan.py` 在完整 Git 历史中确定比较基线，并通过 GitHub Actions API 核对该基线的检查证据；候选分支比较与 main 的共同祖先，PR 比较与目标分支的共同祖先，main 直接推送比较事件中的前一提交。比较整个候选差异，不仅检查最后一次提交；重命名按删除和新增处理。

| 差异范围 | GitHub 检查 |
| --- | --- |
| 仅 docs 下 Markdown 或根 README.md | 文档结构、相对链接、当前事实一致性和既有公开内容检查 |
| 仅上述文档及 deploy/release-policy.json | 增加部署资产、发布事务与制品专项测试；按运行平台保留条件跳过 |
| 运行代码、测试、依赖、工作流、分级脚本、systemd 配置及其他路径 | 原有完整检查 |
| 基线没有成功证据、历史不完整、API 不可用、空差异 | 完整检查兜底 |

同一提交从 `candidate/<完整提交号>` 推送 main 时，main 的检查仍运行，但可复用同仓库、相同 SHA、指定工作流、push 事件的最新候选成功记录。较新的失败、取消或未完成记录不能被更旧的成功覆盖。PR 的合并提交不能借用不同 SHA 的结果。手工 workflow_dispatch 或重跑工作流一律强制全量，以便主动复核环境和依赖变化。

每次执行将 profile、基线、全部差异路径、依据运行链接写入 GitHub 步骤摘要及 `ci-plan.json` 产物。只赋予 actions:read 读取证据，不提供生产凭据。API 条件参见 [GitHub 工作流运行接口](https://docs.github.com/en/rest/actions/workflow-runs#list-workflow-runs-for-a-repository)。

本轮只对 GitHub 检查分级；正式应用发布的本地固定提交全量、wheel 绑定、兼容性与在线验收门禁保持原样。文档和 CI 流程提交通过候选检查后推送主分支，不触发应用部署或 OpenClaw 重启。局部运行代码尚无完备依赖映射，仍按全量处理，不声称已经自动精准选择全部相关测试。

### 分级检查上线证据（2026-09-21）

规则提交 `04053670cb20e09ed141ac088b5cc5cf89668d2b` 的[候选检查](https://github.com/guomxin/cli-helper/actions/runs/35562768689) 从已验证的 `9260e81` 比较完整差异，正确选择 full：Python 1073 通过、11 跳过、1256 子测试通过，Workspace 33、OpenClaw 204 及安装包检查通过；验证步骤用时 640.63 秒。同一提交推送 main 后，[主分支检查](https://github.com/guomxin/cli-helper/actions/runs/35563582008) 选择 reuse，核对上述候选成功记录后通过，整个 job 为 18 秒，未重复全量。耗时仅为本次记录，不是固定性能承诺。

本地分级、候选证据及真实配置校验共 31 项通过；此前相关发布和文档专项 65 项通过、6 项按 Windows 条件跳过、888 子测试通过。真实 Git 多提交及重命名用例证明不会只看末次文档提交。使用上一轮实际 `c11e617 → 9260e81` 历史及在线成功证据回放，正确识别为 release（兼容性声明和两份文档），不是 full；该回放不冒充在 GitHub 执行过发布专项。

保留失败记录：中间候选 `27eefe1` 在英文 Windows 的 cp1252 控制台打印中文路径时报错，未通过、未推送 main。修复为控制台 ASCII 转义、产物和摘要仍保持 UTF-8，并增加同环境编码回归后，最终候选通过。本轮未部署应用或重启 OpenClaw。此段结果文档作为后续 docs 分支的实际验收输入，结果以该文档提交的 GitHub 检查摘要为准。

## 环境配置与公开内容

`deploy/systemd` 是通用示例，使用回环监听及 `.example.test` 下游；默认 TLS，不继承照明明文例外。实际三个运行/备份单元放在仓库外，由 `agentbridge.environment.v1` 配置文件列出 host、root、各单元绝对路径和 SHA256。

发布必须显式传入 `-EnvironmentProfile <仓库外配置文件>`，也可由受控运行环境设置 `AGENTBRIDGE_ENVIRONMENT_PROFILE`。PlanOnly 展示配置路径及单元摘要；目标、文件位置或摘要不符立即拒绝，读取后固定本次使用的字节。配置含真实端点，不能提交仓库；私钥、会话密文、业务结果和完整运行日志也不得放入公开 CI 产物。

原环境配置迁移须先原样复制并核对哈希，再将模板脱敏。服务器仍通过原发布事务安装、核验、切换和回退。应用恢复点包含业务连接配置，不把它说成 systemd 单元的完整副本；外置发布清单与三个单元须独立备份。本轮已在既有独立备份节点保存一次副本并核对摘要（不含解密密钥）；配置变更后必须更新该副本。历史文档和运维辅助脚本尚含真实布局，不宣称整个 Git 历史已经脱敏，不重写历史。每次公开发布仍需人工复核业务内容；高置信凭据扫描通过不能替代此项。

## 传输例外清单

| 链路 | 数据及限制 | 已有控制 | 尚缺证据 | 责任角色与复核 | 退出条件 |
| --- | --- | --- | --- | --- | --- |
| 既有照明系统 HTTP | 中央会话 Cookie/JWT 及已开放业务请求；仅既有试点 | 显式不安全传输开关，适配器权限及受控写规则 | 未证明链路有 VPN/专线等额外加密，不视为传输已保护 | 下游系统与部署维护者；2026-10-01 前及扩大范围前复核 | 提供 HTTPS 与可信证书，完成登录、只读、授权链路回归后移除 HTTP 开关 |
| 既有日志数据库 TLS 关闭例外 | 只限代码中精确旧源；账号、查询和报表仍按既有授权 | 固定旧源匹配、只读事务、关系及函数白名单 | 未证明网络补偿措施，未完成数据库证书改造 | 数据库与部署维护者；2026-10-01 前及接入高敏数据前复核 | 服务端 TLS、主机名验证与可信 CA，连接预检和只读回归通过后删除例外 |

责任角色是工作分工，尚未确认具体责任人。上述两项保留为未关闭例外；不能将登记本身算作安全验收通过。证书错误不能自动降级成明文。扩大用户、数据敏感度或外部暴露前，须落实保护方案并复核，不自动沿用试点结论。

## 资源预算及容量限制

| 层级 | 预算 | 超限行为 |
| --- | --- | --- |
| Auth/Admin/Workspace 每个 HTTP 服务 | 最多 64 个处理连接、每来源 IP 16 个、监听队列 64 | 在线程创建前拒绝新连接，不信任转发头改变来源 |
| TLS 握手 | 10 秒 | 关闭连接并归还额度 |
| 请求头/请求体读取、普通响应写入 | socket 空闲 30 秒；连接绝对寿命 360 秒 | 超时断开；慢速持续发送不能无限延长寿命 |
| 请求体 | Admin 64 KiB，Workspace 普通请求 128 KiB，聊天 18 MiB；Auth 沿用既有限额 | 沿用既有长度检查，认证/CSRF/Origin 边界不变 |
| Workspace SSE | 每已认证 user_subject 最多 6 路，写入超时 30 秒，连接寿命 360 秒 | 第 7 路返回 429；退出和断流释放额度；事件/时间线仍按既有 25 秒轮换重连 |
| MCP HTTP 协议 | 连接 128、每来源 64、队列 64、keep-alive 5 秒；请求头 10 秒、请求体 60 秒；写阻塞 30 秒 | 连接超额或输入/输出超时断开；不计尚未完成的 TLS 握手 |
| MCP 认证请求 | 每真实用户 8、全局 96 个活动请求；多个令牌共用用户额度；POST 最大 18 MiB | 超额 429，不排队；超大请求体 413，业务不执行；退出释放 |

连接额度不等于每用户业务执行预算。来源 IP 限制对共用 NAT 的用户合并计数，应在负载验收后调整。到达全局额度时健康检查也可能被拒绝；不能宣称抵御了分布式耗尽。MCP 已补请求头/请求体预算和按身份拒绝策略，不自动排队或重放。Windows/Linux 隔离容量基准与限制见[MCP 容量补验](../验收记录/协议资源预算与隔离容量补验-2026-09-21.md)。真实混合业务负载、TLS 握手入口容量和跨实例治理仍应在扩大试点前核验；新预算的隔离测试与生产健康验收分别记录。

2026-09-21 用户确认不开展历史公开材料脱敏；该项从本轮实施范围移除，不标记为已脱敏。两条下游 TLS 保持现状。
