# AgentBridge 当前内网部署

> 状态：现行运行手册
>
> 更新日期：2026-08-23
>
> 当前 Release：以管理控制台和 `Test-AgentBridgeReleaseAcceptance.ps1` 的在线回读为准
>
> 历史部署演进见 [归档记录](../历史归档/部署演进记录-2026年7月至8月.md)。

本文只保留当前可执行的部署和运维事实，不再追加逐日开发流水账。

## 1. 运行拓扑

```text
Telegram / 微信 / Agent Workspace
                |
                v
        OpenClaw 2026.7.1（Windows）
                |
                | HTTPS MCP / Task Hub
                v
AgentBridge（Linux 10.10.50.213）
  |- 8780 可信卡片
  |- 8781 noVNC 网关
  |- 8782 管理控制台
  |- 8783 Agent Workspace
  `- 8790 Streamable HTTP MCP
                |
                v
       OA / 泰华 / 语雀 / 照明系统
```

Agent Workspace 需要模型执行时，Linux 服务连接 `ws://127.0.0.1:18789`。该端口由 Windows
工作站主动建立的反向 SSH 隧道映射到本机 OpenClaw Gateway `127.0.0.1:18789`，不要求服务器
访问工作站当前局域网 IP。详细进程和排障见
[Workspace 反向 SSH 隧道](./工作台反向安全隧道.md)。

## 2. 主机与目录

| 项目 | 当前值 |
| --- | --- |
| Linux 主机 | `10.10.50.213` |
| 部署根目录 | `/home/guomao/agentbridge` |
| systemd 服务 | `agentbridge.service` |
| 运行身份 | `agentbridge:agentbridge` |
| Python 环境 | `/home/guomao/agentbridge/venv` |
| 持久数据 | `/home/guomao/agentbridge/data` |
| 备份目录 | `/home/guomao/agentbridge/backups` |
| 受限配置 | `/home/guomao/agentbridge/config` |
| 版本制品 | `/home/guomao/agentbridge/releases/<release-id>` |
| 每日备份 | `agentbridge-backup.timer` / `agentbridge-backup.service` |

服务使用版本控制中的 [systemd unit](../../deploy/systemd/agentbridge.service)。不要只在服务器上
临时修改 `/etc/systemd/system/agentbridge.service`；启动参数变化必须先进入仓库并通过验证。

## 3. 网络入口

| 地址 | 用途 | 调用者 |
| --- | --- | --- |
| `https://10.10.50.213:8780` | 可信登录、字段填写和授权 | 浏览器、Telegram/微信内嵌卡片 |
| `https://10.10.50.213:8781` | 按挑战开放的 noVNC | 可信卡片 |
| `https://10.10.50.213:8782` | 管理控制台 | 管理员浏览器 |
| `https://10.10.50.213:8783` | Agent Workspace | 普通用户浏览器 |
| `https://10.10.50.213:8790/mcp` | MCP | OpenClaw 或其他 MCP 宿主 |

所有固定入口使用内部 CA 签发、包含私网 IP SAN 的证书。客户端只安装根证书；内部 CA 私钥不
部署到 Linux。8781 的 RFB/CDP 后端只监听回环地址，没有活动挑战时不得存在可用浏览器路由。

## 4. 服务安全基线

- systemd 启用 `NoNewPrivileges`、`PrivateTmp`、`ProtectSystem=strict` 和 `UMask=0077`；
- 配置目录只读，数据目录是唯一持久可写区域；
- Linux 会话使用外部 32 字节密钥和 AES-256-GCM，不允许明文 Cookie 回退；
- 每个 MCP Token 服务端绑定 `userSubject`、允许系统、scope 和预期下游身份；
- 凭据、Cookie、Bearer Token、业务字段和卡片控制值不进入模型或普通聊天；
- Smartlight 当前目标地址为 HTTP，只有中心服务到指定目标的适配器被显式允许，不开放通用 HTTP 工具；
- 旧 Chrome 扩展、浏览器桥和 localhost daemon 不得作为故障回退路径。

## 5. 会话与保活

当前启动参数：

```text
--session-keepalive-interval 600
--session-keepalive-lease 604800
```

即每 10 分钟检查仍处于 7 天活动租约内的会话。用户登录和真实请求会更新活动时间，后台探测
本身不会无限延长租约。OA、泰华、语雀和照明系统各自采用适合下游的 Cookie、Token 刷新或
CAS/JWT 再签策略；下游明确注销、身份不匹配或不可恢复的认证失败会使会话过期并重新要求登录。

## 6. OpenClaw 基线

| 项目 | 当前值 |
| --- | --- |
| OpenClaw CLI/Gateway | `2026.7.1` |
| AgentBridge 插件 | `0.4.74` |
| Gateway | Windows 本机 `127.0.0.1:18789` |
| AgentBridge MCP | `https://10.10.50.213:8790/mcp` |
| 可信卡 Origin | `https://10.10.50.213:8780` |

Gateway 与 Workspace 反向隧道均由 Windows 任务计划托管。当前自愈基线为：登录触发、错过触发
后补启动、无限运行时长、异常退出一分钟后重启、最多 999 次、忽略重复实例；Gateway 登录后延迟
20 秒启动，避免开机资源竞争。Gateway 生命周期控制器与 Guard 还共享带心跳的启动租约，发布、
人工重启或故障恢复正在进行时，Guard 不再按旧进程年龄发起第二次重启；租约异常遗留会自动失效。
详见[本机宿主自愈与可靠性演练](./本机宿主自愈与可靠性演练.md)。

Telegram、微信和 Workspace 通过插件的 `identityBindings` 映射到各自的 AgentBridge Token。
不得同时保留一个面向所有用户的共享 Bearer 工具面。详见
[多用户身份路由](../架构设计/智能体宿主多用户身份路由.md)。

## 7. 开发、发布与推送

日常验证入口：

```powershell
.\scripts\Invoke-AgentBridgeValidation.ps1 -Mode Targeted
.\scripts\Invoke-AgentBridgeValidation.ps1 -Mode Full
.\scripts\Test-AgentBridgeRuntimeGovernance.ps1
.\scripts\Test-AgentBridgeMcp.ps1 -Check Release
.\scripts\Test-AgentBridgeReleaseAcceptance.ps1
```

正式发布使用固定入口：

```powershell
.\scripts\Publish-AgentBridge.ps1
```

该脚本要求候选提交干净，随后执行全量验证、构建 wheel、部署版本化 Release、安装受控 systemd
unit、重启服务、执行治理与发布验收，最后通过仓库专用 SSH 配置推送 `origin/main`。需要升级插件
时显式使用脚本支持的 OpenClaw 重启选项；不要手工重复启动多个 Gateway 诊断实例。

只改文档且不影响运行时、服务 unit、部署脚本或配置时，不需要重启或重新部署 AgentBridge；仍应
运行文档链接检查和相关测试，再提交并推送。

## 8. 健康检查

零业务读取、零业务写入的基础检查：

```powershell
.\scripts\Test-AgentBridgeMcp.ps1 -Check Release
.\scripts\Test-AgentBridgeReleaseAcceptance.ps1 -SkipOpenClaw
```

完整检查再加入 OpenClaw 深度 RPC 和插件运行时状态。验收必须确认：

- MCP 必需工具存在；
- 8782、8783 就绪端点返回 `ready`；
- systemd 为 `active` 且 MainPID 有效；
- 每日备份 timer 已启用，最近备份服务成功且清单可校验；
- 线上 Release 与预期提交一致；
- 最近错误日志没有新增异常；
- OpenClaw CLI 与 Gateway 版本一致，插件为 `loaded` 且无版本漂移。

带日期的发布与治理结果进入[验收记录](../验收记录/验收记录导航.md)，不在现行运行手册中固定
某次提交。发布检查不读取业务列表，也不执行业务写入。

## 9. 运行治理与备份

运行 Trace、Span、Signal、Incident、恢复动作和 24 小时试点 SLO 保存在同一运行数据库中。
独立治理周期每 60 秒执行事件评估和汇总，不依赖某个下游系统的会话保活周期。

`agentbridge-backup.timer` 每日使用 SQLite 在线备份 API 生成数据库备份和 SHA-256 清单，默认
保留 30 天。备份校验检查哈希、数据库完整性、必要表和用户主体关联隔离；隔离恢复演练会复制
最新备份、只读打开并证明写入被拒绝。运行治理还支持按小时快照和按日汇总的七天影子观察。详见
[运行治理操作手册](./运行治理操作手册.md)。

## 10. 故障恢复

1. 先分清是 AgentBridge、OpenClaw、SSH 隧道、目标系统还是用户会话故障。
2. AgentBridge 参数或代码变化后可直接重启 `agentbridge.service`，再运行发布验收。
3. Workspace 能同步历史但不能发起新任务时，优先检查反向 SSH 隧道和 OpenClaw Gateway RPC。
4. 单一用户登录失效时只重建该用户、该系统会话，不清理其他用户会话。
5. 写操作返回 `unknown` 时停止自动重试，先通过权威列表或详情对账。
6. 发布失败时保留上一版本制品，按 [发布手册](./发布操作手册.md) 执行受控回退。

## 11. 尚未达到的生产条件

- 企业 OAuth/OIDC、OBO 和正式人员生命周期；
- 每用户独立 OS/容器 Worker；
- Vault/KMS、密钥轮换和生产 PKI；
- 高可用、队列化调度、异机恢复和灾备演练；
- 正式告警通知、生产 SLO、限流、DLP、WORM 审计和安全事件响应；
- 无窗口、最小权限的 Windows 隧道服务化。

这些事项统一维护在 [后续增强事项](../后续规划/后续增强事项.md)。
