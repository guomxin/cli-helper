# 多数据库迁移与验收记录

日期：2026-09-14。已部署并推送，修正版代码 `108bb957f00b`。查询、比较、导出及服务端下载隔离验收通过；浏览器文件保存已由用户确认通过，按用户确认范围收尾。

## 使用与接入

管理控制台新增“数据库”入口。每个数据源独立保存连接、开放对象、能力包及查询预算；先保存草稿，再执行只读预检，最后启用。编辑草稿及失败预检不替换正在使用的配置。停用后目录、查询、文件下载均拒绝此源，其他源继续工作。保存、预检、启停采用版本校验并记录管理员及变更原因。

首期支持 PostgreSQL，新增源要求 `verify-full` TLS；服务使用 PostgreSQL 默认根证书位置或服务环境 `PGSSLROOTCERT`。已有泰华内网源保留原传输配置。通用包提供 schema、自由只读 SQL、CSV；泰华日志包追加目录、日志、正文和评论能力。开放对象填写完整的 `schema.table`，源库账号必须只读且无写入、临时表及高权限角色权限。

账号按“中央账号 × 数据源 × 能力”授权。Token 只承担身份认证，授权更改即时生效，无需重新签发或重启。保留原授权范围；CSV 导出独立授权，下载还校验原查询权限、所有者、源版本和授权版本。用户批准后已给 guomao、lishiyu 增加泰华日志库导出与下载权限，分别由 7/6 项增为 8/7 项；已从控制台读回，lishiyu 的自由 SQL 仍未授权。

智能体先用 `database_capabilities` 选择数据源，再以 `database_execute` 明确 `source_id`。两个时间窗口比较使用 `database.free.read`，不再创建专用比较计划。取消按历史结果 ID 读取原始分析结果；追踪 query_id 不提供读取入口。

CSV 由 `database.report.export` 根据查询定义重新查询，支持自由 SQL、日志明细与数值统计。`request_key` 防止重复执行，相同键不同定义拒绝，失败键不会自动重放。`database.report.download` 交付加密保存的短期文件；文件保留最多 24 小时，认证下载窗口最多 10 分钟。源或授权版本变化后旧文件失效，恢复授权不会复活旧文件。

## 迁移与退役

旧 `analytics/source.json` 和加密凭据只在目标源不存在时导入 `database/catalog.sqlite3`；旧 `database_grants` 仅向 `(账号,taihua_primary)` 补齐缺失记录。已迁移源和授权撤销记录优先，重复启动不会重置它们。历史源配置、凭据和历史结果保留，不删除源库数据。

`taihua:analytics:*` 不再可签发；对应 MCP 工具、中央能力、专用比较变换和计划目录入口已移除。旧 Token 仍可认证，其旧 scope 不产生数据库授权；旧直接能力调用及旧历史下载明确拒绝。普通泰华业务 API 不变。进一步清理已删除旧分析包、命令行权限候选、旧运行时及登录续跑、专用比较分支和旧 CSV 新建交付路径。原始历史文件及已有数据库字段保留，不再被旧运行时读取或清理；旧调用/下载仅保留明确拒绝，旧文件通知仍禁止匿名交付。迁移解析器独立放在 `database/legacy_source.py`，仅用于首次读取旧源配置。

发布前检查旧分析运行中计划和未过期文件均为 0。生产迁移前备份位于 `/home/guomao/agentbridge/data/migration-backups/database-20260914`。回滚必须同时评估旧能力会重新暴露的影响：先停用新数据库入口，保留现有 grants/catalog/reports 数据，使用固定部署入口回滚代码；禁止把旧授权备份直接覆盖现行授权。回滚后若需恢复旧分析入口，应独立确认授权边界。

## 已执行验证

- Python 最终候选全量：962 通过、5 跳过、1009 子测试通过。此前发现新 OpenClaw 版本未登记 L3，已修复。
- OpenClaw 插件 198 测试通过；Workspace Gateway/UI 32 测试通过；Python 编译与依赖检查通过。
- 两个一次性真实 PostgreSQL 数据库：经 TLS 预检启用后各自返回 2 行、1 行；两窗口合计为 2、4；跨账号和未知源拒绝；CSV 2 行；停用一个源后下载拒绝，另一源正常。测试容器和测试凭据已清理，无生产源配置变更。
- 重点验证覆盖多源授权迁移、撤销不复活、源对象白名单、预检与版本竞争、分页绑定、CSV 公式防护/幂等/失效，以及旧入口退役。

测试日志保存在本地 `output/database-multisource-20260914/`。生产仅有现存泰华源，未把测试库冒充第二个生产源。

## 线上验收

主版本 `0c9e48202604` 已部署，目录实测为 149 个 MCP 工具。控制台数据源、账号授权、接入表单以及 390px 手机宽度布局检查通过。lishiyu 在 Workspace 用“统计泰华日志库9月6日的日报条数和工时。”得到 7 条、63.5 小时，与服务端只读核对一致。

实测发现并修复：Workspace 文件卡未接入新的认证下载路径，导致已生成 CSV 误显示已过期；增加路径白名单及拒绝恶意/旧路径的渲染回归。启动恢复后的 runId 被旧防重复规则拒绝，导致目录回答已经生成但派发持续等待；只接受网关确认终止且无工具执行的首轮恢复关联，继续拒绝无关联、错误关联和已有工具活动的重复接收。Workspace 完整 48 测试、3 子测试通过；文件卡 6 项测试通过。

修正版已通过固定发布入口完成推送和部署，插件 `0.4.94`、149 个 MCP 工具，中央/Workspace 健康、双用户身份及端点隔离检查通过；本次修正未重启 Gateway。

guomao 在 Workspace 用“比较9月1日至6日和7日至12日的日报条数与工时。”执行 `database.schema` 和 `database.free.read`，得到 328 条/2674.0 小时与 299 条/2422.5 小时。12:04 服务端重查一致（早前核对时第二窗口为 295 条，源库在实测期间变化，因此未将旧值视为固定快照）。其后“把这个对比导出成CSV。”生成 2 行、107 字节文件；lishiyu 的单日统计导出 1 行、148 字节。两账号均由旧 Token 正常使用新授权。

服务端认证下载读回验证文件字节和 CSV 行数，并证明交换账号后拒绝下载：guomao 文件 SHA-256 为 `4c0a874ec62c4feffb13c2fc3e5b24000273defadbaea9e9011d1ef5913c6a91`；lishiyu 文件为 `fec408c4db995414ac0637fd4b048f18a620f4c542807515620226b61c6f203a`。Workspace 正确展示新认证下载按钮；点击后打开空白下载页，浏览器自动化策略禁止读取该页，未绕过浏览器策略。用户随后确认已点击保存，所提示的 lishiyu 文件 `taihua_primary-20260914-040125.csv` 浏览器保存项按用户确认记为通过；未另行检查本地文件，也不据此声称两个账号的文件均已落盘。本期按用户确认范围收尾。

首条启动恢复测试的目录任务已有权威最终回答，旧派发记录丢失恢复关联；按网关日志及原会话记录修复该测试的 runId 关联并写入事件，未重发请求。其后排队请求在接收前已过期，确认未执行后才重新发起上述比较；新的比较和导出均正常完成。

lishiyu 以“能按结果ID读取以前的分析吗？”测试，Workspace 明确回答不能反查历史结果，只能按原条件重新查询。控制台本轮未记录前端错误。第一阶段未接入非 PostgreSQL 数据库，也未创建虚假的第二个生产源。

## 退役实现清理复验

2026-09-14 按用户要求进一步删除旧分析实现。PostgreSQL 权限检查移入 `database/postgres_policy.py`，不再绑定泰华表结构；运维清点脚本改从现行数据源目录读取，必须指定 `--source-id`。旧会话人员映射不再创建或更新，已有字段原样保留。

本轮全量 Python 952 通过、5 跳过、1007 子测试通过；Workspace 页面 33 测试通过。新增退役检查覆盖 CLI 选项、旧模块不可加载、旧 CSV 禁止新建及历史文件保留；原有通用 HTTP 读取上限测试迁出旧分析测试文件继续执行。线上发布复验结果以本次固定发布记录为准。

### 旧 Token 权限显示补漏

用户指出“用户与令牌”仍显示旧 Scope，Chrome 实测确认有效及已撤销 Token 均存在此问题。原因是 Token 读取直接返回签发时保存的 `scopes_json`。现统一按现行 Scope 白名单生成有效权限，覆盖管理页面、Token 校验及客户端解析；历史签发元数据原样保留，原 Token 无需更换。身份、管理控制台、MCP 与退役相关 97 测试、25 子测试通过。
