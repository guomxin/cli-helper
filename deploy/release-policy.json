{
  "schemaVersion": "agentbridge.release-policy.v1",
  "compatibleFrom": [
    "01dfd176c128"
  ],
  "dataCompatibility": "no-migration",
  "reason": "Reviewed against deployed 01dfd176c128: this release changes only local validation orchestration, GitHub Actions scheduling, release documentation, and tests. It does not change the AgentBridge runtime, OpenClaw integration, SQLite schema, or stored-data interpretation, so the deployed release remains a compatible rollback target."
}
