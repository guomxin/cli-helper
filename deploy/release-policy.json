{
  "schemaVersion": "agentbridge.release-policy.v1",
  "compatibleFrom": [
    "355f9a628062"
  ],
  "dataCompatibility": "no-migration",
  "reason": "Reviewed against deployed 355f9a628062: this release adds read-only approval context to trusted field cards, warns when that context changes before authorization, and removes duplicate ancestor text from OA opinion parsing. It does not change the SQLite schema or stored-data interpretation, so the deployed release remains a compatible rollback target."
}
