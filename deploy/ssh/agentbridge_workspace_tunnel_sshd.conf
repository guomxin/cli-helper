# Reap half-open AgentBridge reverse-tunnel sessions after laptop sleep or
# abrupt network changes. This currently targets the dedicated root key user;
# migrate the Match block when the tunnel moves to a restricted service user.
Match User root
    ClientAliveInterval 5
    ClientAliveCountMax 2

Match all
